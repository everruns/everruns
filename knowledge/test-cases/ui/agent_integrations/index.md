# Agent Integrations (UI)

* [TC001: Agent Integrations tab](TC001_integrations_tab.md) - Verifies the Agent detail Integrations tab lists the agent's endpoints and triggers, publishes an endpoint from its row, and replaces the separate Triggers and Integrate tabs.
* [TC002: Agent endpoint full-page editors](TC002_endpoint_routes.md) - Verifies the agent-scoped endpoint create and edit routes, including that they return to the Integrations tab rather than the App page.
* [TC003: Slack endpoint setup](TC003_slack_endpoint_setup.md) - Verifies a Slack endpoint can create its app from an endpoint-scoped manifest and receive a signed inbound event.
