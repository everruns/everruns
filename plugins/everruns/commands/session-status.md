---
description: Poll status and recent events for an Everruns session
argument-hint: "<session_id>"
---

Call `session_get_status` for the given `session_id`. Show session status, the
latest agent message, and a short list of recent events.

Arguments: `$ARGUMENTS`
