# fetchkit

A small HTTP client library. This copy is a fixture for an Everruns example and
the code is illustrative rather than complete.

Modules: `client` issues requests, `retry` decides when to try again, and
`cache` stores responses.
