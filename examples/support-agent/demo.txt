MODEL: gpt-5.6-terra

QUESTION
Customer cust_mfa reset their password but still cannot sign in. Diagnose the next safe step; do not ask for secrets.

> lookup_customer
  customer_id: cust_mfa

< lookup_customer: OK
authenticator_available: false
lockout_minutes: 0
mfa_enabled: true
password_reset: "completed"
recovery_codes_available: false
verified: true

> read_support_policy

< read_support_policy: OK
# Account recovery policy (fictional service)
- Never request passwords, one-time codes, or recovery-code contents.
- A password reset does not disable MFA.
- When MFA is enabled and neither authenticator nor recovery codes are available,
  direct the customer to verified identity recovery at https://support.example.com/recovery.
  Support must not disable MFA based solely on a chat request.
- An active lockout must expire before retrying. Repeated reset requests do not clear it.
- With no MFA blocker or lockout, retry in a private browser window to exclude stale
  cookies/autofill; escalate to support if that fails.

ANSWER

Your password reset completed, but MFA is still enabled. The account has no active lockout; however, no authenticator or recovery codes are available.

Next safe step: start verified identity recovery at https://support.example.com/recovery to restore access. Do not share passwords or codes.

If recovery cannot be completed, contact support through the recovery process; chat support cannot disable MFA based only on a request.

Completed: 2 iterations, 2 tool calls
