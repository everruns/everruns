---
title: Runtime Compatibility
description: Compatibility guidance for existing everruns-runtime 0.17.x applications and low-level execution hosts.
slug: features/runtime
sidebar:
  hidden: true
---

New application code should use the [Everruns Framework](/framework/) through
the application-facing `everruns` crate. It provides agents, open
model/provider configuration, typed tools, sessions, events, cancellation,
files, MCP, and context inspection without requiring applications to assemble a
low-level execution host.

Advanced system integrators use `everruns` plus `everruns-host` and focused MCP,
provider, platform, or integration crates. Existing `everruns-runtime` 0.17.x
applications remain usable while they follow the authoritative
[runtime migration guide](/framework/runtime-compatibility/).
