---
import DefaultHeader from "@astrojs/starlight/components/Header.astro";

const sidebarTopics = Astro.locals.starlightSidebarTopics;
const topics = sidebarTopics?.topics ?? [];
const hasTopics = topics.length > 0;
---

<DefaultHeader {...Astro.props} />

{hasTopics && (
  <nav class="topic-tabs" aria-label="Documentation sections">
    <ul>
      {topics.map((topic: any) => (
        <li>
          <a
            href={topic.link}
            class:list={{ active: topic.isCurrent }}
            aria-current={topic.isCurrent ? "true" : undefined}
          >
            {topic.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
)}

<style>
  .topic-tabs {
    grid-column: 1 / -1;
    border-bottom: 1px solid var(--sl-color-hairline-light);
    padding: 0 var(--sl-nav-pad-x);
  }

  .topic-tabs ul {
    display: flex;
    gap: 0;
    list-style: none;
    padding: 0;
    margin: 0;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
  }

  .topic-tabs a {
    display: block;
    padding: 0.5rem 1rem;
    font-size: var(--sl-text-sm);
    font-weight: 500;
    color: var(--sl-color-gray-2);
    text-decoration: none;
    white-space: nowrap;
    border-bottom: 2px solid transparent;
    transition: color 0.15s, border-color 0.15s;
  }

  .topic-tabs a:hover {
    color: var(--sl-color-white);
  }

  .topic-tabs a.active {
    color: var(--sl-color-text-accent);
    border-bottom-color: var(--sl-color-text-accent);
  }

  /* Hide on mobile — use sidebar topics there */
  @media (max-width: 49.99rem) {
    .topic-tabs {
      display: none;
    }
  }
</style>
