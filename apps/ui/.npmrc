# pnpm: flat node_modules to match npm-compatible resolution.
# Some packages (and Next.js's Turbopack module resolution) expect
# transitive deps to be reachable directly; isolated linking would
# require declaring every transitive import as a direct dependency.
node-linker=hoisted
