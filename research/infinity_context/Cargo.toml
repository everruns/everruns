[package]
name = "everruns-evals"
version = "0.1.0"
edition = "2024"
description = "Evaluation framework for context management strategies"

[[bin]]
name = "eval"
path = "src/main.rs"

[dependencies]
# Core types from main crate
everruns-core = { path = "../../crates/core" }

# LLM provider drivers
everruns-anthropic = { path = "../../crates/anthropic" }
everruns-openai = { path = "../../crates/openai" }

# Async runtime
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# CLI
clap = { version = "4", features = ["derive"] }

# HTTP client for LLM APIs
reqwest = { version = "0.12", features = ["json", "stream"] }

# Utilities
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1"
dotenvy = "0.15"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# For colored terminal output
colored = "2"

# For pattern matching in scorers
regex = "1"

[dev-dependencies]
tempfile = "3.24.0"
tokio-test = "0.4"
