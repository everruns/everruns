[
  {
    "category": "sessions",
    "cli": "sessions participants add",
    "description": "Add a member participant to a session.",
    "input_schema": {
      "$defs": {
        "AddSessionParticipantRequest": {
          "description": "Request to add a participant to a session.",
          "properties": {
            "agent_id": {
              "description": "Agent to add when `kind` is `agent`.",
              "example": "agent_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "$ref": "#/$defs/SessionParticipantKind",
              "description": "Participant kind to add."
            },
            "role": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SessionParticipantRole",
                  "description": "Participant role. Omit for ordinary members. Host assignment is managed by session creation."
                }
              ]
            }
          },
          "required": [
            "kind"
          ],
          "type": "object"
        },
        "SessionParticipantKind": {
          "description": "Kind of actor participating in a session.",
          "enum": [
            "agent",
            "user"
          ],
          "type": "string"
        },
        "SessionParticipantRole": {
          "description": "Role a participant has inside a session.",
          "enum": [
            "host",
            "member"
          ],
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/AddSessionParticipantRequest",
          "description": "Participant to add."
        },
        {
          "properties": {
            "session_id": {
              "description": "Session that receives the participant.",
              "type": "string"
            }
          },
          "required": [
            "session_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "add_session_participant",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/participants",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents analyze",
    "description": "Run advisory checks (built-in rules plus LLM analysis) against an agent configuration.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "BuiltinTool": {
          "description": "Built-in tool configuration\n\nNote: The `kind` field has been removed. Tools are now identified\nsolely by their `name` field, and execution happens via the ToolRegistry\nwhich looks up tools by name.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering (e.g., \"Get Current Time\" for `get_current_time`)",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for registry lookup)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            },
            "policy": {
              "$ref": "#/$defs/ToolPolicy",
              "description": "Tool policy (auto or requires_approval)"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ClientSideTool": {
          "description": "Client-side tool - executed by the client, not the server\nThe server pauses execution and waits for the client to submit results.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for correlation)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "DeferrablePolicy": {
          "description": "Controls whether a tool's full schema can be deferred (tool_search).\n\nWhen tool_search is active and a model supports it, tools marked as\n`Automatic` or `Always` will have `defer_loading: true` set, meaning\nonly the name+description are sent upfront and full parameter schemas\nare loaded on-demand by the model.",
          "enum": [
            "never",
            "automatic",
            "always"
          ],
          "type": "string"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "SideEffectClass": {
          "description": "How many times a tool call may safely be executed given the same inputs.\n\nUsed by the durable Act activity (EVE-530) to decide what to do when a\nprior execution attempt left a `running` claim in `durable_tool_results`:\n\n* `Pure` / `Idempotent` — the running claim is stale; re-execute freely.\n* `AtMostOnce` — never re-execute from a stale running claim; settle it\n  as `interrupted` and surface an uncertain result to the model instead.\n\nWhen unset (`None`), the conservative default is `AtMostOnce`.",
          "enum": [
            "Pure",
            "Idempotent",
            "AtMostOnce"
          ],
          "type": "string"
        },
        "ToolDefinition": {
          "description": "Tool definition in agent configuration",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/BuiltinTool",
                  "description": "Built-in tool - executed by the worker via ToolRegistry"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "builtin"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Built-in tool - executed by the worker via ToolRegistry"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ClientSideTool",
                  "description": "Client-side tool - executed by the client, not the server"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "client_side"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Client-side tool - executed by the client, not the server"
            }
          ]
        },
        "ToolHints": {
          "description": "Semantic hints describing a tool's behavioral properties.\n\nFollows the MCP tool annotations convention (readOnlyHint, destructiveHint,\nidempotentHint, openWorldHint) plus everruns-specific hints. All fields are\noptional booleans — `None` means \"unknown/unspecified\". Consumers should\ntreat `None` as the conservative default (e.g., assume not readonly, assume\nnot idempotent).\n\nThese hints are informational — they do not enforce policy. Use `ToolPolicy`\nfor execution gating (auto vs requires_approval).",
          "properties": {
            "capability_id": {
              "description": "Capability that contributed this tool definition.\n\nReporting uses this attribution only as metadata. It must never contain\ntool arguments, results, prompts, or any other sensitive payload.",
              "type": [
                "string",
                "null"
              ]
            },
            "capability_name": {
              "description": "Human-readable capability name snapshot for reporting.",
              "type": [
                "string",
                "null"
              ]
            },
            "concurrency_class": {
              "description": "Scheduling conflict key. Tool calls within the same act batch that share\na non-empty `concurrency_class` are executed sequentially in arrival\norder; calls in different classes (or with no class) run concurrently.\n\nSet this on tools that mutate shared session state so that, e.g., two\nfile writes or two SQL mutations in one batch do not race. Read-only\ntools should leave this `None` so they always parallelize. See\n`everruns-engine`'s tool scheduler for how the act phase consumes it.",
              "type": [
                "string",
                "null"
              ]
            },
            "cpu_bound": {
              "description": "Tool performs significant CPU-bound or otherwise non-yielding work in\nprocess (e.g. an in-process interpreter). When true, the act scheduler\nruns the call on its own task (`tokio::spawn`) so a long CPU burst does\nnot starve the cooperative polling of I/O-bound tools in the same batch.\n\nDistinct from `long_running`, which describes wall-clock time for\nI/O-bound work (those tools yield at await points and need no offload).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "destructive": {
              "description": "Tool may irreversibly destroy or delete data.\nSubset of non-readonly — a tool can be non-readonly (writes) without\nbeing destructive (e.g., create/update operations).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "idempotent": {
              "description": "Calling the tool repeatedly with the same arguments produces the same\neffect. Safe to retry on transient failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "long_running": {
              "description": "Tool may take significant time to complete (> ~5s typical).\nUseful for clients to show progress indicators and set timeouts.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "metadata": {
              "description": "Host-owned annotations that core does not interpret.\n\nThe typed hints above are the vocabulary core itself reasons about. This\nis the escape hatch for everything a *host* wants to carry alongside a\ntool — risk tiers for an approval UI, presentation hints, an embedder's\nrouting keys — without adding a field to core for each one. Core reads\nnothing here and no driver sends it to a provider; it travels with the\ndefinition so a consumer sees it at the point of decision (e.g. a\n`PreToolUseHook` gating on what the tool declared).\n\nThe schema belongs to whoever writes it. Never put credentials or other\nsensitive payload here: like the rest of the definition, it is persisted\nand surfaced to clients."
            },
            "narration_noun": {
              "description": "Entity noun for operation-based narration (e.g. \"agent\", \"harness\").\nWhen set, the narration system reads the `operation` argument and\nproduces verb-based narration like \"Created agent: Neon Cartographer\"\ninstead of the generic \"Ran Manage Agents\".",
              "type": [
                "string",
                "null"
              ]
            },
            "open_world": {
              "description": "Tool interacts with external entities beyond the local system\n(network calls, third-party APIs, cloud services).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "persist_output": {
              "description": "Tool output should be persisted to session VFS before truncation.\nWhen set, the `tool_output_persistence` capability (EVE-222, EVE-245) writes\nstdout to `/outputs/{tool_call_id}.stdout` and stderr to\n`/outputs/{tool_call_id}.stderr`, injecting `full_output`, `total_lines`,\nand `output_files` into the result.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "readonly": {
              "description": "Tool does not modify any state (read-only queries, lookups).\nWhen true: safe to call speculatively, result can be cached.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "requires_secrets": {
              "description": "Tool requires API keys, credentials, or other secrets to function.\nUseful for UI to show connection prompts and for LLMs to anticipate\nauthentication failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "side_effect_class": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SideEffectClass",
                  "description": "Replay-safety class used by the durable Act activity (EVE-530).\n\nControls what happens when a worker reclaims a stale `running` claim:\n`Pure`/`Idempotent` tools are re-executed; `AtMostOnce` tools are\nsettled as `interrupted` to prevent double side-effects.\n\n`None` is treated conservatively as `AtMostOnce`."
                }
              ]
            },
            "supports_background": {
              "description": "Tool supports detached background execution via `spawn_background`.\nWhen true, the tool may be executed asynchronously outside the current\nforeground tool call and report status back later.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ToolPolicy": {
          "description": "Tool policy determines how tool calls are handled",
          "enum": [
            "auto",
            "requires_approval",
            "client_side"
          ],
          "type": "string"
        }
      },
      "description": "Run advisory checks (built-in rules + LLM analysis) against an agent shape.",
      "properties": {
        "capabilities": {
          "items": {
            "$ref": "#/$defs/AgentCapabilityConfig"
          },
          "type": "array"
        },
        "mcp_servers": {
          "$ref": "#/$defs/BTreeMap"
        },
        "system_prompt": {
          "type": [
            "string",
            "null"
          ]
        },
        "tools": {
          "items": {
            "$ref": "#/$defs/ToolDefinition"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "method": "POST",
    "name": "analyze_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/analyze",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions archive",
    "description": "Archive a session so it drops out of default lists.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "PUT",
    "name": "archive_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/archive",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Enqueue missing reporting projection work from canonical sources.",
    "input_schema": {
      "$defs": {
        "ReportingBackfillRequest": {
          "description": "Request body for the `reporting_backfill` operation — enqueues source\nrows into the reporting outbox for the projector to re-materialize.",
          "properties": {
            "limit": {
              "description": "Maximum number of outbox rows to enqueue across all source types. Defaults to 1000.",
              "example": 5000,
              "format": "int64",
              "type": "integer"
            }
          },
          "type": "object"
        }
      },
      "$ref": "#/$defs/ReportingBackfillRequest"
    },
    "method": "POST",
    "name": "backfill_reporting",
    "output_shape": "unknown",
    "path": "/v1/reports/admin/backfill",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "session_storage",
    "cli": null,
    "description": "Encrypt and store multiple session secrets in one request.",
    "input_schema": {
      "properties": {
        "secrets": {
          "additionalProperties": {
            "type": "string"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "secrets"
      ],
      "type": "object"
    },
    "method": "PUT",
    "name": "batch_set_session_secrets",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/storage/secrets",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Bulk update scores for all results in an eval run.",
    "input_schema": {
      "$defs": {
        "BulkUpdateEvalResultScoresItem": {
          "properties": {
            "result_id": {
              "type": "string"
            },
            "scores": {
              "items": {
                "$ref": "#/$defs/Score"
              },
              "type": "array"
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/ExternalScoreStatus",
                  "description": "Current lifecycle status."
                }
              ]
            }
          },
          "required": [
            "result_id",
            "scores"
          ],
          "type": "object"
        },
        "BulkUpdateEvalRunScoresRequest": {
          "properties": {
            "metadata": {
              "description": "Free-form metadata attached to this resource."
            },
            "results": {
              "items": {
                "$ref": "#/$defs/BulkUpdateEvalResultScoresItem"
              },
              "type": "array"
            }
          },
          "required": [
            "results"
          ],
          "type": "object"
        },
        "ExternalScoreStatus": {
          "enum": [
            "passed",
            "failed",
            "errored"
          ],
          "type": "string"
        },
        "Score": {
          "description": "Result from a single scorer evaluation.",
          "properties": {
            "pass": {
              "description": "Whether this scorer passed.",
              "type": "boolean"
            },
            "reason": {
              "description": "Human-readable explanation.",
              "type": "string"
            },
            "value": {
              "description": "Score value 0.0–1.0.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "pass",
            "value",
            "reason"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/BulkUpdateEvalRunScoresRequest"
        },
        {
          "properties": {
            "eval_id": {
              "type": "string"
            },
            "run_id": {
              "type": "string"
            }
          },
          "required": [
            "eval_id",
            "run_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "bulk_update_eval_run_scores",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/scores",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Cancel an eval run.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "run_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "cancel_eval_run",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/cancel",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions cancel",
    "description": "Cancel the currently executing turn in a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "cancel_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/cancel",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "Request cooperative cancellation of a session task.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "task_id": {
          "description": "Task's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "task_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "cancel_session_task",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tasks/{task_id}/cancel",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents check-name",
    "description": "Check whether an agent name is available.",
    "input_schema": {
      "description": "Check whether an agent name is available.",
      "properties": {
        "exclude_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "check_agent_name",
    "output_shape": "unknown",
    "path": "/v1/agents/check-name",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Check budget status for a session-scoped budget.",
    "input_schema": {
      "properties": {
        "budget_id": {
          "type": "string"
        }
      },
      "required": [
        "budget_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "check_budget",
    "output_shape": "unknown",
    "path": "/v1/budgets/{budget_id}/check",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Check whether a harness name is available.",
    "input_schema": {
      "description": "Check whether a harness name is available.",
      "properties": {
        "exclude_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "check_harness_name",
    "output_shape": "unknown",
    "path": "/v1/harnesses/check-name",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "providers",
    "cli": null,
    "description": "Check a provider API key without storing it.",
    "input_schema": {
      "$defs": {
        "DriverId": {
          "description": "LLM provider type. Built-in: openai, openrouter, azure_openai, openai_completions, anthropic, gemini, llmsim, bedrock, mai, fireworks, meta. Any other string is treated as an embedder-defined external provider.",
          "type": "string"
        }
      },
      "description": "Check a candidate credential against the provider before it is stored.\n\nDeliberately takes the credential inline rather than a provider id: the\npoint is to find out whether a key works *before* any provider row exists\n(setup) or before an edit overwrites a working one. Nothing is persisted.",
      "properties": {
        "api_key": {
          "description": "The credential to probe. Typed multi-field credentials are assembled\ninto this single document at the HTTP boundary, as for create.",
          "type": "string"
        },
        "base_url": {
          "description": "Optional custom endpoint. Validated exactly as on create — this issues\na real outbound request.",
          "type": [
            "string",
            "null"
          ]
        },
        "provider_type": {
          "$ref": "#/$defs/DriverId"
        }
      },
      "required": [
        "provider_type",
        "api_key"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "check_provider_credentials",
    "output_shape": "unknown",
    "path": "/v1/providers/check-credentials",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Check all budgets for a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "check_session_budgets",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/budget-check",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents copy",
    "description": "Copy an agent. Generates a unique name.",
    "input_schema": {
      "description": "Copy an agent. Generates a unique name ({name}-copy, -copy-2, etc.)",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "copy_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/{id}/copy",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Copy a harness. Generates a unique name.",
    "input_schema": {
      "description": "Copy a harness. Generates a unique name ({name}-copy, -copy-2, etc.)",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "copy_harness",
    "output_shape": "unknown",
    "path": "/v1/harnesses/{id}/copy",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "files",
    "cli": null,
    "description": "Copy a file in the session filesystem.",
    "input_schema": {
      "$defs": {
        "CopyFileRequest": {
          "description": "Request to copy a file",
          "properties": {
            "dst_path": {
              "description": "Destination path (relative to the workspace filesystem root).",
              "example": "docs/runbooks/refund-30-days.md",
              "type": "string"
            },
            "src_path": {
              "description": "Source path (relative to the workspace filesystem root).",
              "example": "templates/runbook.md",
              "type": "string"
            }
          },
          "required": [
            "src_path",
            "dst_path"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CopyFileRequest"
        },
        {
          "properties": {
            "session_id": {
              "description": "Session's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "session_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "copy_workspace_file",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/_/copy",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents create",
    "description": "Create a new agent with a name, system prompt, and optional capabilities. Agent name must contain only lowercase letters, digits, and hyphens (e.g. joke-telling-agent).",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "BuiltinTool": {
          "description": "Built-in tool configuration\n\nNote: The `kind` field has been removed. Tools are now identified\nsolely by their `name` field, and execution happens via the ToolRegistry\nwhich looks up tools by name.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering (e.g., \"Get Current Time\" for `get_current_time`)",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for registry lookup)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            },
            "policy": {
              "$ref": "#/$defs/ToolPolicy",
              "description": "Tool policy (auto or requires_approval)"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ClientSideTool": {
          "description": "Client-side tool - executed by the client, not the server\nThe server pauses execution and waits for the client to submit results.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for correlation)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ConversationStarter": {
          "description": "A conversation starter shown on a fresh Platform Chat thread.\nSelecting one inserts its text into the composer. `icon` reuses the\nharness icon name set (`HarnessIcon`); unknown names fall back to the\ndefault glyph.",
          "properties": {
            "icon": {
              "description": "Optional icon name from the harness icon set.",
              "example": "zap",
              "type": [
                "string",
                "null"
              ]
            },
            "text": {
              "description": "Prompt text inserted into the composer when selected.",
              "example": "Triage the newest P1",
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "type": "object"
        },
        "DeferrablePolicy": {
          "description": "Controls whether a tool's full schema can be deferred (tool_search).\n\nWhen tool_search is active and a model supports it, tools marked as\n`Automatic` or `Always` will have `defer_loading: true` set, meaning\nonly the name+description are sent upfront and full parameter schemas\nare loaded on-demand by the model.",
          "enum": [
            "never",
            "automatic",
            "always"
          ],
          "type": "string"
        },
        "InitialFile": {
          "description": "Starter file copied into a new session from an agent or harness.",
          "properties": {
            "content": {
              "description": "File content: plain text or base64-encoded binary.",
              "type": "string"
            },
            "encoding": {
              "description": "Content encoding: `text` or `base64`.",
              "type": "string"
            },
            "is_readonly": {
              "description": "Prevent session-side edits or deletes when true.",
              "type": "boolean"
            },
            "path": {
              "description": "Absolute path within the session workspace. `/workspace` prefix is accepted.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "NetworkAccessList": {
          "description": "Network access list controlling which hosts/URLs an agent session can reach.\n\n- `allowed`: if non-empty, only URLs matching these patterns are permitted.\n- `blocked`: URLs matching these patterns are always denied (takes precedence over allowed).\n\nPattern format:\n- `example.com` — exact domain match (any port, any path)\n- `*.example.com` — domain and all subdomains\n- `https://example.com/api/` — exact URL prefix (scheme + host + path)",
          "example": {
            "allowed": [
              "*.example.com",
              "https://api.acme.com/"
            ],
            "blocked": [
              "169.254.169.254"
            ]
          },
          "properties": {
            "allowed": {
              "description": "Allowed host patterns. If non-empty, only matching URLs are permitted.\nAn empty list means \"no restriction from this layer\" (inherit parent).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "blocked": {
              "description": "Blocked host patterns. Always denied, even if matched by `allowed`.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "SideEffectClass": {
          "description": "How many times a tool call may safely be executed given the same inputs.\n\nUsed by the durable Act activity (EVE-530) to decide what to do when a\nprior execution attempt left a `running` claim in `durable_tool_results`:\n\n* `Pure` / `Idempotent` — the running claim is stale; re-execute freely.\n* `AtMostOnce` — never re-execute from a stale running claim; settle it\n  as `interrupted` and surface an uncertain result to the model instead.\n\nWhen unset (`None`), the conservative default is `AtMostOnce`.",
          "enum": [
            "Pure",
            "Idempotent",
            "AtMostOnce"
          ],
          "type": "string"
        },
        "ToolDefinition": {
          "description": "Tool definition in agent configuration",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/BuiltinTool",
                  "description": "Built-in tool - executed by the worker via ToolRegistry"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "builtin"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Built-in tool - executed by the worker via ToolRegistry"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ClientSideTool",
                  "description": "Client-side tool - executed by the client, not the server"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "client_side"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Client-side tool - executed by the client, not the server"
            }
          ]
        },
        "ToolHints": {
          "description": "Semantic hints describing a tool's behavioral properties.\n\nFollows the MCP tool annotations convention (readOnlyHint, destructiveHint,\nidempotentHint, openWorldHint) plus everruns-specific hints. All fields are\noptional booleans — `None` means \"unknown/unspecified\". Consumers should\ntreat `None` as the conservative default (e.g., assume not readonly, assume\nnot idempotent).\n\nThese hints are informational — they do not enforce policy. Use `ToolPolicy`\nfor execution gating (auto vs requires_approval).",
          "properties": {
            "capability_id": {
              "description": "Capability that contributed this tool definition.\n\nReporting uses this attribution only as metadata. It must never contain\ntool arguments, results, prompts, or any other sensitive payload.",
              "type": [
                "string",
                "null"
              ]
            },
            "capability_name": {
              "description": "Human-readable capability name snapshot for reporting.",
              "type": [
                "string",
                "null"
              ]
            },
            "concurrency_class": {
              "description": "Scheduling conflict key. Tool calls within the same act batch that share\na non-empty `concurrency_class` are executed sequentially in arrival\norder; calls in different classes (or with no class) run concurrently.\n\nSet this on tools that mutate shared session state so that, e.g., two\nfile writes or two SQL mutations in one batch do not race. Read-only\ntools should leave this `None` so they always parallelize. See\n`everruns-engine`'s tool scheduler for how the act phase consumes it.",
              "type": [
                "string",
                "null"
              ]
            },
            "cpu_bound": {
              "description": "Tool performs significant CPU-bound or otherwise non-yielding work in\nprocess (e.g. an in-process interpreter). When true, the act scheduler\nruns the call on its own task (`tokio::spawn`) so a long CPU burst does\nnot starve the cooperative polling of I/O-bound tools in the same batch.\n\nDistinct from `long_running`, which describes wall-clock time for\nI/O-bound work (those tools yield at await points and need no offload).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "destructive": {
              "description": "Tool may irreversibly destroy or delete data.\nSubset of non-readonly — a tool can be non-readonly (writes) without\nbeing destructive (e.g., create/update operations).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "idempotent": {
              "description": "Calling the tool repeatedly with the same arguments produces the same\neffect. Safe to retry on transient failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "long_running": {
              "description": "Tool may take significant time to complete (> ~5s typical).\nUseful for clients to show progress indicators and set timeouts.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "metadata": {
              "description": "Host-owned annotations that core does not interpret.\n\nThe typed hints above are the vocabulary core itself reasons about. This\nis the escape hatch for everything a *host* wants to carry alongside a\ntool — risk tiers for an approval UI, presentation hints, an embedder's\nrouting keys — without adding a field to core for each one. Core reads\nnothing here and no driver sends it to a provider; it travels with the\ndefinition so a consumer sees it at the point of decision (e.g. a\n`PreToolUseHook` gating on what the tool declared).\n\nThe schema belongs to whoever writes it. Never put credentials or other\nsensitive payload here: like the rest of the definition, it is persisted\nand surfaced to clients."
            },
            "narration_noun": {
              "description": "Entity noun for operation-based narration (e.g. \"agent\", \"harness\").\nWhen set, the narration system reads the `operation` argument and\nproduces verb-based narration like \"Created agent: Neon Cartographer\"\ninstead of the generic \"Ran Manage Agents\".",
              "type": [
                "string",
                "null"
              ]
            },
            "open_world": {
              "description": "Tool interacts with external entities beyond the local system\n(network calls, third-party APIs, cloud services).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "persist_output": {
              "description": "Tool output should be persisted to session VFS before truncation.\nWhen set, the `tool_output_persistence` capability (EVE-222, EVE-245) writes\nstdout to `/outputs/{tool_call_id}.stdout` and stderr to\n`/outputs/{tool_call_id}.stderr`, injecting `full_output`, `total_lines`,\nand `output_files` into the result.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "readonly": {
              "description": "Tool does not modify any state (read-only queries, lookups).\nWhen true: safe to call speculatively, result can be cached.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "requires_secrets": {
              "description": "Tool requires API keys, credentials, or other secrets to function.\nUseful for UI to show connection prompts and for LLMs to anticipate\nauthentication failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "side_effect_class": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SideEffectClass",
                  "description": "Replay-safety class used by the durable Act activity (EVE-530).\n\nControls what happens when a worker reclaims a stale `running` claim:\n`Pure`/`Idempotent` tools are re-executed; `AtMostOnce` tools are\nsettled as `interrupted` to prevent double side-effects.\n\n`None` is treated conservatively as `AtMostOnce`."
                }
              ]
            },
            "supports_background": {
              "description": "Tool supports detached background execution via `spawn_background`.\nWhen true, the tool may be executed asynchronously outside the current\nforeground tool call and report status back later.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ToolPolicy": {
          "description": "Tool policy determines how tool calls are handled",
          "enum": [
            "auto",
            "requires_approval",
            "client_side"
          ],
          "type": "string"
        }
      },
      "description": "Request to create a new agent",
      "properties": {
        "capabilities": {
          "description": "Capabilities to enable for this agent with per-agent configuration.\nEach capability has a `ref` (capability ID) and optional `config`.",
          "example": [
            {
              "config": {},
              "ref": "current_time"
            },
            {
              "config": {},
              "ref": "web_fetch"
            }
          ],
          "items": {
            "$ref": "#/$defs/AgentCapabilityConfig"
          },
          "type": "array"
        },
        "default_model_id": {
          "description": "The ID of the default LLM model to use for this agent.\nIf not specified, the system default model will be used.",
          "example": "model_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "A human-readable description of what the agent does.",
          "example": "Handles customer inquiries and support tickets",
          "type": [
            "string",
            "null"
          ]
        },
        "display_name": {
          "description": "Human-readable display name shown in UI.\nFalls back to `name` when absent.",
          "example": "Customer Support Agent",
          "type": [
            "string",
            "null"
          ]
        },
        "harness_id": {
          "description": "Harness ID used as this agent's base execution environment. If omitted,\nthe org's built-in `generic` harness is used.",
          "example": "harness_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "harness_name": {
          "description": "Addressable harness name. Alternative to `harness_id`.",
          "example": "generic",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "Client-supplied agent ID (format: agent_{32-hex}).\nIf not provided, one is auto-generated.",
          "example": "agent_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "initial_files": {
          "description": "Starter files copied into each new session for this agent.",
          "example": [
            {
              "content": "Always respond in formal English.\n",
              "path": "INSTRUCTIONS.md"
            }
          ],
          "items": {
            "$ref": "#/$defs/InitialFile"
          },
          "type": "array"
        },
        "intro_markdown": {
          "description": "Markdown intro shown as an intro box on a fresh Platform Chat thread.\nImages are allowed. Wins over the harness intro. Hidden once the user\ninputs.",
          "example": "Hey, I'm Ava. Ask me anything about your account.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_iterations": {
          "description": "Maximum number of LLM iterations per turn for this agent.",
          "example": 20,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "mcpServers": {
          "$ref": "#/$defs/BTreeMap",
          "description": "Remote MCP servers scoped to this agent."
        },
        "name": {
          "description": "Name, unique per org. Lowercase alphanumeric and hyphens.\nFormat: lowercase alphanumeric and hyphens (e.g. \"customer-support\").",
          "example": "customer-support",
          "type": "string"
        },
        "network_access": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/NetworkAccessList",
              "description": "Network access list controlling which hosts/URLs this agent's sessions can reach.\nIf set, merged with harness and session layers (allowed: intersect, blocked: union).\nExample shape is defined on `NetworkAccessList`."
            }
          ]
        },
        "parallel_tool_calls": {
          "description": "Request-level parallel tool calling preference (EVE-598). `true` signals\nthe provider that parallel tool calls are wanted; `false` requests at\nmost one tool call per turn and forces serial execution. Omit to use the\nprovider default.",
          "example": true,
          "type": [
            "boolean",
            "null"
          ]
        },
        "short_description": {
          "description": "One-line description in simplified Markdown, shown below the chat title\nonce the intro is hidden. Wins over the harness value.",
          "example": "Answers account questions in seconds.",
          "type": [
            "string",
            "null"
          ]
        },
        "starters": {
          "description": "Conversation starters for a fresh Platform Chat thread. Win over the\nharness starters when non-empty. `icon` reuses the harness icon set.",
          "items": {
            "$ref": "#/$defs/ConversationStarter"
          },
          "type": "array"
        },
        "system_prompt": {
          "description": "The system prompt that defines the agent's behavior and capabilities.\nThis is sent as the first message in every conversation.",
          "example": "You are a helpful customer support agent. Be polite and professional.",
          "type": "string"
        },
        "tags": {
          "description": "Tags for organizing and filtering agents.",
          "example": [
            "support",
            "customer-facing"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tools": {
          "description": "Client-side tools for this agent.\nThese tools are sent to the LLM but executed by the client, not the server.",
          "example": [
            {
              "description": "Open URL in the user's browser",
              "name": "open_url",
              "parameters": {
                "properties": {
                  "url": {
                    "type": "string"
                  }
                },
                "required": [
                  "url"
                ],
                "type": "object"
              },
              "type": "client_side"
            }
          ],
          "items": {
            "$ref": "#/$defs/ToolDefinition"
          },
          "type": "array"
        }
      },
      "required": [
        "name",
        "system_prompt"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_agent",
    "output_shape": "unknown",
    "path": "/v1/agents",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": null,
    "description": "Create a write-only Agent credential setup requirement for one attached MCP tool parameter. This command never accepts a secret value; the user completes setup in the Agent Credentials UI.",
    "input_schema": {
      "description": "Declare a credential requirement for an agent's attached MCP tool.",
      "properties": {
        "agent_id": {
          "description": "Agent ID, populated from the request path by the HTTP API.",
          "example": "agent_01933b5a000070008000000000000001",
          "type": "string"
        },
        "description": {
          "description": "Optional explanation of how the credential will be used.",
          "example": "Delivers scheduled status notifications",
          "type": [
            "string",
            "null"
          ]
        },
        "label": {
          "description": "Human-readable label shown in the secure setup UI.",
          "example": "Visti channel key",
          "type": "string"
        },
        "mcp_server_name": {
          "description": "Name of an MCP server attached to the agent.",
          "example": "visti",
          "type": "string"
        },
        "parameter_name": {
          "description": "Top-level tool argument to inject outside model context.",
          "example": "channel_key",
          "type": "string"
        },
        "tool_name": {
          "description": "MCP tool whose outbound call requires the credential.",
          "example": "visti_send",
          "type": "string"
        }
      },
      "required": [
        "mcp_server_name",
        "tool_name",
        "parameter_name",
        "label"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_agent_credential_binding",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/credentials",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "Create an ingress endpoint for an agent.",
    "input_schema": {
      "$defs": {
        "ChannelType": {
          "description": "Supported channel types for app distribution.",
          "enum": [
            "slack",
            "ag_ui",
            "schedule",
            "webhook",
            "a2a",
            "fcp",
            "api_endpoint",
            "public_chat"
          ],
          "example": "webhook",
          "type": "string"
        },
        "CreateAgentEndpointRequest": {
          "description": "Request to create an ingress endpoint owned by an Agent.",
          "properties": {
            "channel_config": {
              "description": "Transport-specific endpoint configuration."
            },
            "channel_type": {
              "$ref": "#/$defs/ChannelType",
              "description": "Transport used by the endpoint."
            },
            "enabled": {
              "description": "Whether the endpoint can accept ingress traffic.",
              "type": "boolean"
            }
          },
          "required": [
            "channel_type"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CreateAgentEndpointRequest"
        },
        {
          "properties": {
            "agent_id": {
              "type": "string"
            }
          },
          "required": [
            "agent_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "create_agent_endpoint",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_identities",
    "cli": null,
    "description": "Create a new agent identity (virtual principal for unattended execution).",
    "input_schema": {
      "properties": {
        "avatar_url": {
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "Human-readable description. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "locale": {
          "example": "en-US",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "example": "Ops Bot",
          "type": "string"
        },
        "timezone": {
          "example": "America/Los_Angeles",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_agent_identity",
    "output_shape": "unknown",
    "path": "/v1/agent-identities",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_triggers",
    "cli": null,
    "description": "Create a schedule or webhook trigger for an agent.",
    "input_schema": {
      "$defs": {
        "AgentTriggerType": {
          "description": "The kind of event that fires an agent trigger.",
          "enum": [
            "schedule",
            "webhook"
          ],
          "type": "string"
        },
        "CreateAgentTriggerRequest": {
          "description": "Request to create a trigger on an agent.",
          "properties": {
            "auth": {
              "description": "Shared endpoint auth is not supported by webhook triggers."
            },
            "cron_expression": {
              "description": "Cron expression that drives the durable schedule. Accepts 5-field\n(min hour day month weekday) or 7-field (sec … year) form.",
              "example": "0 9 * * *",
              "type": [
                "string",
                "null"
              ]
            },
            "enabled": {
              "description": "Whether the trigger is active on creation (default `true`).",
              "type": "boolean"
            },
            "message": {
              "description": "Message content or `{{template}}` sent when the trigger fires.",
              "example": "Run the daily digest",
              "type": "string"
            },
            "rate_limit_per_minute": {
              "description": "Optional per-ingress, per-IP webhook request limit.",
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "session_mode": {
              "$ref": "#/$defs/SessionBinding",
              "description": "Whether invocations reuse a stable session or create a new one."
            },
            "timezone": {
              "description": "IANA timezone identifier for cron evaluation (default `UTC`).",
              "example": "UTC",
              "type": "string"
            },
            "token": {
              "description": "Shared secret for webhook triggers.",
              "type": [
                "string",
                "null"
              ]
            },
            "trigger_type": {
              "$ref": "#/$defs/AgentTriggerType",
              "description": "Trigger kind. Omitted values retain the schedule API default."
            }
          },
          "required": [
            "message"
          ],
          "type": "object"
        },
        "SessionBinding": {
          "description": "What identity keys a session, for every exposure and every transport.\n\nOne enum replaces the former `SessionStrategy` (messaging channels) and\n`InvocationSessionMode` (triggers and request/reply endpoints), which asked\nthe same question with disjoint vocabularies and forced every new surface to\npick a side (EVE-1005).\n\n**The serialized values are deliberately the legacy ones.** Every variant\nrenames in Rust but serializes exactly as it did before, with the new name\naccepted as a read alias. Persisted `channel_config` JSONB therefore needs no\nmigration, and the API and UI keep exchanging the values they already do.\nMoving the wire vocabulary is a separate, migration-bearing change.\n\n`Requester` keys on the **transport's own external actor id** — the Slack\nuser id, the Public Chat visitor id — never on an Everruns principal. Those\nactors are unrelated to Everruns accounts (a Public Chat visitor is anonymous\nor Google-signed-in), so there is one consistent answer rather than a split\nvariant: whatever the transport calls the requester, scoped by the\n`{platform}:` tag prefix that already namespaces it.",
          "enum": [
            "per_thread",
            "per_channel",
            "per_user",
            "shared_session",
            "session_per_invocation"
          ],
          "example": "per_thread",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CreateAgentTriggerRequest"
        },
        {
          "properties": {
            "agent_id": {
              "description": "Owning agent's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "agent_id"
          ],
          "type": "object"
        }
      ],
      "description": "Create a trigger on an agent."
    },
    "method": "POST",
    "name": "create_agent_trigger",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/triggers",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents versions create",
    "description": "Save the current agent draft as an immutable version.",
    "input_schema": {
      "$defs": {
        "AgentVersionChangeKind": {
          "description": "Reason a version was created. Stored as lower_snake_case text.\nOne of `auto`, `manual`, `patch`, `minor`, `major`, `import`, `rollback`, `fork`.",
          "enum": [
            "auto",
            "manual",
            "patch",
            "minor",
            "major",
            "import",
            "rollback",
            "fork"
          ],
          "example": "manual",
          "type": "string"
        },
        "CreateAgentVersionRequest": {
          "description": "Request body for the `create_agent_version` operation.",
          "properties": {
            "change_kind": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/AgentVersionChangeKind",
                  "description": "Reason this version was created. See `AgentVersionChangeKind` for the allowed values.\nDefaults to `manual` when omitted."
                }
              ]
            },
            "summary": {
              "description": "Free-text summary of what changed in this version. Shown in the version timeline.",
              "example": "Tightened the refund-window check and added a regression test.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CreateAgentVersionRequest"
        },
        {
          "properties": {
            "agent_id": {
              "description": "Agent's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "agent_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "create_agent_version",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/versions",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Create a budget for a subject (session, agent, user, org). Sets a spending cap in the given currency.",
    "input_schema": {
      "$defs": {
        "BudgetPeriod": {
          "description": "Budget period configuration for recurring budgets.\n\nPeriods drive automatic balance reset:\n- `Duration` is a fixed-length sliding window (e.g. last 5 hours, last 30 days)\n  measured from `Budget::period_started_at`. When the window elapses the\n  balance is reset to `limit` and the window restarts.\n- `Calendar` aligns to a calendar boundary (`hour | day | week | month | year`)\n  in UTC. The balance resets when the next boundary is crossed.\n- `Rolling` is preserved for backwards compatibility and parses common\n  shorthand (`24h`, `5h`, `7d`, `30d`) into a `Duration`-equivalent reset\n  policy.",
          "oneOf": [
            {
              "description": "Sliding window of a configurable number of seconds.",
              "properties": {
                "seconds": {
                  "format": "int64",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": {
                  "enum": [
                    "duration"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "seconds",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Rolling window described as a humanized string (\"5h\", \"24h\", \"30d\").",
              "properties": {
                "type": {
                  "enum": [
                    "rolling"
                  ],
                  "type": "string"
                },
                "window": {
                  "type": "string"
                }
              },
              "required": [
                "window",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Calendar-aligned (`hour`, `day`, `week`, `month`, `year`).",
              "properties": {
                "type": {
                  "enum": [
                    "calendar"
                  ],
                  "type": "string"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "unit",
                "type"
              ],
              "type": "object"
            }
          ]
        }
      },
      "description": "Request body for creating a spending budget.",
      "properties": {
        "currency": {
          "description": "Unit in which usage and the limit are measured.",
          "example": "usd",
          "type": "string"
        },
        "limit": {
          "description": "Hard spending ceiling for the budget.",
          "example": 100.0,
          "format": "double",
          "type": "number"
        },
        "metadata": {
          "description": "Free-form metadata attached to this resource."
        },
        "period": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/BudgetPeriod",
              "description": "Optional recurring reset period for the budget balance."
            }
          ]
        },
        "soft_limit": {
          "description": "Optional threshold that triggers a warning or pause before exhaustion.",
          "example": 20.0,
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "subject_id": {
          "description": "Public identifier of the constrained resource.",
          "example": "agent_01933b5a00007000800000000000001",
          "type": "string"
        },
        "subject_type": {
          "description": "Kind of resource constrained by the budget.",
          "example": "agent",
          "type": "string"
        }
      },
      "required": [
        "subject_type",
        "subject_id",
        "currency",
        "limit"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_budget",
    "output_shape": "unknown",
    "path": "/v1/budgets",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "Create a persisted declarative capability.",
    "input_schema": {
      "description": "Request body for the `create_declarative_capability` operation.",
      "properties": {
        "definition": {
          "description": "Definition for the new declarative capability. `name` must be unique per org and becomes the canonical `declarative:<name>` capability ref.",
          "type": "object"
        }
      },
      "required": [
        "definition"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_declarative_capability",
    "output_shape": "unknown",
    "path": "/v1/capabilities",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Create a new eval.",
    "input_schema": {
      "$defs": {
        "EvalTarget": {
          "description": "Defines how to instantiate a session for an eval case.\n\nTwo modes:\n- `Session`: mirrors `CreateSessionRequest` — full control over session creation parameters.\n- `App`: references a deployed app by ID.\n\nResolution order: EvalRun.target → EvalCase.target → Eval.target → org default harness.",
          "oneOf": [
            {
              "description": "Session creation parameters (mirrors CreateSessionRequest).",
              "properties": {
                "agent_id": {
                  "description": "Agent to work in this session.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_id": {
                  "description": "Harness for the session. If omitted, org default harness is used.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_name": {
                  "description": "Addressable harness name (alternative to harness_id).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "max_iterations": {
                  "description": "Max LLM iterations per turn.",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "model_id": {
                  "description": "LLM model override.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "system_prompt": {
                  "description": "System prompt override (prepended to agent prompt).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "session"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Reference to a deployed app.",
              "properties": {
                "app_id": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "app"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "app_id",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Label-only target for externally-executed runs (e.g. imported from Mira).\n\nCarries provider/model labels and opaque params instead of session setup:\nexternal runs are ingested already-complete, so everruns never builds a\nsession from this. Mirrors a provider-agnostic `(provider, model)` pair.",
              "properties": {
                "model": {
                  "type": "string"
                },
                "params": {},
                "provider": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "external"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "provider",
                "model",
                "type"
              ],
              "type": "object"
            }
          ]
        }
      },
      "description": "Request to create a new eval",
      "properties": {
        "description": {
          "description": "Human-readable description. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "model_override": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "tags": {
          "description": "Free-form tags attached to this resource.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "target": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/EvalTarget",
              "description": "Session setup target (harness+agent, app, or full session params)."
            }
          ]
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_eval",
    "output_shape": "unknown",
    "path": "/v1/evals",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Create an eval case.",
    "input_schema": {
      "$defs": {
        "ArtifactSpec": {
          "description": "Named session file to collect after an eval case completes.",
          "properties": {
            "name": {
              "description": "Export key for this artifact (for example `patch` or `log`).",
              "type": "string"
            },
            "path": {
              "description": "Absolute path in the session filesystem.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "path"
          ],
          "type": "object"
        },
        "CreateEvalCaseRequest": {
          "description": "Request to create an eval case",
          "properties": {
            "artifacts": {
              "description": "Session files to capture after scoring completes.",
              "items": {
                "$ref": "#/$defs/ArtifactSpec"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "conversation": {
              "items": {
                "$ref": "#/$defs/EvalInputMessage"
              },
              "type": "array"
            },
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "max_turns": {
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "type": "string"
            },
            "position": {
              "format": "int32",
              "type": [
                "integer",
                "null"
              ]
            },
            "post": {
              "description": "Verification messages sent after conversation completes and session idles.",
              "items": {
                "$ref": "#/$defs/EvalInputMessage"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "scorers": {
              "items": {
                "$ref": "#/$defs/Scorer"
              },
              "type": "array"
            },
            "tags": {
              "description": "Free-form tags attached to this resource.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "target": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/EvalTarget",
                  "description": "Optional per-case target override."
                }
              ]
            },
            "timeout_seconds": {
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "name",
            "conversation",
            "scorers"
          ],
          "type": "object"
        },
        "EvalInputMessage": {
          "description": "A message to send to the agent during an eval case.",
          "properties": {
            "content": {
              "description": "The text content to send.",
              "type": "string"
            }
          },
          "required": [
            "content"
          ],
          "type": "object"
        },
        "EvalTarget": {
          "description": "Defines how to instantiate a session for an eval case.\n\nTwo modes:\n- `Session`: mirrors `CreateSessionRequest` — full control over session creation parameters.\n- `App`: references a deployed app by ID.\n\nResolution order: EvalRun.target → EvalCase.target → Eval.target → org default harness.",
          "oneOf": [
            {
              "description": "Session creation parameters (mirrors CreateSessionRequest).",
              "properties": {
                "agent_id": {
                  "description": "Agent to work in this session.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_id": {
                  "description": "Harness for the session. If omitted, org default harness is used.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_name": {
                  "description": "Addressable harness name (alternative to harness_id).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "max_iterations": {
                  "description": "Max LLM iterations per turn.",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "model_id": {
                  "description": "LLM model override.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "system_prompt": {
                  "description": "System prompt override (prepended to agent prompt).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "session"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Reference to a deployed app.",
              "properties": {
                "app_id": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "app"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "app_id",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Label-only target for externally-executed runs (e.g. imported from Mira).\n\nCarries provider/model labels and opaque params instead of session setup:\nexternal runs are ingested already-complete, so everruns never builds a\nsession from this. Mirrors a provider-agnostic `(provider, model)` pair.",
              "properties": {
                "model": {
                  "type": "string"
                },
                "params": {},
                "provider": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "external"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "provider",
                "model",
                "type"
              ],
              "type": "object"
            }
          ]
        },
        "Scorer": {
          "description": "A scoring rule applied to eval case output.",
          "oneOf": [
            {
              "description": "Final assistant message contains substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message does NOT contain substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "not_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message matches regex pattern.",
              "properties": {
                "pattern": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "regex"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "pattern",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent called named tool at least `min` times.",
              "properties": {
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent did NOT call named tool.",
              "properties": {
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_not_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Total tool calls within range.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "tool_call_count"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Completed within N turns.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": {
                  "enum": [
                    "turns_within"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "max",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Session filesystem file contains substring.",
              "properties": {
                "path": {
                  "type": "string"
                },
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "file_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "path",
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message parses as JSON matching schema.",
              "properties": {
                "schema": {},
                "type": {
                  "enum": [
                    "json_schema"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "schema",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness: the answer's citations (see `knowledge/runtime-resources/citations.md`)\nmust cover the claim and be verified as supported. Scored from the\n`TextAnnotation`s on the final message — pair with the\n`citation_verification` capability so verdicts are present.",
              "properties": {
                "min_citations": {
                  "description": "Minimum number of citations the answer must carry.",
                  "example": 1,
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "pass_threshold": {
                  "description": "Minimum fraction of citations verified `entailed` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "type": {
                  "enum": [
                    "citation_faithful"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness judged by an LLM: each cited claim/source pair is\ngraded by a model, so the eval works even without the\n`citation_verification` capability. See `knowledge/runtime-resources/citations.md`.",
              "properties": {
                "model_id": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/$defs/modelId",
                      "description": "Judge model; the org's default is used when absent."
                    }
                  ]
                },
                "pass_threshold": {
                  "description": "Minimum judged score `[0,1]` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "rubric": {
                  "description": "Rubric override; a citation-faithfulness rubric is used when absent.",
                  "example": "Score the fraction of cited claims supported by their source.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "citation_judged"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            }
          ]
        },
        "modelId": {
          "description": "Prefixed identifier with 'model' prefix",
          "example": "model_01933b5a000070008000000000000001",
          "pattern": "^model_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CreateEvalCaseRequest"
        },
        {
          "properties": {
            "eval_id": {
              "type": "string"
            }
          },
          "required": [
            "eval_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "create_eval_case",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/cases",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Create an eval run.",
    "input_schema": {
      "$defs": {
        "CreateEvalRunRequest": {
          "description": "Request to create an eval run",
          "properties": {
            "model_override": {
              "type": [
                "string",
                "null"
              ]
            },
            "target": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/EvalTarget",
                  "description": "Optional per-run target override."
                }
              ]
            }
          },
          "type": "object"
        },
        "EvalTarget": {
          "description": "Defines how to instantiate a session for an eval case.\n\nTwo modes:\n- `Session`: mirrors `CreateSessionRequest` — full control over session creation parameters.\n- `App`: references a deployed app by ID.\n\nResolution order: EvalRun.target → EvalCase.target → Eval.target → org default harness.",
          "oneOf": [
            {
              "description": "Session creation parameters (mirrors CreateSessionRequest).",
              "properties": {
                "agent_id": {
                  "description": "Agent to work in this session.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_id": {
                  "description": "Harness for the session. If omitted, org default harness is used.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_name": {
                  "description": "Addressable harness name (alternative to harness_id).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "max_iterations": {
                  "description": "Max LLM iterations per turn.",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "model_id": {
                  "description": "LLM model override.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "system_prompt": {
                  "description": "System prompt override (prepended to agent prompt).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "session"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Reference to a deployed app.",
              "properties": {
                "app_id": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "app"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "app_id",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Label-only target for externally-executed runs (e.g. imported from Mira).\n\nCarries provider/model labels and opaque params instead of session setup:\nexternal runs are ingested already-complete, so everruns never builds a\nsession from this. Mirrors a provider-agnostic `(provider, model)` pair.",
              "properties": {
                "model": {
                  "type": "string"
                },
                "params": {},
                "provider": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "external"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "provider",
                "model",
                "type"
              ],
              "type": "object"
            }
          ]
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CreateEvalRunRequest"
        },
        {
          "properties": {
            "eval_id": {
              "type": "string"
            }
          },
          "required": [
            "eval_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "create_eval_run",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Mint a read-only share link for an eval run.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "run_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_eval_run_share",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/share",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Create a new harness with a name, system prompt, and optional capabilities.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "ConversationStarter": {
          "description": "A conversation starter shown on a fresh Platform Chat thread.\nSelecting one inserts its text into the composer. `icon` reuses the\nharness icon name set (`HarnessIcon`); unknown names fall back to the\ndefault glyph.",
          "properties": {
            "icon": {
              "description": "Optional icon name from the harness icon set.",
              "example": "zap",
              "type": [
                "string",
                "null"
              ]
            },
            "text": {
              "description": "Prompt text inserted into the composer when selected.",
              "example": "Triage the newest P1",
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "type": "object"
        },
        "InitialFile": {
          "description": "Starter file copied into a new session from an agent or harness.",
          "properties": {
            "content": {
              "description": "File content: plain text or base64-encoded binary.",
              "type": "string"
            },
            "encoding": {
              "description": "Content encoding: `text` or `base64`.",
              "type": "string"
            },
            "is_readonly": {
              "description": "Prevent session-side edits or deletes when true.",
              "type": "boolean"
            },
            "path": {
              "description": "Absolute path within the session workspace. `/workspace` prefix is accepted.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "NetworkAccessList": {
          "description": "Network access list controlling which hosts/URLs an agent session can reach.\n\n- `allowed`: if non-empty, only URLs matching these patterns are permitted.\n- `blocked`: URLs matching these patterns are always denied (takes precedence over allowed).\n\nPattern format:\n- `example.com` — exact domain match (any port, any path)\n- `*.example.com` — domain and all subdomains\n- `https://example.com/api/` — exact URL prefix (scheme + host + path)",
          "example": {
            "allowed": [
              "*.example.com",
              "https://api.acme.com/"
            ],
            "blocked": [
              "169.254.169.254"
            ]
          },
          "properties": {
            "allowed": {
              "description": "Allowed host patterns. If non-empty, only matching URLs are permitted.\nAn empty list means \"no restriction from this layer\" (inherit parent).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "blocked": {
              "description": "Blocked host patterns. Always denied, even if matched by `allowed`.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        }
      },
      "description": "Request to create a new harness",
      "properties": {
        "capabilities": {
          "description": "Capabilities to enable with per-harness configuration.",
          "example": [
            {
              "config": {},
              "ref": "current_time"
            },
            {
              "config": {},
              "ref": "web_fetch"
            }
          ],
          "items": {
            "$ref": "#/$defs/AgentCapabilityConfig"
          },
          "type": "array"
        },
        "default_model_id": {
          "description": "Default LLM model ID for this harness. Lowest priority in model chain.",
          "example": "model_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "Description of what the harness does.",
          "example": "Research harness with planning and web capabilities",
          "type": [
            "string",
            "null"
          ]
        },
        "display_name": {
          "description": "Human-readable display name shown in UI.",
          "example": "Deep Research",
          "type": [
            "string",
            "null"
          ]
        },
        "embedder_metadata": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Arbitrary key-value metadata injected into LLM requests for observability.",
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "initial_files": {
          "description": "Starter files copied into each new session for this harness.",
          "items": {
            "$ref": "#/$defs/InitialFile"
          },
          "type": "array"
        },
        "intro_markdown": {
          "description": "Markdown intro shown as an intro box on a fresh Platform Chat thread.\nImages are allowed. The agent intro wins. Hidden once the user inputs.",
          "example": "I triage incidents, dig through logs, and draft the update.",
          "type": [
            "string",
            "null"
          ]
        },
        "mcpServers": {
          "$ref": "#/$defs/BTreeMap",
          "description": "Remote MCP servers scoped to this harness."
        },
        "name": {
          "description": "Name, unique per org. Lowercase alphanumeric and hyphens.",
          "example": "deep-research",
          "type": "string"
        },
        "network_access": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/NetworkAccessList",
              "description": "Network access list controlling which hosts/URLs sessions can reach."
            }
          ]
        },
        "parent_harness_id": {
          "description": "Optional parent harness to inherit from.",
          "example": "harness_01933b5a000070008000000000000602",
          "type": [
            "string",
            "null"
          ]
        },
        "short_description": {
          "description": "One-line description in simplified Markdown, shown below the chat title\nonce the intro is hidden. The agent value wins.",
          "example": "Triage incidents, dig through logs, draft the update.",
          "type": [
            "string",
            "null"
          ]
        },
        "starters": {
          "description": "Conversation starters for a fresh Platform Chat thread. Selecting one\ninserts its text into the composer. The agent starters win when\nnon-empty. `icon` reuses the harness icon name set.",
          "items": {
            "$ref": "#/$defs/ConversationStarter"
          },
          "type": "array"
        },
        "system_prompt": {
          "description": "Base system prompt defining the harness's behavior. Optional: omit (or\nsend an empty string) to contribute no base prompt, in which case the\neffective prompt comes from the parent harness, agent, session, and\ncapability layers.",
          "example": "You are a research assistant with deep analytical capabilities.",
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "description": "Tags for organizing harnesses.",
          "example": [
            "research",
            "planning"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_harness",
    "output_shape": "unknown",
    "path": "/v1/harnesses",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Create a knowledge base in the current organization.",
    "input_schema": {
      "properties": {
        "description": {
          "description": "Human-readable description. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "embedding_model_id": {
          "description": "Optional embedding model for hybrid retrieval.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_knowledge_base",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Create an entry inside a knowledge base.",
    "input_schema": {
      "properties": {
        "body": {
          "type": "string"
        },
        "kb_id": {
          "description": "Knowledge base's prefixed public identifier.",
          "type": "string"
        },
        "kind": {
          "description": "Discriminator selecting the variant of this resource.",
          "type": [
            "string",
            "null"
          ]
        },
        "resource": {
          "description": "Optional OKF resource URI identifying the underlying asset.",
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "description": "Free-form tags attached to this resource.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "title": {
          "description": "Human-readable title. Safe to render in user-facing messages.",
          "type": "string"
        }
      },
      "required": [
        "kb_id",
        "title",
        "body"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_knowledge_entry",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}/entries",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_indexes",
    "cli": null,
    "description": "Create a knowledge index in the current organization.",
    "input_schema": {
      "properties": {
        "description": {
          "description": "Human-readable description. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "embedding_model_id": {
          "description": "Embedding model used to embed chunks. Required.",
          "type": "string"
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "source_config": {
          "description": "Non-secret source coordinates."
        },
        "source_type": {
          "description": "External source type. One of `github`, `git`. Defaults to `github`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name",
        "embedding_model_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_knowledge_index",
    "output_shape": "unknown",
    "path": "/v1/knowledge-indexes",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "mcp_servers",
    "cli": "mcp-servers create",
    "description": "Create a new MCP server with a name, URL, and optional authentication.",
    "input_schema": {
      "$defs": {
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        }
      },
      "description": "Request to create a new MCP server",
      "properties": {
        "api_key": {
          "description": "API key for authentication (optional). Sent with each request; never echoed in responses.",
          "example": "sk-mcp-redacted-1234567890abcdef",
          "type": [
            "string",
            "null"
          ]
        },
        "auth_mode": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/McpServerAuthMode",
              "description": "Authentication mode. Defaults to `api_key` when `api_key` is provided, otherwise `none`.\nExample shape is defined on `McpServerAuthMode`."
            }
          ]
        },
        "description": {
          "description": "A human-readable description of what the MCP server provides.",
          "example": "Atlassian MCP Server for Jira and Confluence",
          "type": [
            "string",
            "null"
          ]
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Additional HTTP headers for authentication.",
          "example": {
            "X-Atlassian-Cloud-Id": "00000000-0000-0000-0000-000000000000"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": [
            "object",
            "null"
          ]
        },
        "name": {
          "description": "The name of the MCP server. Must be unique.",
          "example": "atlassian-mcp-server",
          "type": "string"
        },
        "protocol_mode": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/McpProtocolMode",
              "description": "Protocol-era policy. Defaults to `auto` (negotiates every protocol era)."
            }
          ]
        },
        "transport_type": {
          "$ref": "#/$defs/McpServerTransportType",
          "description": "Transport type. Currently only \"http\" is supported.\nExample shape is defined on `McpServerTransportType`."
        },
        "url": {
          "description": "The URL of the MCP server endpoint.",
          "example": "https://mcp.atlassian.com/v1/mcp",
          "type": "string"
        }
      },
      "required": [
        "name",
        "url"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_mcp_server",
    "output_shape": "unknown",
    "path": "/v1/mcp-servers",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "memories",
    "cli": null,
    "description": "Create a workspace memory in the current organization.",
    "input_schema": {
      "$defs": {
        "CreateMemorySourceRequest": {
          "description": "Request body for the `create_memory_source` operation.",
          "example": {
            "branch": "main",
            "repository": "acme/design-docs",
            "root_folder": "docs/",
            "type": "github"
          },
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/GitHubMemorySourceRequest"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "github"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ]
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/GitMemorySourceRequest"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "git"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ]
            }
          ]
        },
        "GitHubMemorySourceRequest": {
          "description": "Request body for GitHub memory source.",
          "properties": {
            "branch": {
              "description": "Branch to sync. Defaults to the repository's default branch when omitted.",
              "example": "main",
              "type": [
                "string",
                "null"
              ]
            },
            "repository": {
              "description": "GitHub repository in `owner/repo` form.",
              "example": "acme/design-docs",
              "type": "string"
            },
            "root_folder": {
              "description": "Sub-directory within the repository to sync. Empty / omitted = whole tree.",
              "example": "docs/",
              "type": [
                "string",
                "null"
              ]
            },
            "sync_interval_secs": {
              "description": "Automatic resync interval in seconds. Omit or set 0 for manual-only; scheduled sync accepts 300 through 604800.",
              "example": 3600,
              "format": "int32",
              "maximum": 604800,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "repository"
          ],
          "type": "object"
        },
        "GitMemorySourceRequest": {
          "description": "Request body for git memory source.",
          "properties": {
            "branch": {
              "description": "Branch to sync. Defaults to the repository's default branch when omitted.",
              "example": "main",
              "type": [
                "string",
                "null"
              ]
            },
            "root_folder": {
              "description": "Sub-directory within the repository to sync. Empty / omitted = whole tree.",
              "example": "docs/",
              "type": [
                "string",
                "null"
              ]
            },
            "sync_interval_secs": {
              "description": "Automatic resync interval in seconds. Omit or set 0 for manual-only; scheduled sync accepts 300 through 604800.",
              "example": 3600,
              "format": "int32",
              "maximum": 604800,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "url": {
              "description": "Clonable git URL (SSH or HTTPS).",
              "example": "https://github.com/acme/design-docs.git",
              "type": "string"
            }
          },
          "required": [
            "url"
          ],
          "type": "object"
        }
      },
      "properties": {
        "description": {
          "description": "Human-readable description. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "source": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/CreateMemorySourceRequest"
            }
          ]
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_memory",
    "output_shape": "unknown",
    "path": "/v1/memories",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "messages",
    "cli": null,
    "description": "Create a user message in a session and start the next run. The message content is an array of content parts, e.g. --content '[{\"type\":\"text\",\"text\":\"Tell me a short, family-friendly joke.\"}]'.",
    "input_schema": {
      "$defs": {
        "AnnotationSource": {
          "description": "The source a [`TextAnnotation`] points to.",
          "properties": {
            "location": {
              "description": "Provenance within the document (line / char / page / block ranges),\nreusing the retrieval `location` JSONB shape."
            },
            "snippet": {
              "description": "Trimmed passage that backs the claim. Display-only; never relied on for\nprompt reconstruction.",
              "example": "The control plane owns durable state.",
              "type": [
                "string",
                "null"
              ]
            },
            "title": {
              "description": "Human-readable source title, when known.",
              "example": "Architecture Overview",
              "type": [
                "string",
                "null"
              ]
            },
            "uri": {
              "description": "Stable, linkable locator (e.g. `github://owner/repo@main/docs/x.md` or an\n`https://` URL).",
              "example": "github://owner/repo@main/docs/x.md",
              "type": "string"
            }
          },
          "required": [
            "uri"
          ],
          "type": "object"
        },
        "Controls": {
          "description": "Runtime controls for message processing",
          "properties": {
            "error_disclosure": {
              "description": "Error disclosure override for this turn: \"generic\", \"standard\", or\n\"detailed\". Clamped to at most the mode allowed by the agent's\n`error_disclosure` capability (capability absent => \"standard\"), so a\nclient can narrow but never widen disclosure.",
              "type": [
                "string",
                "null"
              ]
            },
            "hints": {
              "description": "Generic client hints — arbitrary key-value pairs declared by the client.\nSession-level defaults are set at session creation; per-message values\noverride session hints key-by-key (shallow merge).\n\nExamples: `{\"setup_connection\": true, \"rich_media\": true}`",
              "type": [
                "object",
                "null"
              ]
            },
            "locale": {
              "description": "Locale override for this message turn (BCP 47, e.g. `uk-UA`).\nOverrides the session locale for backend-authored strings and prompts.",
              "type": [
                "string",
                "null"
              ]
            },
            "model_id": {
              "description": "Model ID to use for this message (format: model_{32-hex}).\nOverrides session and agent model settings.",
              "example": "model_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "reasoning": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/ReasoningConfig",
                  "description": "Reasoning configuration"
                }
              ]
            },
            "speed": {
              "description": "Speed (service tier) for this message turn: \"flex\", \"default\", or\n\"priority\". Only sent to providers whose model profile advertises a\nspeed config (OpenAI `service_tier`).",
              "type": [
                "string",
                "null"
              ]
            },
            "verbosity": {
              "description": "Verbosity for this message turn: \"low\", \"medium\", or \"high\". Only sent\nto providers whose model profile advertises a verbosity config (OpenAI\n`verbosity`).",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ExternalActor": {
          "description": "External actor identity for messages originating from external channels\n(Slack, Discord, Teams, etc.).\n\nChannel adapters populate this to identify the sender without coupling\ncore logic to any specific channel. The ReasonAtom uses this to prefix\nuser messages so the LLM knows who is speaking.",
          "properties": {
            "actor_id": {
              "description": "Opaque actor identifier from the source channel (e.g. Slack user ID \"U0123456789\")",
              "type": "string"
            },
            "actor_name": {
              "description": "Resolved display name (e.g. \"Alice\"). Falls back to actor_id if absent.",
              "type": [
                "string",
                "null"
              ]
            },
            "metadata": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Channel-specific metadata (e.g. team_id, channel_id)",
              "propertyNames": {
                "type": "string"
              },
              "type": [
                "object",
                "null"
              ]
            },
            "source": {
              "description": "Source channel identifier (e.g. \"slack\", \"discord\")",
              "type": "string"
            }
          },
          "required": [
            "actor_id",
            "source"
          ],
          "type": "object"
        },
        "FileContentPart": {
          "description": "File content part (reference to an uploaded file, e.g. a PDF)\n\nThis is used for files uploaded via the /files API.\nThe file data is stored separately and referenced by ID.",
          "properties": {
            "file_id": {
              "description": "ID of the uploaded file (format: file_{32-hex})",
              "example": "file_01933b5a00007000800000000000001",
              "type": "string"
            },
            "filename": {
              "description": "Original filename (for display and provider file parts)",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "file_id"
          ],
          "type": "object"
        },
        "ImageContentPart": {
          "description": "Image content part (base64 or URL)",
          "properties": {
            "base64": {
              "type": [
                "string",
                "null"
              ]
            },
            "media_type": {
              "type": [
                "string",
                "null"
              ]
            },
            "url": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ImageFileContentPart": {
          "description": "Image file content part (reference to uploaded image)\n\nThis is used for images uploaded via the /images API.\nThe image data is stored separately and referenced by ID.\nNote: Currently filtered out before sending to LLM.",
          "properties": {
            "filename": {
              "description": "Original filename (for display)",
              "type": [
                "string",
                "null"
              ]
            },
            "image_id": {
              "description": "ID of the uploaded image (format: img_{32-hex})",
              "example": "img_01933b5a00007000800000000000001",
              "type": "string"
            }
          },
          "required": [
            "image_id"
          ],
          "type": "object"
        },
        "InputContentPart": {
          "description": "Input content part - text, image, and image_file (for user input)\n\nThis is a subset of ContentPart that users can send.\nTool calls and results are system-generated.",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/TextContentPart",
                  "description": "Text content"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "text"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Text content"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ImageContentPart",
                  "description": "Image content (base64 or URL)"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "image"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Image content (base64 or URL)"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ImageFileContentPart",
                  "description": "Image file content (reference to uploaded image by ID)"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "image_file"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Image file content (reference to uploaded image by ID)"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/FileContentPart",
                  "description": "File content (reference to uploaded file, e.g. PDF, by ID)"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "file"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "File content (reference to uploaded file, e.g. PDF, by ID)"
            }
          ]
        },
        "InputMessage": {
          "description": "Input message for creating a user message\n\nOnly user messages can be created via the API.\nAgent messages are created internally by the workflow.",
          "example": {
            "content": [
              {
                "text": "Why is the build failing on main?",
                "type": "text"
              }
            ],
            "role": "user"
          },
          "properties": {
            "content": {
              "description": "Array of content parts (text and image only)",
              "items": {
                "$ref": "#/$defs/InputContentPart"
              },
              "type": "array"
            },
            "role": {
              "$ref": "#/$defs/MessageRole",
              "description": "Message role (always \"user\" for API-created messages)"
            }
          },
          "required": [
            "content"
          ],
          "type": "object"
        },
        "MessageRole": {
          "description": "Message role (API layer)\n\nSimplified to only user and agent messages.\nTool results are conveyed via `tool.completed` events.\nSystem messages are internal and not exposed via API.",
          "enum": [
            "user",
            "agent"
          ],
          "type": "string"
        },
        "ReasoningConfig": {
          "description": "Reasoning configuration for the model",
          "properties": {
            "effort": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/ReasoningEffort",
                  "description": "Effort level for reasoning.\n\nTyped rather than free-form: the effort taxonomy is closed, and each\ndriver previously re-parsed the string with its own case handling, which\nlet `minimal` silently mean \"no reasoning\" on budget-based models."
                }
              ]
            }
          },
          "type": "object"
        },
        "ReasoningEffort": {
          "description": "Reasoning effort level for models that support it",
          "enum": [
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "type": "string"
        },
        "TextAnnotation": {
          "description": "A claim-level citation attached to a span of generated text.\n\nThe single shared type across every citation capability: a text span linked\nto a source. Producers agree only on this render contract — each capability\nkeeps its own richer representation (e.g. `KnowledgeIndexCitation`) and maps\ninto this envelope at emit time. See `knowledge/runtime-resources/citations.md`.",
          "properties": {
            "end": {
              "description": "Exclusive end char offset.",
              "example": 19,
              "minimum": 0,
              "type": "integer"
            },
            "external_id": {
              "description": "Opaque producer id (e.g. `kchk_…`, `kbe_…`, a URL hash). Not interpreted\nby the render contract.",
              "example": "kchk_01j9y3q8w2",
              "type": [
                "string",
                "null"
              ]
            },
            "origin": {
              "description": "Capability id that produced this annotation (e.g. `citation_retrieval`).\nLets the UI and evals attribute and filter each citation by feed.",
              "example": "citation_retrieval",
              "type": "string"
            },
            "source": {
              "$ref": "#/$defs/AnnotationSource",
              "description": "The cited source."
            },
            "start": {
              "description": "0-indexed start char offset into the enclosing `TextContentPart.text`.",
              "example": 0,
              "minimum": 0,
              "type": "integer"
            },
            "verified": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/VerificationVerdict",
                  "description": "Verification verdict, filled by the `citation_verification` capability.\nAbsent means unverified (not \"unsupported\")."
                }
              ]
            }
          },
          "required": [
            "start",
            "end",
            "origin",
            "source"
          ],
          "type": "object"
        },
        "TextContentPart": {
          "description": "Text content part",
          "properties": {
            "annotations": {
              "description": "Claim-level citations attached to spans of `text`.\n\nThe narrow render contract shared by all citation capabilities (see\n`knowledge/runtime-resources/citations.md`). Empty for non-cited text, so the wire shape of\nexisting messages is unchanged.",
              "items": {
                "$ref": "#/$defs/TextAnnotation"
              },
              "type": "array"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "type": "object"
        },
        "VerificationStatus": {
          "description": "Whether a cited source entails the claim it is attached to.",
          "enum": [
            "entailed",
            "unsupported",
            "uncertain"
          ],
          "example": "entailed",
          "type": "string"
        },
        "VerificationVerdict": {
          "description": "Outcome of citation verification (see the `citation_verification` capability).",
          "properties": {
            "score": {
              "description": "Entailment confidence in `[0, 1]`, when the verifier produced one.",
              "example": 0.92,
              "format": "float",
              "type": [
                "number",
                "null"
              ]
            },
            "status": {
              "$ref": "#/$defs/VerificationStatus",
              "description": "Whether the cited source supports the claim."
            }
          },
          "required": [
            "status"
          ],
          "type": "object"
        },
        "partId": {
          "description": "Prefixed identifier with 'part' prefix",
          "example": "part_01933b5a000070008000000000000001",
          "pattern": "^part_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "properties": {
        "addressed_participant_id": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/partId"
            }
          ]
        },
        "controls": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/Controls"
            }
          ]
        },
        "external_actor": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ExternalActor"
            }
          ]
        },
        "message": {
          "$ref": "#/$defs/InputMessage"
        },
        "metadata": {
          "additionalProperties": {},
          "description": "Free-form metadata attached to this resource.",
          "propertyNames": {
            "type": "string"
          },
          "type": [
            "object",
            "null"
          ]
        },
        "request_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "tags": {
          "description": "Free-form tags attached to this resource.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        }
      },
      "required": [
        "session_id",
        "message"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_message",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/messages",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "models",
    "cli": null,
    "description": "Create a new model for a provider.",
    "input_schema": {
      "properties": {
        "capabilities": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "display_name": {
          "description": "Human-readable display name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "enabled": {
          "description": "Whether this resource is enabled.",
          "type": "boolean"
        },
        "is_favorite": {
          "type": "boolean"
        },
        "model_id": {
          "description": "LLM model's prefixed public identifier.",
          "type": "string"
        },
        "provider_id": {
          "description": "LLM provider's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "provider_id",
        "model_id",
        "display_name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_model",
    "output_shape": "unknown",
    "path": "/v1/providers/{provider_id}/models",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "observers",
    "cli": null,
    "description": "Create an observer (online scoring).",
    "input_schema": {
      "$defs": {
        "LlmJudgeConfig": {
          "description": "LLM-as-judge scoring configuration. The judge grades the scoped trace\nslice against `rubric` and returns a 0.0–1.0 value, an optional\ncategorical label, and free-text reasoning.",
          "properties": {
            "model_id": {
              "description": "Org model to judge with. When `None`, the org's default model is used.\nJudge calls go through the org's own providers and are billed to it.",
              "type": [
                "string",
                "null"
              ]
            },
            "pass_threshold": {
              "description": "Score value at/above which `pass` is true.",
              "format": "double",
              "type": "number"
            },
            "rubric": {
              "description": "Grading rubric shown to the judge model. Should describe what a high\nvs. low score means.",
              "type": "string"
            }
          },
          "required": [
            "rubric"
          ],
          "type": "object"
        },
        "ObserverMatch": {
          "description": "Predicates selecting which production sessions an observer scores.\nAll present predicates must match (AND); within a list, any entry matches (OR).\nAn empty match block matches all org traffic. Sessions tagged `eval` are\nalways excluded so synthetic eval-run sessions are never scored.",
          "properties": {
            "agent_ids": {
              "description": "Match sessions running any of these agents.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "harness_ids": {
              "description": "Match sessions on any of these harnesses.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "session_tags": {
              "description": "Match sessions carrying any of these tags.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ObserverScope": {
          "description": "What slice of the trace a scorer grades. Phase 1 implements `turn` only;\n`session` and `tool` scopes are specced in knowledge/evaluation/online-evals.md.",
          "enum": [
            "turn"
          ],
          "type": "string"
        },
        "ObserverScorerConfig": {
          "allOf": [
            {
              "$ref": "#/$defs/ScorerMethod",
              "description": "How this scorer grades (rule or llm_judge)."
            },
            {
              "properties": {
                "key": {
                  "description": "Stable name within the observer (score series name).",
                  "type": "string"
                },
                "scope": {
                  "$ref": "#/$defs/ObserverScope",
                  "description": "Trace slice this scorer grades."
                }
              },
              "required": [
                "key"
              ],
              "type": "object"
            }
          ],
          "description": "One scorer inside an observer. `key` names the score series in listings\nand future dashboards; `scope` selects the trace slice; `method` is how\nit grades."
        },
        "Scorer": {
          "description": "A scoring rule applied to eval case output.",
          "oneOf": [
            {
              "description": "Final assistant message contains substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message does NOT contain substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "not_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message matches regex pattern.",
              "properties": {
                "pattern": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "regex"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "pattern",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent called named tool at least `min` times.",
              "properties": {
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent did NOT call named tool.",
              "properties": {
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_not_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Total tool calls within range.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "tool_call_count"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Completed within N turns.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": {
                  "enum": [
                    "turns_within"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "max",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Session filesystem file contains substring.",
              "properties": {
                "path": {
                  "type": "string"
                },
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "file_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "path",
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message parses as JSON matching schema.",
              "properties": {
                "schema": {},
                "type": {
                  "enum": [
                    "json_schema"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "schema",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness: the answer's citations (see `knowledge/runtime-resources/citations.md`)\nmust cover the claim and be verified as supported. Scored from the\n`TextAnnotation`s on the final message — pair with the\n`citation_verification` capability so verdicts are present.",
              "properties": {
                "min_citations": {
                  "description": "Minimum number of citations the answer must carry.",
                  "example": 1,
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "pass_threshold": {
                  "description": "Minimum fraction of citations verified `entailed` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "type": {
                  "enum": [
                    "citation_faithful"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness judged by an LLM: each cited claim/source pair is\ngraded by a model, so the eval works even without the\n`citation_verification` capability. See `knowledge/runtime-resources/citations.md`.",
              "properties": {
                "model_id": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/$defs/modelId",
                      "description": "Judge model; the org's default is used when absent."
                    }
                  ]
                },
                "pass_threshold": {
                  "description": "Minimum judged score `[0,1]` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "rubric": {
                  "description": "Rubric override; a citation-faithfulness rubric is used when absent.",
                  "example": "Score the fraction of cited claims supported by their source.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "citation_judged"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            }
          ]
        },
        "ScorerMethod": {
          "description": "How a scorer grades a trace slice: a deterministic `rule` (reusing the\neval scorer vocabulary) or an `llm_judge`.",
          "oneOf": [
            {
              "description": "Deterministic rule. `file_contains` is rejected for observers (session\nfilesystems are not part of the observable trace contract).",
              "properties": {
                "method": {
                  "enum": [
                    "rule"
                  ],
                  "type": "string"
                },
                "rule": {
                  "$ref": "#/$defs/Scorer"
                }
              },
              "required": [
                "rule",
                "method"
              ],
              "type": "object"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/LlmJudgeConfig",
                  "description": "LLM-as-judge."
                },
                {
                  "properties": {
                    "method": {
                      "enum": [
                        "llm_judge"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "method"
                  ],
                  "type": "object"
                }
              ],
              "description": "LLM-as-judge."
            }
          ]
        },
        "modelId": {
          "description": "Prefixed identifier with 'model' prefix",
          "example": "model_01933b5a000070008000000000000001",
          "pattern": "^model_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "description": "Request to create a new observer.",
      "properties": {
        "description": {
          "description": "Human-readable description. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "match": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ObserverMatch",
              "description": "Which production sessions to score. Empty matches all org traffic."
            }
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "sampling_rate": {
          "description": "Fraction of matching turns to score (0.0–1.0). Defaults to 0.1.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "scorers": {
          "description": "Scoring rules. Must contain at least one.",
          "items": {
            "$ref": "#/$defs/ObserverScorerConfig"
          },
          "type": "array"
        }
      },
      "required": [
        "name",
        "scorers"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_observer",
    "output_shape": "unknown",
    "path": "/v1/observers",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Create a machine-payment wallet account.",
    "input_schema": {
      "description": "Request body for the `create_payment_account` operation.",
      "properties": {
        "label": {
          "description": "Human-readable label. Safe to render in user-facing messages.",
          "example": "Refund agent · USDC on Base",
          "type": "string"
        },
        "metadata": {
          "description": "Free-form metadata attached to this account (caller-defined; opaque to the platform).\nExample: `{\"team\": \"support\", \"cost_center\": \"ops\"}`."
        },
        "owner_id": {
          "description": "Prefixed identifier of the owning principal. Must use the prefix that matches `owner_type`\n(see above). Example below pairs with `owner_type = \"agent_identity\"`.",
          "example": "identity_01933b5a00007000800000000000001",
          "type": "string"
        },
        "owner_type": {
          "description": "Principal class that owns the account. One of: `user`, `agent_identity`, `organization`.\nThe prefix on `owner_id` must match this: `user` → `user_…`, `agent_identity` → `identity_…`,\n`organization` → `org_…`.",
          "example": "agent_identity",
          "type": "string"
        },
        "private_key": {
          "description": "Private key material for the rail. Stored encrypted; never returned in responses.\nExample shown as an obvious placeholder — supply a real 32-byte hex value at create time.",
          "example": "0x<your-32-byte-hex-private-key>",
          "type": [
            "string",
            "null"
          ]
        },
        "public_address": {
          "description": "Public address on the rail (chain address, account number, etc.). Optional; can be filled in later.",
          "example": "0x742d35Cc6634C0532925a3b844Bc454e4438f44e",
          "type": [
            "string",
            "null"
          ]
        },
        "rail": {
          "description": "Settlement rail this account operates on. One of: `mpp_tempo`, `x402_base`.",
          "example": "x402_base",
          "type": "string"
        }
      },
      "required": [
        "owner_type",
        "owner_id",
        "rail",
        "label"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_payment_account",
    "output_shape": "unknown",
    "path": "/v1/payments/accounts",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Create a machine-payment spend policy.",
    "input_schema": {
      "description": "Request body for the `create_payment_policy` operation.",
      "properties": {
        "allowed_capabilities": {
          "description": "Capability IDs this policy permits paid calls for. Empty list means no capability gating.",
          "example": [
            "weather.lookup",
            "shipping.quote"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowed_hosts": {
          "description": "HTTP host allowlist for paid outbound calls. Empty list means no host gating.",
          "example": [
            "api.shippo.com",
            "api.openweathermap.org"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_amount_usd_per_day": {
          "description": "Maximum cumulative amount (USD) per UTC day. **Advisory only — not yet enforced.** Stored for forward compatibility; the authority currently checks only `max_amount_usd_per_request`. `None` means no per-day cap.",
          "example": 100.0,
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "max_amount_usd_per_request": {
          "description": "Maximum amount (USD) any single paid request may settle for. **Enforced** by the payment authority at policy selection. `None` means no per-request cap.",
          "example": 5.0,
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "max_amount_usd_per_turn": {
          "description": "Maximum cumulative amount (USD) per agent turn. **Advisory only — not yet enforced.** Stored for forward compatibility; the authority currently checks only `max_amount_usd_per_request`. `None` means no per-turn cap.",
          "example": 20.0,
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "metadata": {
          "description": "Free-form metadata attached to this policy.\nExample: `{\"owner_team\": \"ops\", \"ticket\": \"OPS-1248\"}`."
        },
        "payment_account_id": {
          "description": "Payment account this policy authorizes spending from. Accepts a prefixed `payacct_…`\nidentifier or a bare UUID.",
          "example": "payacct_01933b5a00007000800000000000001",
          "type": "string"
        },
        "rail_preference": {
          "description": "Preferred settlement rails in priority order; the authority picks the first available.",
          "example": [
            "x402_base",
            "mpp_tempo"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "require_approval_above_usd": {
          "description": "Threshold (USD) above which a request would require explicit human approval. **Advisory only — not yet enforced.** Stored for forward compatibility; no approval gate is wired up yet. `None` disables the (future) gate.",
          "example": 10.0,
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "subject_id": {
          "description": "Prefixed identifier of the bound subject. Must use the prefix matching `subject_type`\n(see above). Example below pairs with `subject_type = \"agent_identity\"`.",
          "example": "identity_01933b5a00007000800000000000001",
          "type": "string"
        },
        "subject_type": {
          "description": "Class of subject this policy binds to. One of: `user`, `agent_identity`, `agent`, `app`, `session`, `org`.\nThe prefix on `subject_id` must match: `user`→`user_…`, `agent_identity`→`identity_…`,\n`agent`→`agent_…`, `app`→`app_…`, `session`→`session_…`, `org`→`org_…`.",
          "example": "agent_identity",
          "type": "string"
        }
      },
      "required": [
        "payment_account_id",
        "subject_type",
        "subject_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_payment_policy",
    "output_shape": "unknown",
    "path": "/v1/payments/policies",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Register a new plugin marketplace for this organization.",
    "input_schema": {
      "description": "Request body for creating a plugin marketplace.",
      "properties": {
        "name": {
          "description": "Unique name within the org (kebab-case, e.g. `my-org-plugins`).",
          "example": "everruns-plugins",
          "type": "string"
        },
        "source": {
          "description": "Source value. For `github`: `\"owner/repo\"`. For `url`: full HTTPS URL.\nFor `local_path` (dev only): absolute filesystem path.",
          "example": "everruns/plugins",
          "type": "string"
        },
        "source_type": {
          "description": "Source type: `github`, `url`, or `local_path` (dev/test only).",
          "example": "github",
          "type": "string"
        }
      },
      "required": [
        "name",
        "source_type",
        "source"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_plugin_marketplace",
    "output_shape": "unknown",
    "path": "/v1/plugin_marketplaces",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "providers",
    "cli": null,
    "description": "Create a new LLM provider.",
    "input_schema": {
      "$defs": {
        "DriverId": {
          "description": "LLM provider type. Built-in: openai, openrouter, azure_openai, openai_completions, anthropic, gemini, llmsim, bedrock, mai, fireworks, meta. Any other string is treated as an embedder-defined external provider.",
          "type": "string"
        },
        "ProviderRequestHeader": {
          "description": "One extra HTTP header sent with every request to a provider connection.",
          "properties": {
            "name": {
              "description": "Header name, e.g. `x-gateway-tenant`.",
              "type": "string"
            },
            "value": {
              "description": "Header value, sent verbatim.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "value"
          ],
          "type": "object"
        },
        "ProviderRequestOptions": {
          "description": "Per-connection request options: what an org wants added to every outbound\nrequest to this provider, beyond endpoint and credentials.\n\nThese are connection-level on purpose. A gateway header or a diagnostics\nopt-in describes the *service* an org talks to, not one agent's behavior, so\nit belongs next to the base URL and credentials rather than on every agent.",
          "properties": {
            "cache_diagnostics": {
              "description": "Ask the provider to explain unexpected prompt-cache misses. Honored by\ndrivers with a diagnostics protocol (today: Anthropic's\n`cache-diagnosis` beta); ignored elsewhere.",
              "type": "boolean"
            },
            "headers": {
              "description": "Extra HTTP headers added to every request to this provider. They\noverride the driver's and the connection's own headers by name, but\nconnection-level headers (`host`, `content-length`, ...) are ignored.",
              "items": {
                "$ref": "#/$defs/ProviderRequestHeader"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "ProviderTraceConfig": {
          "description": "Configuration for linking from the chat UI to a provider's observability\ndashboard (\"trace\"/\"logs\").\n\nThis is provider-agnostic: any driver with a dashboard can supply default\ntemplates (see [`DriverId::default_trace_templates`]), and an org enables\nlinks per provider once it has confirmed logging is on for that account.\nURL templates support the `{response_id}`, `{session_id}`, `{turn_id}` and\n`{model}` placeholders, so the same mechanism works for OpenRouter today and\nfor third-party observability backends (Langfuse, Helicone, ...) via an\noverride.",
          "properties": {
            "enabled": {
              "description": "Whether trace links should be shown for this provider. Defaults to\n`false`: vendors typically do not retain trace content unless logging is\nexplicitly enabled, so the org opts in once that is set up.",
              "type": "boolean"
            },
            "generation_url_template": {
              "description": "URL template for a single generation's trace, e.g.\n`\"https://openrouter.ai/logs?id={response_id}\"`.",
              "type": [
                "string",
                "null"
              ]
            },
            "session_url_template": {
              "description": "URL template for a session's grouped trace, e.g.\n`\"https://openrouter.ai/logs\"`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "enabled"
          ],
          "type": "object"
        }
      },
      "properties": {
        "api_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "base_url": {
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "provider_type": {
          "$ref": "#/$defs/DriverId"
        },
        "request_options": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ProviderRequestOptions"
            }
          ]
        },
        "trace": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ProviderTraceConfig",
              "description": "Trace/observability link configuration override (driver defaults apply\nwhen omitted)."
            }
          ]
        }
      },
      "required": [
        "name",
        "provider_type"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_provider",
    "output_shape": "unknown",
    "path": "/v1/providers",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Create an org-scoped saved report definition.",
    "input_schema": {
      "$defs": {
        "CreateSavedReportRequest": {
          "description": "Request body for the `create_saved_report` operation.",
          "properties": {
            "dashboard": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SavedReportDashboardMetadata",
                  "description": "Optional dashboard placement metadata. Omit to create a library-only\nreport that isn't pinned to any dashboard layout."
                }
              ]
            },
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "example": "Rolling 30-day count of agents with at least one session per day, grouped by org.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "example": "Weekly active agents — last 30 days",
              "type": "string"
            },
            "query": {
              "$ref": "#/$defs/ReportQuery",
              "description": "The query this report executes when run or exported. See `ReportQuery`\nfor the full field breakdown."
            }
          },
          "required": [
            "name",
            "query"
          ],
          "type": "object"
        },
        "ReportFilter": {
          "description": "One predicate filter applied to the dataset before aggregation.\nCombined with other filters via logical AND.",
          "properties": {
            "field": {
              "description": "Field to filter on. Must be a filter field exposed by the dataset (see `filter_fields` in the catalog).",
              "example": "status",
              "type": "string"
            },
            "op": {
              "$ref": "#/$defs/ReportFilterOp",
              "description": "Comparison operator. Determines the expected shape of `value`\n(scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`, array for `in`)."
            },
            "value": {
              "description": "Comparison value. Type depends on `op`: a scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`,\nan array for `in`. Example for `op = in`: `[\"completed\", \"failed\"]`."
            }
          },
          "required": [
            "field",
            "op",
            "value"
          ],
          "type": "object"
        },
        "ReportFilterOp": {
          "description": "Comparison operator used in a `ReportFilter`. The `In` variant takes a\nJSON array as its value; all others take a scalar.",
          "enum": [
            "eq",
            "neq",
            "in",
            "gt",
            "gte",
            "lt",
            "lte"
          ],
          "type": "string"
        },
        "ReportOrderBy": {
          "description": "One sort clause applied to the aggregated result. Either `dimension`\nOR `measure` is set (mutually exclusive), never both.",
          "properties": {
            "dimension": {
              "description": "Dimension name to sort by. Mutually exclusive with `measure`.",
              "example": "org_id",
              "type": [
                "string",
                "null"
              ]
            },
            "direction": {
              "$ref": "#/$defs/ReportOrderDirection",
              "description": "Sort direction (`asc` or `desc`). Defaults to `asc`."
            },
            "measure": {
              "description": "Measure name to sort by. Mutually exclusive with `dimension`.",
              "example": "session_count",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ReportOrderDirection": {
          "description": "Sort direction for a `ReportOrderBy` clause.",
          "enum": [
            "asc",
            "desc"
          ],
          "type": "string"
        },
        "ReportQuery": {
          "description": "Semantic query a caller submits to the reporting layer. The backend\ncompiles this to its native query language, scopes it to the calling\norg, and returns a `ReportResult`.",
          "properties": {
            "dataset": {
              "description": "Dataset name to query (see `GET /v1/reports/catalog` for the list of available datasets).",
              "example": "sessions",
              "type": "string"
            },
            "dimensions": {
              "description": "Columns to group by. Empty list returns one aggregate row.",
              "example": [
                "status"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "filters": {
              "description": "Predicate filters applied before aggregation.",
              "items": {
                "$ref": "#/$defs/ReportFilter"
              },
              "type": "array"
            },
            "limit": {
              "description": "Maximum number of rows to return (defaults to 100).",
              "example": 100,
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "measures": {
              "description": "Aggregations to compute (count, sum, avg, etc.). Empty list returns row counts only.",
              "example": [
                "session_count",
                "avg_duration_ms"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "order_by": {
              "description": "Sort spec applied after aggregation. Empty list yields unspecified order.",
              "items": {
                "$ref": "#/$defs/ReportOrderBy"
              },
              "type": "array"
            },
            "time_range": {
              "$ref": "#/$defs/ReportTimeRange",
              "description": "Time window for the query. The dataset selects which timestamp column the range applies to."
            }
          },
          "required": [
            "dataset",
            "time_range"
          ],
          "type": "object"
        },
        "ReportTimeRange": {
          "description": "Half-open time window applied to the dataset's primary timestamp column\nduring a report query. `from` is inclusive, `to` is exclusive.",
          "properties": {
            "from": {
              "description": "Start of the window (RFC 3339, inclusive).",
              "example": "2026-04-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            },
            "to": {
              "description": "End of the window (RFC 3339, exclusive).",
              "example": "2026-05-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            }
          },
          "required": [
            "from",
            "to"
          ],
          "type": "object"
        },
        "SavedReportDashboardMetadata": {
          "description": "Dashboard placement metadata attached to a `SavedReport`. Captures how\nand where to render the report in the operator dashboard UI.",
          "properties": {
            "chart_type": {
              "description": "Rendering hint for the UI (e.g. `\"line\"`, `\"bar\"`, `\"table\"`,\n`\"big_number\"`). Free-form string — the server doesn't enforce a\nclosed set so new chart types can roll out client-side first.",
              "type": [
                "string",
                "null"
              ]
            },
            "position": {
              "description": "Sort key within `section`. Lower values render first. `None` means\n\"place at the end\".",
              "format": "int32",
              "type": [
                "integer",
                "null"
              ]
            },
            "section": {
              "description": "Dashboard section/group this report belongs to (free-form bucket name\nlike `\"Operations\"` or `\"Finance\"`). Used to cluster related reports\nin the UI.",
              "type": [
                "string",
                "null"
              ]
            },
            "title": {
              "description": "Human-readable title. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "$ref": "#/$defs/CreateSavedReportRequest"
    },
    "method": "POST",
    "name": "create_saved_report",
    "output_shape": "unknown",
    "path": "/v1/reports/saved",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Create a new durable scheduled task with a cron expression.",
    "input_schema": {
      "$defs": {
        "ScheduleTarget": {
          "description": "Target for a schedule - either a workflow or activity",
          "example": {
            "input": {
              "session_id": "session_01933b5a00007000800000000000001"
            },
            "name": "session.run",
            "type": "workflow"
          },
          "properties": {
            "input": {
              "description": "Input JSON for the workflow/activity"
            },
            "name": {
              "description": "Workflow type name or activity type name",
              "example": "session.run",
              "type": "string"
            },
            "type": {
              "description": "Target type: \"workflow\" or \"activity\"",
              "example": "workflow",
              "type": "string"
            }
          },
          "required": [
            "type",
            "name"
          ],
          "type": "object"
        }
      },
      "description": "Create schedule request",
      "properties": {
        "catch_up_missed": {
          "description": "Whether to catch up missed triggers (default: false)",
          "example": false,
          "type": "boolean"
        },
        "cron_expression": {
          "description": "Cron expression (5-field or 7-field). Standard `min hour day-of-month month day-of-week` form.",
          "example": "0 2 * * *",
          "type": "string"
        },
        "description": {
          "description": "Optional description",
          "example": "Fires the support-triage agent every night at 02:00 UTC",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "description": "Whether schedule is enabled (default: true)",
          "example": true,
          "type": "boolean"
        },
        "max_catch_up": {
          "description": "Max catch-up executions when `catch_up_missed` is true. Older missed fires are dropped.",
          "example": 3,
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_concurrent": {
          "description": "Max concurrent executions. Omit for no limit beyond the worker pool's concurrency.",
          "example": 1,
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "name": {
          "description": "Unique name for the schedule",
          "example": "nightly-triage",
          "type": "string"
        },
        "retry_policy": {
          "description": "Retry policy for failed executions (provider-specific JSON; see the durable engine's `RetryPolicy`).\nExample: `{\"max_attempts\": 3, \"initial_backoff_secs\": 30, \"backoff_multiplier\": 2.0}`."
        },
        "target": {
          "$ref": "#/$defs/ScheduleTarget",
          "description": "Target to trigger. Variant shape is defined on `ScheduleTarget`."
        },
        "timezone": {
          "description": "Timezone (default: UTC). IANA name (e.g. `UTC`, `America/New_York`).",
          "example": "UTC",
          "type": "string"
        }
      },
      "required": [
        "name",
        "cron_expression",
        "target"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_schedule",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions create",
    "description": "Create a new session. Optionally assign an agent and harness. Assign the agent with --agent_id or --agent_name (the everruns CLI spelling of this flag is --agent).",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "BuiltinTool": {
          "description": "Built-in tool configuration\n\nNote: The `kind` field has been removed. Tools are now identified\nsolely by their `name` field, and execution happens via the ToolRegistry\nwhich looks up tools by name.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering (e.g., \"Get Current Time\" for `get_current_time`)",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for registry lookup)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            },
            "policy": {
              "$ref": "#/$defs/ToolPolicy",
              "description": "Tool policy (auto or requires_approval)"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ClientSideTool": {
          "description": "Client-side tool - executed by the client, not the server\nThe server pauses execution and waits for the client to submit results.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for correlation)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "DeferrablePolicy": {
          "description": "Controls whether a tool's full schema can be deferred (tool_search).\n\nWhen tool_search is active and a model supports it, tools marked as\n`Automatic` or `Always` will have `defer_loading: true` set, meaning\nonly the name+description are sent upfront and full parameter schemas\nare loaded on-demand by the model.",
          "enum": [
            "never",
            "automatic",
            "always"
          ],
          "type": "string"
        },
        "InitialFile": {
          "description": "Starter file copied into a new session from an agent or harness.",
          "properties": {
            "content": {
              "description": "File content: plain text or base64-encoded binary.",
              "type": "string"
            },
            "encoding": {
              "description": "Content encoding: `text` or `base64`.",
              "type": "string"
            },
            "is_readonly": {
              "description": "Prevent session-side edits or deletes when true.",
              "type": "boolean"
            },
            "path": {
              "description": "Absolute path within the session workspace. `/workspace` prefix is accepted.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "NetworkAccessList": {
          "description": "Network access list controlling which hosts/URLs an agent session can reach.\n\n- `allowed`: if non-empty, only URLs matching these patterns are permitted.\n- `blocked`: URLs matching these patterns are always denied (takes precedence over allowed).\n\nPattern format:\n- `example.com` — exact domain match (any port, any path)\n- `*.example.com` — domain and all subdomains\n- `https://example.com/api/` — exact URL prefix (scheme + host + path)",
          "example": {
            "allowed": [
              "*.example.com",
              "https://api.acme.com/"
            ],
            "blocked": [
              "169.254.169.254"
            ]
          },
          "properties": {
            "allowed": {
              "description": "Allowed host patterns. If non-empty, only matching URLs are permitted.\nAn empty list means \"no restriction from this layer\" (inherit parent).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "blocked": {
              "description": "Blocked host patterns. Always denied, even if matched by `allowed`.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "SideEffectClass": {
          "description": "How many times a tool call may safely be executed given the same inputs.\n\nUsed by the durable Act activity (EVE-530) to decide what to do when a\nprior execution attempt left a `running` claim in `durable_tool_results`:\n\n* `Pure` / `Idempotent` — the running claim is stale; re-execute freely.\n* `AtMostOnce` — never re-execute from a stale running claim; settle it\n  as `interrupted` and surface an uncertain result to the model instead.\n\nWhen unset (`None`), the conservative default is `AtMostOnce`.",
          "enum": [
            "Pure",
            "Idempotent",
            "AtMostOnce"
          ],
          "type": "string"
        },
        "ToolDefinition": {
          "description": "Tool definition in agent configuration",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/BuiltinTool",
                  "description": "Built-in tool - executed by the worker via ToolRegistry"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "builtin"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Built-in tool - executed by the worker via ToolRegistry"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ClientSideTool",
                  "description": "Client-side tool - executed by the client, not the server"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "client_side"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Client-side tool - executed by the client, not the server"
            }
          ]
        },
        "ToolHints": {
          "description": "Semantic hints describing a tool's behavioral properties.\n\nFollows the MCP tool annotations convention (readOnlyHint, destructiveHint,\nidempotentHint, openWorldHint) plus everruns-specific hints. All fields are\noptional booleans — `None` means \"unknown/unspecified\". Consumers should\ntreat `None` as the conservative default (e.g., assume not readonly, assume\nnot idempotent).\n\nThese hints are informational — they do not enforce policy. Use `ToolPolicy`\nfor execution gating (auto vs requires_approval).",
          "properties": {
            "capability_id": {
              "description": "Capability that contributed this tool definition.\n\nReporting uses this attribution only as metadata. It must never contain\ntool arguments, results, prompts, or any other sensitive payload.",
              "type": [
                "string",
                "null"
              ]
            },
            "capability_name": {
              "description": "Human-readable capability name snapshot for reporting.",
              "type": [
                "string",
                "null"
              ]
            },
            "concurrency_class": {
              "description": "Scheduling conflict key. Tool calls within the same act batch that share\na non-empty `concurrency_class` are executed sequentially in arrival\norder; calls in different classes (or with no class) run concurrently.\n\nSet this on tools that mutate shared session state so that, e.g., two\nfile writes or two SQL mutations in one batch do not race. Read-only\ntools should leave this `None` so they always parallelize. See\n`everruns-engine`'s tool scheduler for how the act phase consumes it.",
              "type": [
                "string",
                "null"
              ]
            },
            "cpu_bound": {
              "description": "Tool performs significant CPU-bound or otherwise non-yielding work in\nprocess (e.g. an in-process interpreter). When true, the act scheduler\nruns the call on its own task (`tokio::spawn`) so a long CPU burst does\nnot starve the cooperative polling of I/O-bound tools in the same batch.\n\nDistinct from `long_running`, which describes wall-clock time for\nI/O-bound work (those tools yield at await points and need no offload).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "destructive": {
              "description": "Tool may irreversibly destroy or delete data.\nSubset of non-readonly — a tool can be non-readonly (writes) without\nbeing destructive (e.g., create/update operations).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "idempotent": {
              "description": "Calling the tool repeatedly with the same arguments produces the same\neffect. Safe to retry on transient failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "long_running": {
              "description": "Tool may take significant time to complete (> ~5s typical).\nUseful for clients to show progress indicators and set timeouts.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "metadata": {
              "description": "Host-owned annotations that core does not interpret.\n\nThe typed hints above are the vocabulary core itself reasons about. This\nis the escape hatch for everything a *host* wants to carry alongside a\ntool — risk tiers for an approval UI, presentation hints, an embedder's\nrouting keys — without adding a field to core for each one. Core reads\nnothing here and no driver sends it to a provider; it travels with the\ndefinition so a consumer sees it at the point of decision (e.g. a\n`PreToolUseHook` gating on what the tool declared).\n\nThe schema belongs to whoever writes it. Never put credentials or other\nsensitive payload here: like the rest of the definition, it is persisted\nand surfaced to clients."
            },
            "narration_noun": {
              "description": "Entity noun for operation-based narration (e.g. \"agent\", \"harness\").\nWhen set, the narration system reads the `operation` argument and\nproduces verb-based narration like \"Created agent: Neon Cartographer\"\ninstead of the generic \"Ran Manage Agents\".",
              "type": [
                "string",
                "null"
              ]
            },
            "open_world": {
              "description": "Tool interacts with external entities beyond the local system\n(network calls, third-party APIs, cloud services).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "persist_output": {
              "description": "Tool output should be persisted to session VFS before truncation.\nWhen set, the `tool_output_persistence` capability (EVE-222, EVE-245) writes\nstdout to `/outputs/{tool_call_id}.stdout` and stderr to\n`/outputs/{tool_call_id}.stderr`, injecting `full_output`, `total_lines`,\nand `output_files` into the result.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "readonly": {
              "description": "Tool does not modify any state (read-only queries, lookups).\nWhen true: safe to call speculatively, result can be cached.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "requires_secrets": {
              "description": "Tool requires API keys, credentials, or other secrets to function.\nUseful for UI to show connection prompts and for LLMs to anticipate\nauthentication failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "side_effect_class": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SideEffectClass",
                  "description": "Replay-safety class used by the durable Act activity (EVE-530).\n\nControls what happens when a worker reclaims a stale `running` claim:\n`Pure`/`Idempotent` tools are re-executed; `AtMostOnce` tools are\nsettled as `interrupted` to prevent double side-effects.\n\n`None` is treated conservatively as `AtMostOnce`."
                }
              ]
            },
            "supports_background": {
              "description": "Tool supports detached background execution via `spawn_background`.\nWhen true, the tool may be executed asynchronously outside the current\nforeground tool call and report status back later.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ToolPolicy": {
          "description": "Tool policy determines how tool calls are handled",
          "enum": [
            "auto",
            "requires_approval",
            "client_side"
          ],
          "type": "string"
        }
      },
      "description": "Request to create a session",
      "properties": {
        "agent_id": {
          "description": "ID of the agent to work in this session (optional, format: agent_{32-hex}).\nWhen supplied without a harness, the session inherits the agent's harness.\nMutually exclusive with `agent_name`.",
          "example": "agent_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "agent_identity_id": {
          "description": "Optional resident agent identity used for unattended/background execution.",
          "example": "identity_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "agent_name": {
          "description": "Name of the agent to work in this session (optional).\nAlternative to `agent_id` — looked up by name within the org.\nMutually exclusive with `agent_id`.",
          "example": "support",
          "type": [
            "string",
            "null"
          ]
        },
        "capabilities": {
          "description": "Session-level capabilities (additive to agent capabilities).\nApplied after agent capabilities when building RuntimeAgent.",
          "example": [
            {
              "config": {},
              "ref": "current_time"
            },
            {
              "config": {},
              "ref": "web_fetch"
            }
          ],
          "items": {
            "$ref": "#/$defs/AgentCapabilityConfig"
          },
          "type": "array"
        },
        "goal": {
          "description": "Optional objective for the session. Visible to the agent at system-prompt level.",
          "example": "Investigate the queue latency regression and propose a fix",
          "type": [
            "string",
            "null"
          ]
        },
        "harness_id": {
          "description": "ID of the harness for this session (format: harness_{32-hex}).\nIf omitted, the harness is derived from the agent (when one is supplied),\nelse the org default harness, else the built-in fallback. New orgs default\nthat to Generic. Mutually exclusive with `harness_name`.",
          "example": "harness_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "harness_name": {
          "description": "Harness name (e.g. \"generic\", \"deep-research\").\nAlternative to `harness_id` — looked up by name within the org.\nMutually exclusive with `harness_id`.",
          "example": "generic",
          "type": [
            "string",
            "null"
          ]
        },
        "hints": {
          "additionalProperties": {},
          "description": "Session-level client hints — arbitrary key-value pairs that tell the\nserver what the client can handle. These are defaults for every turn;\nper-message `controls.hints` override these key-by-key (shallow merge).\n\nTwo hints decide whether a turn may pause for the user rather than talk\npast them: `setup_connection` (the client renders connection setup) and\n`url_elicitation` (the client renders consent for a URL an MCP server\nasks the user to open).",
          "example": {
            "rich_media": true,
            "setup_connection": true,
            "url_elicitation": true
          },
          "propertyNames": {
            "type": "string"
          },
          "type": [
            "object",
            "null"
          ]
        },
        "initial_files": {
          "description": "Session-level initial files (additive to agent initial_files).\nFiles with matching paths override agent/harness files; new paths are appended.",
          "example": [
            {
              "content": "# Project notes\n",
              "path": "README.md"
            }
          ],
          "items": {
            "$ref": "#/$defs/InitialFile"
          },
          "type": "array"
        },
        "locale": {
          "description": "Session locale (BCP 47, e.g. `uk-UA`).",
          "example": "uk-UA",
          "type": [
            "string",
            "null"
          ]
        },
        "max_iterations": {
          "description": "Maximum number of LLM iterations per turn for this session.",
          "example": 20,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "mcpServers": {
          "$ref": "#/$defs/BTreeMap",
          "description": "Remote MCP servers scoped to this session only."
        },
        "model_id": {
          "description": "The ID of the LLM model to use for this session.\nOverrides the agent's default model if specified.",
          "example": "model_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "network_access": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/NetworkAccessList",
              "description": "Network access list controlling which hosts/URLs this session can reach.\nMerged with harness and agent layers (allowed: intersect, blocked: union).\nExample shape is defined on `NetworkAccessList`."
            }
          ]
        },
        "parallel_tool_calls": {
          "description": "Request-level parallel tool calling preference (EVE-598). `true` signals\nthe provider that parallel tool calls are wanted; `false` requests at\nmost one tool call per turn and forces serial execution. Omit to inherit\nthe agent/harness preference or the provider default.",
          "example": true,
          "type": [
            "boolean",
            "null"
          ]
        },
        "source": {
          "description": "How this session was started. Clients may declare only `chat` (an\ninteractive thread) or `api` (the default); every other source is\nserver-owned so the sessions facet rail stays trustworthy.",
          "example": "chat",
          "type": [
            "string",
            "null"
          ]
        },
        "system_prompt": {
          "description": "Optional session-level system prompt override.\nPrepended to the agent's system prompt when building RuntimeAgent.",
          "example": "You are debugging a production incident. Be concise and cite log lines verbatim.",
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "description": "Tags for organizing and filtering sessions.",
          "example": [
            "debugging",
            "urgent"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "title": {
          "description": "Human-readable title for the session.",
          "example": "Debug login issue",
          "type": [
            "string",
            "null"
          ]
        },
        "tools": {
          "description": "Client-side tools for this session (additive to agent tools).\nThese tools are sent to the LLM but executed by the client.",
          "example": [
            {
              "description": "Open URL in the user's browser",
              "name": "open_url",
              "parameters": {
                "properties": {
                  "url": {
                    "type": "string"
                  }
                },
                "required": [
                  "url"
                ],
                "type": "object"
              },
              "type": "client_side"
            }
          ],
          "items": {
            "$ref": "#/$defs/ToolDefinition"
          },
          "type": "array"
        },
        "workspace_id": {
          "description": "Attach this session to an existing Workspace (format: `wsp_<32-hex>`)\ninstead of auto-creating a default per-session workspace. The workspace\nmust exist in the caller's org and be `active`. Lets multiple sessions\nshare one working filesystem. Omit for the default 1:1 behavior.",
          "example": "wsp_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "POST",
    "name": "create_session",
    "output_shape": "unknown",
    "path": "/v1/sessions",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "session_databases",
    "cli": null,
    "description": "Create a new SQL database inside a session.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_session_database",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/databases",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "skills",
    "cli": "skills create",
    "description": "Create a new skill from SKILL.md content.",
    "input_schema": {
      "description": "Request to create a skill from SKILL.md content",
      "properties": {
        "skill_md": {
          "description": "Full SKILL.md content (YAML frontmatter + markdown body)",
          "example": "---\nname: pdf-processing\ndescription: Extract text from PDFs.\n---\n\n# PDF Processing\n\nUse pdfplumber...",
          "type": "string"
        }
      },
      "required": [
        "skill_md"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_skill",
    "output_shape": "unknown",
    "path": "/v1/skills",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "Create a per-task push-notification config.",
    "input_schema": {
      "properties": {
        "event_filter": {
          "description": "Events that trigger delivery. Defaults to `[\"terminal\"]`.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "secret": {
          "description": "Optional HMAC-SHA256 signing secret. When set, every delivery includes\nan `X-Everruns-Signature: sha256=<hex>` header. Never returned once set.",
          "type": [
            "string",
            "null"
          ]
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "task_id": {
          "description": "Task's prefixed public identifier.",
          "type": "string"
        },
        "url": {
          "description": "URL to POST task events to.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "task_id",
        "url"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_task_push_config",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tasks/{task_id}/push-configs",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "workspaces",
    "cli": null,
    "description": "Create a new Workspace in the current organization.",
    "input_schema": {
      "properties": {
        "description": {
          "description": "Optional human-readable description.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Human-readable workspace name. Must be unique per org.",
          "type": "string"
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "create_workspace",
    "output_shape": "unknown",
    "path": "/v1/workspaces",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "files",
    "cli": null,
    "description": "Create a file or directory in the session filesystem.",
    "input_schema": {
      "$defs": {
        "CreateFileRequest": {
          "description": "Request to create a file",
          "properties": {
            "content": {
              "description": "File content (text or base64-encoded). Must match `encoding`.",
              "example": "# Project notes\n\nDraft outline of the migration plan.\n",
              "type": [
                "string",
                "null"
              ]
            },
            "encoding": {
              "description": "Content encoding: \"text\" or \"base64\". Defaults to text.",
              "example": "text",
              "type": [
                "string",
                "null"
              ]
            },
            "is_directory": {
              "description": "Whether to create a directory instead of a file (ignores `content`/`encoding`).",
              "example": false,
              "type": [
                "boolean",
                "null"
              ]
            },
            "is_readonly": {
              "description": "Whether file is read-only",
              "example": false,
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CreateFileRequest"
        },
        {
          "properties": {
            "path": {
              "type": "string"
            },
            "session_id": {
              "description": "Session's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "path"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "create_workspace_file",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/{path}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents delete",
    "description": "Archive an agent (soft delete). Can be restored.",
    "input_schema": {
      "description": "Archive an agent (soft delete).",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "agents",
    "cli": null,
    "description": "Delete an agent check rule (built-in override or custom rule).",
    "input_schema": {
      "description": "Delete a check rule (removes a built-in override or a custom rule).",
      "properties": {
        "rule_id": {
          "type": "string"
        }
      },
      "required": [
        "rule_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_agent_check_rule",
    "output_shape": "unknown",
    "path": "/v1/agents/check-rules/{rule_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "Delete an agent ingress endpoint.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "endpoint_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "endpoint_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_agent_endpoint",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints/{endpoint_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_identities",
    "cli": null,
    "description": "Archive an agent identity (soft delete). Can be restored.",
    "input_schema": {
      "description": "Archive an agent identity (soft delete).",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_agent_identity",
    "output_shape": "unknown",
    "path": "/v1/agent-identities/{identity_id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "agent_triggers",
    "cli": null,
    "description": "Archive an agent trigger and remove its durable schedule.",
    "input_schema": {
      "description": "Archive a trigger (soft delete) and tear down its durable schedule.",
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "trigger_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "trigger_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_agent_trigger",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/triggers/{trigger_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Delete a budget.",
    "input_schema": {
      "properties": {
        "budget_id": {
          "type": "string"
        }
      },
      "required": [
        "budget_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_budget",
    "output_shape": "unknown",
    "path": "/v1/budgets/{budget_id}",
    "positional_arg": "budget_id",
    "read_only": false
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "Archive a persisted declarative capability.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_declarative_capability",
    "output_shape": "unknown",
    "path": "/v1/capabilities/declarative/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Delete an eval.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_eval",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Delete an eval case.",
    "input_schema": {
      "properties": {
        "case_id": {
          "type": "string"
        },
        "eval_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "case_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_eval_case",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/cases/{case_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Archive a harness (soft delete). Can be restored.",
    "input_schema": {
      "description": "Archive a harness (soft delete).",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_harness",
    "output_shape": "unknown",
    "path": "/v1/harnesses/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Archive a knowledge base.",
    "input_schema": {
      "properties": {
        "kb_id": {
          "description": "Knowledge base's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "kb_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_knowledge_base",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Delete a knowledge entry.",
    "input_schema": {
      "properties": {
        "entry_id": {
          "description": "Knowledge base entry's prefixed public identifier.",
          "type": "string"
        },
        "kb_id": {
          "description": "Knowledge base's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "kb_id",
        "entry_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_knowledge_entry",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}/entries/{entry_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_indexes",
    "cli": null,
    "description": "Archive a knowledge index.",
    "input_schema": {
      "properties": {
        "index_id": {
          "description": "Knowledge index's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "index_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_knowledge_index",
    "output_shape": "unknown",
    "path": "/v1/knowledge-indexes/{index_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "mcp_servers",
    "cli": "mcp-servers delete",
    "description": "Archive an MCP server (soft delete). Can be restored.",
    "input_schema": {
      "description": "Archive an MCP server (soft delete).",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_mcp_server",
    "output_shape": "unknown",
    "path": "/v1/mcp-servers/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "memories",
    "cli": null,
    "description": "Archive a workspace memory.",
    "input_schema": {
      "properties": {
        "memory_id": {
          "description": "Workspace memory's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "memory_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_memory",
    "output_shape": "unknown",
    "path": "/v1/memories/{memory_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "models",
    "cli": null,
    "description": "Delete a model.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_model",
    "output_shape": "unknown",
    "path": "/v1/models/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "observers",
    "cli": null,
    "description": "Archive an observer.",
    "input_schema": {
      "properties": {
        "observer_id": {
          "type": "string"
        }
      },
      "required": [
        "observer_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_observer",
    "output_shape": "unknown",
    "path": "/v1/observers/{observer_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Delete a plugin marketplace. Installed plugins become unattached.",
    "input_schema": {
      "description": "Delete a plugin marketplace registration. Installed plugins from this\nmarketplace become unattached (marketplace_id is set to NULL).",
      "properties": {
        "id": {
          "description": "Public marketplace ID.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_plugin_marketplace",
    "output_shape": "unknown",
    "path": "/v1/plugin_marketplaces/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "providers",
    "cli": null,
    "description": "Delete an LLM provider.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_provider",
    "output_shape": "unknown",
    "path": "/v1/providers/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Delete an org-scoped saved report definition.",
    "input_schema": {
      "properties": {
        "report_id": {
          "description": "Saved report's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "report_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_saved_report",
    "output_shape": "unknown",
    "path": "/v1/reports/saved/{report_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Delete a durable schedule.",
    "input_schema": {
      "properties": {
        "schedule_id": {
          "description": "Schedule's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "schedule_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_schedule",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions delete",
    "description": "Delete a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "session_databases",
    "cli": null,
    "description": "Delete a session SQL database.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "name"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_session_database",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/databases/{name}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "session_storage",
    "cli": null,
    "description": "Delete a user-managed encrypted session secret by name.",
    "input_schema": {
      "description": "Delete one encrypted secret from a session's private storage.",
      "properties": {
        "name": {
          "description": "Exact secret name to delete.",
          "example": "SERVICE_TOKEN",
          "type": "string"
        },
        "session_id": {
          "description": "Session that owns the secret.",
          "example": "session_01933b5a000070008000000000000001",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "name"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_session_secret",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/storage/secrets/{name}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "skills",
    "cli": "skills delete",
    "description": "Archive a skill (soft delete). Can be restored.",
    "input_schema": {
      "description": "Archive a skill (soft delete).",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_skill",
    "output_shape": "unknown",
    "path": "/v1/skills/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "Delete a per-task push-notification config.",
    "input_schema": {
      "properties": {
        "config_id": {
          "description": "Push config public id (tpc_...).",
          "type": "string"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "task_id": {
          "description": "Task's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "task_id",
        "config_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_task_push_config",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tasks/{task_id}/push-configs/{config_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "workspaces",
    "cli": null,
    "description": "Archive a Workspace (soft-delete).",
    "input_schema": {
      "properties": {
        "workspace_id": {
          "description": "Workspace ID (wsp_<32-hex>).",
          "type": "string"
        }
      },
      "required": [
        "workspace_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_workspace",
    "output_shape": "unknown",
    "path": "/v1/workspaces/{workspace_id}",
    "positional_arg": "workspace_id",
    "read_only": false
  },
  {
    "category": "files",
    "cli": null,
    "description": "Delete a file or directory in the session filesystem.",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        },
        "recursive": {
          "type": "boolean"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "path"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "delete_workspace_file",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/{path}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents destroy",
    "description": "Permanently delete an archived agent.",
    "input_schema": {
      "description": "Permanently delete an archived agent.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "destroy_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/{id}/delete",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "agent_identities",
    "cli": null,
    "description": "Permanently delete an agent identity.",
    "input_schema": {
      "description": "Permanently delete an agent identity.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "destroy_agent_identity",
    "output_shape": "unknown",
    "path": "/v1/agent-identities/{identity_id}/delete",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "Permanently delete an archived declarative capability.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "destroy_declarative_capability",
    "output_shape": "unknown",
    "path": "/v1/capabilities/declarative/{id}/delete",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Permanently delete an archived harness.",
    "input_schema": {
      "description": "Permanently delete an archived harness.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "destroy_harness",
    "output_shape": "unknown",
    "path": "/v1/harnesses/{id}/delete",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "mcp_servers",
    "cli": "mcp-servers destroy",
    "description": "Permanently delete an archived MCP server.",
    "input_schema": {
      "description": "Permanently delete an archived MCP server.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "destroy_mcp_server",
    "output_shape": "unknown",
    "path": "/v1/mcp-servers/{id}/delete",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "skills",
    "cli": "skills destroy",
    "description": "Permanently delete an archived skill.",
    "input_schema": {
      "description": "Permanently delete an archived skill.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "destroy_skill",
    "output_shape": "unknown",
    "path": "/v1/skills/{id}/delete",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents versions diff",
    "description": "Compare two immutable agent versions.",
    "input_schema": {
      "$defs": {
        "agentverId": {
          "description": "Prefixed identifier with 'agentver' prefix",
          "example": "agentver_01933b5a000070008000000000000001",
          "pattern": "^agentver_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "properties": {
        "agent_id": {
          "description": "Agent's prefixed public identifier.",
          "type": "string"
        },
        "from_version_id": {
          "$ref": "#/$defs/agentverId"
        },
        "to_version_id": {
          "$ref": "#/$defs/agentverId"
        }
      },
      "required": [
        "agent_id",
        "from_version_id",
        "to_version_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "diff_agent_versions",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/versions/{from_version_id}/diff/{to_version_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Disable a machine-payment wallet account.",
    "input_schema": {
      "properties": {
        "payment_account_id": {
          "type": "string"
        }
      },
      "required": [
        "payment_account_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "disable_payment_account",
    "output_shape": "unknown",
    "path": "/v1/payments/accounts/{payment_account_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Disable a machine-payment spend policy.",
    "input_schema": {
      "properties": {
        "payment_policy_id": {
          "type": "string"
        }
      },
      "required": [
        "payment_policy_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "disable_payment_policy",
    "output_shape": "unknown",
    "path": "/v1/payments/policies/{payment_policy_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "Evaluate a guardrails capability config against sample text without a session. Returns triggered checks and whether the content would be blocked.",
    "input_schema": {
      "$defs": {
        "GuardrailStage": {
          "description": "Pipeline stage a check applies to.",
          "enum": [
            "output",
            "tool_use",
            "tool_output"
          ],
          "example": "output",
          "type": "string"
        }
      },
      "description": "Request body for the `dry_run_guardrails` operation: evaluate a\nguardrails capability config against sample content without a session.",
      "properties": {
        "config": {
          "description": "The `guardrails` capability config to evaluate (same shape persisted\nin `AgentCapabilityConfig.config`).",
          "type": "object"
        },
        "stage": {
          "$ref": "#/$defs/GuardrailStage",
          "description": "Pipeline stage to evaluate."
        },
        "text": {
          "description": "Sample content: model output, serialized tool arguments, or tool\noutput, depending on `stage`.",
          "example": "the model said something darn surprising",
          "type": "string"
        },
        "tool_name": {
          "description": "Tool name for `tool_use` stage checks (`tool_pattern` rules).",
          "example": "bashkit_exec",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "config",
        "stage",
        "text"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "dry_run_guardrails",
    "output_shape": "unknown",
    "path": "/v1/capabilities/guardrails/dry-run",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Report whether the caller can import eval results.",
    "input_schema": {
      "description": "Preflight: report whether the caller can import (feature enabled + has the\neval-management permission) without failing. No policy gate so any org\nmember can probe; the report just returns `false`.",
      "type": "object"
    },
    "method": "GET",
    "name": "eval_import_preflight",
    "output_shape": "unknown",
    "path": "/v1/evals/import/preflight",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "events",
    "cli": null,
    "description": "One-shot debug summary for a session: counts by type, first/last timestamps, turn count, error count.",
    "input_schema": {
      "description": "Debug snapshot for a session: per-type counts and time span.",
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "events_summary",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/events/summary",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents export",
    "description": "Export agent as JSON.",
    "input_schema": {
      "description": "Get agent data for export.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "export_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/{id}/export",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Export eval run artifacts as NDJSON.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "run_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "export_eval_run_artifacts",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/artifacts",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Enqueue an async reward-labeled trajectory dataset export from a completed eval run.",
    "input_schema": {
      "$defs": {
        "DatasetFilters": {
          "description": "Selection filters applied per case. `org_id` is never part of this — it is\ninjected by the command from the authenticated caller, so a filter can never\nwiden the org scope.",
          "properties": {
            "min_score": {
              "description": "Keep only cases whose mean scorer value is >= this (0.0–1.0).",
              "format": "double",
              "type": [
                "number",
                "null"
              ]
            },
            "pass": {
              "description": "Keep only cases whose pass/fail equals this.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "DatasetFormat": {
          "description": "Output schema for the exported dataset.",
          "enum": [
            "trajectory",
            "sft",
            "atif"
          ],
          "type": "string"
        },
        "ExportEvalRunDatasetRequest": {
          "description": "Request body for `POST /v1/evals/{eval_id}/runs/{run_id}/dataset`.",
          "properties": {
            "filters": {
              "$ref": "#/$defs/DatasetFilters",
              "description": "Per-case selection filters."
            },
            "format": {
              "$ref": "#/$defs/DatasetFormat",
              "description": "Output schema (defaults to `trajectory`)."
            },
            "redaction": {
              "$ref": "#/$defs/RedactionOptions",
              "description": "Redaction controls."
            }
          },
          "type": "object"
        },
        "RedactionOptions": {
          "description": "Redaction controls. Secret scrubbing is always applied regardless of these.",
          "properties": {
            "redact_content": {
              "description": "When true, replace message text/tool content with a placeholder, keeping\nonly structure (roles, tool names, ids). Secret scrubbing still runs.",
              "type": "boolean"
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/ExportEvalRunDatasetRequest"
        },
        {
          "properties": {
            "eval_id": {
              "type": "string"
            },
            "run_id": {
              "type": "string"
            }
          },
          "required": [
            "eval_id",
            "run_id"
          ],
          "type": "object"
        }
      ],
      "description": "Enqueue an async dataset export for a completed eval run and return a handle.\n\nThe export runs as a fire-and-forget background job (mirroring the eval\nrunner): it reconstructs each surviving case's model-view messages (faithful\nto what the model saw, via the compaction model-view masking), joins reward +\nefficiency metadata, and stores the produced NDJSON on the handle. Fetch the\nNDJSON via `GET .../dataset/{dataset_id}` once `status` is `completed`. Gated\nby `DATASET_EXPORT` and org-scoped through `get_run` (which resolves only\nruns owned by the caller's org)."
    },
    "method": "POST",
    "name": "export_eval_run_dataset",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/dataset",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Run and export an org-scoped semantic reporting query.",
    "input_schema": {
      "$defs": {
        "ExportReportQueryRequest": {
          "description": "Request body for the `export_report_query` operation.",
          "properties": {
            "format": {
              "$ref": "#/$defs/ReportExportFormat",
              "description": "Export format. Defaults to `csv` when omitted."
            },
            "query": {
              "$ref": "#/$defs/ReportQuery",
              "description": "Ad-hoc query to materialize and export. Same shape as the body of\n`POST /v1/reports/query` — see `ReportQuery` for the field breakdown."
            }
          },
          "required": [
            "query"
          ],
          "type": "object"
        },
        "ReportExportFormat": {
          "description": "Output format for a report export. `Csv` emits a header row plus one\nrow per result; `Json` emits an envelope with the same shape as\n`ReportResult`.",
          "enum": [
            "csv",
            "json"
          ],
          "type": "string"
        },
        "ReportFilter": {
          "description": "One predicate filter applied to the dataset before aggregation.\nCombined with other filters via logical AND.",
          "properties": {
            "field": {
              "description": "Field to filter on. Must be a filter field exposed by the dataset (see `filter_fields` in the catalog).",
              "example": "status",
              "type": "string"
            },
            "op": {
              "$ref": "#/$defs/ReportFilterOp",
              "description": "Comparison operator. Determines the expected shape of `value`\n(scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`, array for `in`)."
            },
            "value": {
              "description": "Comparison value. Type depends on `op`: a scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`,\nan array for `in`. Example for `op = in`: `[\"completed\", \"failed\"]`."
            }
          },
          "required": [
            "field",
            "op",
            "value"
          ],
          "type": "object"
        },
        "ReportFilterOp": {
          "description": "Comparison operator used in a `ReportFilter`. The `In` variant takes a\nJSON array as its value; all others take a scalar.",
          "enum": [
            "eq",
            "neq",
            "in",
            "gt",
            "gte",
            "lt",
            "lte"
          ],
          "type": "string"
        },
        "ReportOrderBy": {
          "description": "One sort clause applied to the aggregated result. Either `dimension`\nOR `measure` is set (mutually exclusive), never both.",
          "properties": {
            "dimension": {
              "description": "Dimension name to sort by. Mutually exclusive with `measure`.",
              "example": "org_id",
              "type": [
                "string",
                "null"
              ]
            },
            "direction": {
              "$ref": "#/$defs/ReportOrderDirection",
              "description": "Sort direction (`asc` or `desc`). Defaults to `asc`."
            },
            "measure": {
              "description": "Measure name to sort by. Mutually exclusive with `dimension`.",
              "example": "session_count",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ReportOrderDirection": {
          "description": "Sort direction for a `ReportOrderBy` clause.",
          "enum": [
            "asc",
            "desc"
          ],
          "type": "string"
        },
        "ReportQuery": {
          "description": "Semantic query a caller submits to the reporting layer. The backend\ncompiles this to its native query language, scopes it to the calling\norg, and returns a `ReportResult`.",
          "properties": {
            "dataset": {
              "description": "Dataset name to query (see `GET /v1/reports/catalog` for the list of available datasets).",
              "example": "sessions",
              "type": "string"
            },
            "dimensions": {
              "description": "Columns to group by. Empty list returns one aggregate row.",
              "example": [
                "status"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "filters": {
              "description": "Predicate filters applied before aggregation.",
              "items": {
                "$ref": "#/$defs/ReportFilter"
              },
              "type": "array"
            },
            "limit": {
              "description": "Maximum number of rows to return (defaults to 100).",
              "example": 100,
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "measures": {
              "description": "Aggregations to compute (count, sum, avg, etc.). Empty list returns row counts only.",
              "example": [
                "session_count",
                "avg_duration_ms"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "order_by": {
              "description": "Sort spec applied after aggregation. Empty list yields unspecified order.",
              "items": {
                "$ref": "#/$defs/ReportOrderBy"
              },
              "type": "array"
            },
            "time_range": {
              "$ref": "#/$defs/ReportTimeRange",
              "description": "Time window for the query. The dataset selects which timestamp column the range applies to."
            }
          },
          "required": [
            "dataset",
            "time_range"
          ],
          "type": "object"
        },
        "ReportTimeRange": {
          "description": "Half-open time window applied to the dataset's primary timestamp column\nduring a report query. `from` is inclusive, `to` is exclusive.",
          "properties": {
            "from": {
              "description": "Start of the window (RFC 3339, inclusive).",
              "example": "2026-04-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            },
            "to": {
              "description": "End of the window (RFC 3339, exclusive).",
              "example": "2026-05-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            }
          },
          "required": [
            "from",
            "to"
          ],
          "type": "object"
        }
      },
      "$ref": "#/$defs/ExportReportQueryRequest"
    },
    "method": "POST",
    "name": "export_report_query",
    "output_shape": "unknown",
    "path": "/v1/reports/query/export",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Run and export an org-scoped saved report definition.",
    "input_schema": {
      "$defs": {
        "ExportSavedReportRequest": {
          "description": "Request body for the `export_saved_report` operation.",
          "properties": {
            "format": {
              "$ref": "#/$defs/ReportExportFormat",
              "description": "Export format. Defaults to `csv` when omitted."
            }
          },
          "type": "object"
        },
        "ReportExportFormat": {
          "description": "Output format for a report export. `Csv` emits a header row plus one\nrow per result; `Json` emits an envelope with the same shape as\n`ReportResult`.",
          "enum": [
            "csv",
            "json"
          ],
          "type": "string"
        }
      },
      "properties": {
        "report_id": {
          "description": "Saved report's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        },
        "request": {
          "$ref": "#/$defs/ExportSavedReportRequest"
        }
      },
      "required": [
        "report_id",
        "request"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "export_saved_report",
    "output_shape": "unknown",
    "path": "/v1/reports/saved/{report_id}/export",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "messages",
    "cli": null,
    "description": "Export session messages as JSONL.",
    "input_schema": {
      "$defs": {
        "SessionExportFormat": {
          "description": "Output format for session export.",
          "enum": [
            "jsonl",
            "atif"
          ],
          "type": "string"
        }
      },
      "properties": {
        "format": {
          "$ref": "#/$defs/SessionExportFormat",
          "description": "Output format (defaults to `jsonl`)."
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "export_session_messages",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/export",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents versions fork",
    "description": "Fork an agent version into a new editable agent.",
    "input_schema": {
      "$defs": {
        "ForkAgentVersionRequest": {
          "description": "Request body for the `fork_agent_version` operation.",
          "properties": {
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "example": "Fork to test new refund-flow capabilities before promoting",
              "type": [
                "string",
                "null"
              ]
            },
            "display_name": {
              "description": "Human-readable display name. Safe to render in user-facing messages.",
              "example": "Support Agent (Experimental)",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "example": "support-agent-experimental",
              "type": "string"
            }
          },
          "required": [
            "name"
          ],
          "type": "object"
        },
        "agentverId": {
          "description": "Prefixed identifier with 'agentver' prefix",
          "example": "agentver_01933b5a000070008000000000000001",
          "pattern": "^agentver_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/ForkAgentVersionRequest"
        },
        {
          "properties": {
            "agent_id": {
              "description": "Agent's prefixed public identifier.",
              "type": "string"
            },
            "version_id": {
              "$ref": "#/$defs/agentverId",
              "description": "Agent version's prefixed public identifier."
            }
          },
          "required": [
            "agent_id",
            "version_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "fork_agent_version",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/versions/{version_id}/fork",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions fork",
    "description": "Fork a session into a new, independent session that copies its conversation history and workspace files.",
    "input_schema": {
      "$defs": {
        "ForkSessionRequest": {
          "description": "Request to fork a session (knowledge/runtime-resources/forking-sessions.md). Every field is\noptional; omitted fields inherit the parent session's value. Title defaults\nto \"{parent title} (fork)\" when omitted.",
          "properties": {
            "agent_id": {
              "description": "Override the agent assigned to the fork.",
              "example": "agent_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "goal": {
              "description": "Goal for the fork. Omitted inherits the parent's goal.",
              "example": "Try the async rewrite from this state",
              "type": [
                "string",
                "null"
              ]
            },
            "locale": {
              "description": "Override the locale (BCP 47).",
              "example": "uk-UA",
              "type": [
                "string",
                "null"
              ]
            },
            "model_id": {
              "description": "Override the LLM model for the fork.",
              "example": "model_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "system_prompt": {
              "description": "Override the session-level system prompt.",
              "type": [
                "string",
                "null"
              ]
            },
            "tags": {
              "description": "Tags for the fork. Replaces (does not merge with) the parent's tags.",
              "example": [
                "experiment"
              ],
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "title": {
              "description": "Title for the fork. Defaults to \"{parent title} (fork)\".",
              "example": "Branch: try the async rewrite",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "description": "Fork a session into a new, independent session (knowledge/runtime-resources/forking-sessions.md).",
      "properties": {
        "overrides": {
          "$ref": "#/$defs/ForkSessionRequest",
          "description": "Optional overrides applied to the fork."
        },
        "session_id": {
          "description": "Session to fork (prefixed public id).",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "fork_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fork",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents get",
    "description": "Get a single agent by ID or name.",
    "input_schema": {
      "description": "Get a single agent by ID or name.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "Get an agent ingress endpoint.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "endpoint_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "endpoint_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_agent_endpoint",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints/{endpoint_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agents",
    "cli": null,
    "description": "Get a single agent health check run with its results.",
    "input_schema": {
      "description": "Get a health check run by id.",
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "run_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_agent_health_check_run",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/health-checks/{run_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agent_identities",
    "cli": null,
    "description": "Get a single agent identity by ID.",
    "input_schema": {
      "description": "Get a single agent identity by ID.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_agent_identity",
    "output_shape": "unknown",
    "path": "/v1/agent-identities/{identity_id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "agent_triggers",
    "cli": null,
    "description": "Get a single agent trigger by id.",
    "input_schema": {
      "description": "Get a single trigger by id.",
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "trigger_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "trigger_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_agent_trigger",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/triggers/{trigger_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Get a single budget by ID.",
    "input_schema": {
      "properties": {
        "budget_id": {
          "type": "string"
        }
      },
      "required": [
        "budget_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_budget",
    "output_shape": "unknown",
    "path": "/v1/budgets/{budget_id}",
    "positional_arg": "budget_id",
    "read_only": true
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "Get a specific capability by ID.",
    "input_schema": {
      "description": "Get a specific capability by ID.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_capability",
    "output_shape": "unknown",
    "path": "/v1/capabilities/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "Get a persisted declarative capability.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_declarative_capability",
    "output_shape": "unknown",
    "path": "/v1/capabilities/declarative/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Get a single eval.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_eval",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}",
    "positional_arg": "eval_id",
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Get an eval case.",
    "input_schema": {
      "properties": {
        "case_id": {
          "type": "string"
        },
        "eval_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "case_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_eval_case",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/cases/{case_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Get an eval run.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "run_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_eval_run",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Fetch an eval-run dataset export handle (status + NDJSON body).",
    "input_schema": {
      "description": "Fetch an async dataset-export handle: status plus the produced NDJSON `body`\nonce the export is `completed`. Org-scoped through `get_dataset`.",
      "properties": {
        "dataset_id": {
          "type": "string"
        },
        "eval_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "run_id",
        "dataset_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_eval_run_dataset",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/dataset/{dataset_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Whether an eval run has an active share link.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "run_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_eval_run_share",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/share",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Get a durable schedule execution.",
    "input_schema": {
      "properties": {
        "execution_id": {
          "description": "Schedule execution's identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "execution_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_execution",
    "output_shape": "unknown",
    "path": "/v1/durable/executions/{execution_id}",
    "positional_arg": "execution_id",
    "read_only": true
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Get a single harness by ID or name.",
    "input_schema": {
      "description": "Get a single harness by ID or name.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_harness",
    "output_shape": "unknown",
    "path": "/v1/harnesses/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "images",
    "cli": null,
    "description": "Get image data by ID.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_image",
    "output_shape": "unknown",
    "path": "/v1/images/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Get a knowledge base by ID.",
    "input_schema": {
      "properties": {
        "kb_id": {
          "description": "Knowledge base's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "kb_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_knowledge_base",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}",
    "positional_arg": "kb_id",
    "read_only": true
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Get a knowledge entry by ID.",
    "input_schema": {
      "properties": {
        "entry_id": {
          "description": "Knowledge base entry's prefixed public identifier.",
          "type": "string"
        },
        "kb_id": {
          "description": "Knowledge base's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "kb_id",
        "entry_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_knowledge_entry",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}/entries/{entry_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "knowledge_indexes",
    "cli": null,
    "description": "Get a knowledge index by ID.",
    "input_schema": {
      "properties": {
        "index_id": {
          "description": "Knowledge index's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "index_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_knowledge_index",
    "output_shape": "unknown",
    "path": "/v1/knowledge-indexes/{index_id}",
    "positional_arg": "index_id",
    "read_only": true
  },
  {
    "category": "agents",
    "cli": null,
    "description": "Get the latest health check run for an agent, with a stale-config flag.",
    "input_schema": {
      "description": "Get the latest health check run for an agent, without triggering a new one.",
      "properties": {
        "agent_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_latest_agent_health_check_run",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/health-checks/latest",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "List plugin catalog entries for a marketplace (with installed flag per entry).",
    "input_schema": {
      "description": "List the synced catalog entries for a marketplace, with `installed` flag.",
      "properties": {
        "id": {
          "description": "Public marketplace ID.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_marketplace_catalog",
    "output_shape": "unknown",
    "path": "/v1/plugin_marketplaces/{id}/plugins",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "mcp_servers",
    "cli": "mcp-servers get",
    "description": "Get a single MCP server by ID.",
    "input_schema": {
      "description": "Get a single MCP server by ID.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_mcp_server",
    "output_shape": "unknown",
    "path": "/v1/mcp-servers/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "memories",
    "cli": null,
    "description": "Get a workspace memory by ID.",
    "input_schema": {
      "properties": {
        "memory_id": {
          "description": "Workspace memory's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "memory_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_memory",
    "output_shape": "unknown",
    "path": "/v1/memories/{memory_id}",
    "positional_arg": "memory_id",
    "read_only": true
  },
  {
    "category": "models",
    "cli": null,
    "description": "Get a specific model with provider information.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_model",
    "output_shape": "unknown",
    "path": "/v1/models/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "observers",
    "cli": null,
    "description": "Get a single observer.",
    "input_schema": {
      "properties": {
        "observer_id": {
          "type": "string"
        }
      },
      "required": [
        "observer_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_observer",
    "output_shape": "unknown",
    "path": "/v1/observers/{observer_id}",
    "positional_arg": "observer_id",
    "read_only": true
  },
  {
    "category": "organizations",
    "cli": null,
    "description": "Get organization details.",
    "input_schema": {
      "properties": {
        "org": {
          "type": "string"
        }
      },
      "required": [
        "org"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_org",
    "output_shape": "unknown",
    "path": "/v1/orgs/{org}",
    "positional_arg": "org",
    "read_only": true
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Get a machine-payment wallet account.",
    "input_schema": {
      "properties": {
        "payment_account_id": {
          "type": "string"
        }
      },
      "required": [
        "payment_account_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_payment_account",
    "output_shape": "unknown",
    "path": "/v1/payments/accounts/{payment_account_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Get a machine-payment spend policy.",
    "input_schema": {
      "properties": {
        "payment_policy_id": {
          "type": "string"
        }
      },
      "required": [
        "payment_policy_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_payment_policy",
    "output_shape": "unknown",
    "path": "/v1/payments/policies/{payment_policy_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Get an installed plugin by ID.",
    "input_schema": {
      "description": "Get an installed plugin by ID.",
      "properties": {
        "id": {
          "description": "Public plugin ID (`plugin_<32-hex>`).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_plugin",
    "output_shape": "unknown",
    "path": "/v1/plugins/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Get a plugin marketplace by ID.",
    "input_schema": {
      "description": "Get a plugin marketplace by ID.",
      "properties": {
        "id": {
          "description": "Public marketplace ID (`plgmkt_<32-hex>`).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_plugin_marketplace",
    "output_shape": "unknown",
    "path": "/v1/plugin_marketplaces/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "providers",
    "cli": null,
    "description": "Get a specific LLM provider.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_provider",
    "output_shape": "unknown",
    "path": "/v1/providers/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Return semantic reporting datasets, dimensions, measures, and filter fields.",
    "input_schema": {
      "default": null
    },
    "method": "GET",
    "name": "get_report_catalog",
    "output_shape": "unknown",
    "path": "/v1/reports/catalog",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Inspect reporting projector lag and failed outbox rows.",
    "input_schema": {
      "default": null
    },
    "method": "GET",
    "name": "get_reporting_diagnostics",
    "output_shape": "unknown",
    "path": "/v1/reports/admin/diagnostics",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Get an org-scoped saved report definition.",
    "input_schema": {
      "properties": {
        "report_id": {
          "description": "Saved report's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "report_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_saved_report",
    "output_shape": "unknown",
    "path": "/v1/reports/saved/{report_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Get a durable schedule.",
    "input_schema": {
      "properties": {
        "schedule_id": {
          "description": "Schedule's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "schedule_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_schedule",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}",
    "positional_arg": "schedule_id",
    "read_only": true
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Get durable schedule statistics.",
    "input_schema": {
      "properties": {
        "schedule_id": {
          "description": "Schedule's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "schedule_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_schedule_stats",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}/stats",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "sessions",
    "cli": "sessions get",
    "description": "Get session details including status, agent, harness, and model.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "sessions",
    "cli": "sessions context",
    "description": "Get the latest estimated context token breakdown for a session, grouped by system prompt, tools, rules, skills, MCP, subagents, and conversation.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_session_context_report",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/context-report",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "session_databases",
    "cli": null,
    "description": "Get metadata for a session SQL database.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "name"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_session_database",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/databases/{name}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "session_databases",
    "cli": null,
    "description": "Inspect the schema of a session SQL database.",
    "input_schema": {
      "properties": {
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": "string"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "name"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_session_database_schema",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/databases/{name}/schema",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "environments",
    "cli": null,
    "description": "Inspect where a session's commands run and what they may touch.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_session_environment",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/environment",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "sessions",
    "cli": "sessions facets",
    "description": "Counts per status, source, and agent plus masthead metrics for the sessions list, over the same filters as list_sessions.",
    "input_schema": {
      "$defs": {
        "SessionFilterArgs": {
          "description": "Filters shared by `ListSessions` and `GetSessionFacets` so the page and the\ncounts that annotate it can never describe different populations (EVE-852).",
          "properties": {
            "agent_id": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/agentId",
                  "description": "Agent's prefixed public identifier."
                }
              ]
            },
            "created_after": {
              "description": "Inclusive lower bound on `created_at` (RFC 3339).",
              "type": [
                "string",
                "null"
              ]
            },
            "created_before": {
              "description": "Exclusive upper bound on `created_at` (RFC 3339).",
              "type": [
                "string",
                "null"
              ]
            },
            "include_archived": {
              "description": "Include archived sessions. Default `false`.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "mine": {
              "description": "Restrict to sessions owned by the calling user.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "order": {
              "description": "`created_at` (default) or `last_activity`. The chat thread list is this\nendpoint with `source=chat`, `mine=true`, `order=last_activity`.",
              "type": [
                "string",
                "null"
              ]
            },
            "search": {
              "description": "Case-insensitive title substring match.",
              "type": [
                "string",
                "null"
              ]
            },
            "source": {
              "description": "Comma-separated sources (`chat`, `api`, `slack`, `ag_ui`, `fcp`,\n`schedule`, `webhook`, `a2a`, `eval`, `subagent`, `unknown`).",
              "type": [
                "string",
                "null"
              ]
            },
            "status": {
              "description": "Comma-separated derived activities (`running`, `paused`, `failed`,\n`completed`, `idle`).",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "agentId": {
          "description": "Prefixed identifier with 'agent' prefix",
          "example": "agent_01933b5a000070008000000000000001",
          "pattern": "^agent_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/SessionFilterArgs"
        }
      ],
      "description": "Facet-rail counts and masthead metrics over the sessions list predicate."
    },
    "method": "GET",
    "name": "get_session_facets",
    "output_shape": "unknown",
    "path": "/v1/sessions/facets",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "session_sandbox",
    "cli": null,
    "description": "Inspect the managed sandbox owned by a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_session_sandbox",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/sandbox",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "sessions",
    "cli": "sessions stats",
    "description": "Get session counts by status.",
    "input_schema": {
      "default": null
    },
    "method": "GET",
    "name": "get_session_stats",
    "output_shape": "unknown",
    "path": "/v1/sessions/stats",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "Get one session task with its recent message thread.",
    "input_schema": {
      "properties": {
        "after_id": {
          "description": "Return only messages newer than this message ID (exclusive cursor).\nWhen omitted, the most recent `limit` messages are returned.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Maximum number of messages to return. Defaults to 50.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "task_id": {
          "description": "Task's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "task_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_session_task",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tasks/{task_id}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "skills",
    "cli": "skills get",
    "description": "Get a single skill by ID.",
    "input_schema": {
      "description": "Get a single skill by ID.",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_skill",
    "output_shape": "unknown",
    "path": "/v1/skills/{id}",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "skills",
    "cli": "skills content",
    "description": "Get full skill content (SKILL.md + files).",
    "input_schema": {
      "description": "Get full skill content (SKILL.md + files).",
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_skill_content",
    "output_shape": "unknown",
    "path": "/v1/skills/{id}/content",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "workspaces",
    "cli": null,
    "description": "Fetch a Workspace by its public ID.",
    "input_schema": {
      "properties": {
        "workspace_id": {
          "description": "Workspace ID (wsp_<32-hex>).",
          "type": "string"
        }
      },
      "required": [
        "workspace_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_workspace",
    "output_shape": "unknown",
    "path": "/v1/workspaces/{workspace_id}",
    "positional_arg": "workspace_id",
    "read_only": true
  },
  {
    "category": "files",
    "cli": null,
    "description": "Get a file or directory at a path in the session filesystem.",
    "input_schema": {
      "properties": {
        "path": {
          "type": "string"
        },
        "recursive": {
          "type": "boolean"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "path"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "get_workspace_file",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/{path}",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "files",
    "cli": null,
    "description": "Search files in the session filesystem.",
    "input_schema": {
      "$defs": {
        "GrepRequest": {
          "description": "Request to search files",
          "properties": {
            "path_pattern": {
              "description": "Optional path glob to filter files (`**/*.rs`, `docs/*.md`).",
              "example": "**/*.rs",
              "type": [
                "string",
                "null"
              ]
            },
            "pattern": {
              "description": "Regex pattern to search for. Standard PCRE-ish (Rust `regex` crate) syntax.",
              "example": "TODO\\(perf\\)",
              "type": "string"
            }
          },
          "required": [
            "pattern"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/GrepRequest"
        },
        {
          "properties": {
            "session_id": {
              "description": "Session's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "session_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "grep_workspace_files",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/_/grep",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "system",
    "cli": "system health",
    "description": "Health check endpoint.",
    "input_schema": {
      "type": "object"
    },
    "method": "GET",
    "name": "health_check",
    "output_shape": "unknown",
    "path": "/health",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents import",
    "description": "Import an agent from a definition.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "BuiltinTool": {
          "description": "Built-in tool configuration\n\nNote: The `kind` field has been removed. Tools are now identified\nsolely by their `name` field, and execution happens via the ToolRegistry\nwhich looks up tools by name.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering (e.g., \"Get Current Time\" for `get_current_time`)",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for registry lookup)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            },
            "policy": {
              "$ref": "#/$defs/ToolPolicy",
              "description": "Tool policy (auto or requires_approval)"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ClientSideTool": {
          "description": "Client-side tool - executed by the client, not the server\nThe server pauses execution and waits for the client to submit results.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for correlation)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ConversationStarter": {
          "description": "A conversation starter shown on a fresh Platform Chat thread.\nSelecting one inserts its text into the composer. `icon` reuses the\nharness icon name set (`HarnessIcon`); unknown names fall back to the\ndefault glyph.",
          "properties": {
            "icon": {
              "description": "Optional icon name from the harness icon set.",
              "example": "zap",
              "type": [
                "string",
                "null"
              ]
            },
            "text": {
              "description": "Prompt text inserted into the composer when selected.",
              "example": "Triage the newest P1",
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "type": "object"
        },
        "DeferrablePolicy": {
          "description": "Controls whether a tool's full schema can be deferred (tool_search).\n\nWhen tool_search is active and a model supports it, tools marked as\n`Automatic` or `Always` will have `defer_loading: true` set, meaning\nonly the name+description are sent upfront and full parameter schemas\nare loaded on-demand by the model.",
          "enum": [
            "never",
            "automatic",
            "always"
          ],
          "type": "string"
        },
        "InitialFile": {
          "description": "Starter file copied into a new session from an agent or harness.",
          "properties": {
            "content": {
              "description": "File content: plain text or base64-encoded binary.",
              "type": "string"
            },
            "encoding": {
              "description": "Content encoding: `text` or `base64`.",
              "type": "string"
            },
            "is_readonly": {
              "description": "Prevent session-side edits or deletes when true.",
              "type": "boolean"
            },
            "path": {
              "description": "Absolute path within the session workspace. `/workspace` prefix is accepted.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "NetworkAccessList": {
          "description": "Network access list controlling which hosts/URLs an agent session can reach.\n\n- `allowed`: if non-empty, only URLs matching these patterns are permitted.\n- `blocked`: URLs matching these patterns are always denied (takes precedence over allowed).\n\nPattern format:\n- `example.com` — exact domain match (any port, any path)\n- `*.example.com` — domain and all subdomains\n- `https://example.com/api/` — exact URL prefix (scheme + host + path)",
          "example": {
            "allowed": [
              "*.example.com",
              "https://api.acme.com/"
            ],
            "blocked": [
              "169.254.169.254"
            ]
          },
          "properties": {
            "allowed": {
              "description": "Allowed host patterns. If non-empty, only matching URLs are permitted.\nAn empty list means \"no restriction from this layer\" (inherit parent).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "blocked": {
              "description": "Blocked host patterns. Always denied, even if matched by `allowed`.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "SideEffectClass": {
          "description": "How many times a tool call may safely be executed given the same inputs.\n\nUsed by the durable Act activity (EVE-530) to decide what to do when a\nprior execution attempt left a `running` claim in `durable_tool_results`:\n\n* `Pure` / `Idempotent` — the running claim is stale; re-execute freely.\n* `AtMostOnce` — never re-execute from a stale running claim; settle it\n  as `interrupted` and surface an uncertain result to the model instead.\n\nWhen unset (`None`), the conservative default is `AtMostOnce`.",
          "enum": [
            "Pure",
            "Idempotent",
            "AtMostOnce"
          ],
          "type": "string"
        },
        "ToolDefinition": {
          "description": "Tool definition in agent configuration",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/BuiltinTool",
                  "description": "Built-in tool - executed by the worker via ToolRegistry"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "builtin"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Built-in tool - executed by the worker via ToolRegistry"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ClientSideTool",
                  "description": "Client-side tool - executed by the client, not the server"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "client_side"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Client-side tool - executed by the client, not the server"
            }
          ]
        },
        "ToolHints": {
          "description": "Semantic hints describing a tool's behavioral properties.\n\nFollows the MCP tool annotations convention (readOnlyHint, destructiveHint,\nidempotentHint, openWorldHint) plus everruns-specific hints. All fields are\noptional booleans — `None` means \"unknown/unspecified\". Consumers should\ntreat `None` as the conservative default (e.g., assume not readonly, assume\nnot idempotent).\n\nThese hints are informational — they do not enforce policy. Use `ToolPolicy`\nfor execution gating (auto vs requires_approval).",
          "properties": {
            "capability_id": {
              "description": "Capability that contributed this tool definition.\n\nReporting uses this attribution only as metadata. It must never contain\ntool arguments, results, prompts, or any other sensitive payload.",
              "type": [
                "string",
                "null"
              ]
            },
            "capability_name": {
              "description": "Human-readable capability name snapshot for reporting.",
              "type": [
                "string",
                "null"
              ]
            },
            "concurrency_class": {
              "description": "Scheduling conflict key. Tool calls within the same act batch that share\na non-empty `concurrency_class` are executed sequentially in arrival\norder; calls in different classes (or with no class) run concurrently.\n\nSet this on tools that mutate shared session state so that, e.g., two\nfile writes or two SQL mutations in one batch do not race. Read-only\ntools should leave this `None` so they always parallelize. See\n`everruns-engine`'s tool scheduler for how the act phase consumes it.",
              "type": [
                "string",
                "null"
              ]
            },
            "cpu_bound": {
              "description": "Tool performs significant CPU-bound or otherwise non-yielding work in\nprocess (e.g. an in-process interpreter). When true, the act scheduler\nruns the call on its own task (`tokio::spawn`) so a long CPU burst does\nnot starve the cooperative polling of I/O-bound tools in the same batch.\n\nDistinct from `long_running`, which describes wall-clock time for\nI/O-bound work (those tools yield at await points and need no offload).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "destructive": {
              "description": "Tool may irreversibly destroy or delete data.\nSubset of non-readonly — a tool can be non-readonly (writes) without\nbeing destructive (e.g., create/update operations).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "idempotent": {
              "description": "Calling the tool repeatedly with the same arguments produces the same\neffect. Safe to retry on transient failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "long_running": {
              "description": "Tool may take significant time to complete (> ~5s typical).\nUseful for clients to show progress indicators and set timeouts.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "metadata": {
              "description": "Host-owned annotations that core does not interpret.\n\nThe typed hints above are the vocabulary core itself reasons about. This\nis the escape hatch for everything a *host* wants to carry alongside a\ntool — risk tiers for an approval UI, presentation hints, an embedder's\nrouting keys — without adding a field to core for each one. Core reads\nnothing here and no driver sends it to a provider; it travels with the\ndefinition so a consumer sees it at the point of decision (e.g. a\n`PreToolUseHook` gating on what the tool declared).\n\nThe schema belongs to whoever writes it. Never put credentials or other\nsensitive payload here: like the rest of the definition, it is persisted\nand surfaced to clients."
            },
            "narration_noun": {
              "description": "Entity noun for operation-based narration (e.g. \"agent\", \"harness\").\nWhen set, the narration system reads the `operation` argument and\nproduces verb-based narration like \"Created agent: Neon Cartographer\"\ninstead of the generic \"Ran Manage Agents\".",
              "type": [
                "string",
                "null"
              ]
            },
            "open_world": {
              "description": "Tool interacts with external entities beyond the local system\n(network calls, third-party APIs, cloud services).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "persist_output": {
              "description": "Tool output should be persisted to session VFS before truncation.\nWhen set, the `tool_output_persistence` capability (EVE-222, EVE-245) writes\nstdout to `/outputs/{tool_call_id}.stdout` and stderr to\n`/outputs/{tool_call_id}.stderr`, injecting `full_output`, `total_lines`,\nand `output_files` into the result.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "readonly": {
              "description": "Tool does not modify any state (read-only queries, lookups).\nWhen true: safe to call speculatively, result can be cached.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "requires_secrets": {
              "description": "Tool requires API keys, credentials, or other secrets to function.\nUseful for UI to show connection prompts and for LLMs to anticipate\nauthentication failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "side_effect_class": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SideEffectClass",
                  "description": "Replay-safety class used by the durable Act activity (EVE-530).\n\nControls what happens when a worker reclaims a stale `running` claim:\n`Pure`/`Idempotent` tools are re-executed; `AtMostOnce` tools are\nsettled as `interrupted` to prevent double side-effects.\n\n`None` is treated conservatively as `AtMostOnce`."
                }
              ]
            },
            "supports_background": {
              "description": "Tool supports detached background execution via `spawn_background`.\nWhen true, the tool may be executed asynchronously outside the current\nforeground tool call and report status back later.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ToolPolicy": {
          "description": "Tool policy determines how tool calls are handled",
          "enum": [
            "auto",
            "requires_approval",
            "client_side"
          ],
          "type": "string"
        }
      },
      "description": "Request to create a new agent",
      "properties": {
        "capabilities": {
          "description": "Capabilities to enable for this agent with per-agent configuration.\nEach capability has a `ref` (capability ID) and optional `config`.",
          "example": [
            {
              "config": {},
              "ref": "current_time"
            },
            {
              "config": {},
              "ref": "web_fetch"
            }
          ],
          "items": {
            "$ref": "#/$defs/AgentCapabilityConfig"
          },
          "type": "array"
        },
        "default_model_id": {
          "description": "The ID of the default LLM model to use for this agent.\nIf not specified, the system default model will be used.",
          "example": "model_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "description": "A human-readable description of what the agent does.",
          "example": "Handles customer inquiries and support tickets",
          "type": [
            "string",
            "null"
          ]
        },
        "display_name": {
          "description": "Human-readable display name shown in UI.\nFalls back to `name` when absent.",
          "example": "Customer Support Agent",
          "type": [
            "string",
            "null"
          ]
        },
        "harness_id": {
          "description": "Harness ID used as this agent's base execution environment. If omitted,\nthe org's built-in `generic` harness is used.",
          "example": "harness_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "harness_name": {
          "description": "Addressable harness name. Alternative to `harness_id`.",
          "example": "generic",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "Client-supplied agent ID (format: agent_{32-hex}).\nIf not provided, one is auto-generated.",
          "example": "agent_01933b5a00007000800000000000001",
          "type": [
            "string",
            "null"
          ]
        },
        "initial_files": {
          "description": "Starter files copied into each new session for this agent.",
          "example": [
            {
              "content": "Always respond in formal English.\n",
              "path": "INSTRUCTIONS.md"
            }
          ],
          "items": {
            "$ref": "#/$defs/InitialFile"
          },
          "type": "array"
        },
        "intro_markdown": {
          "description": "Markdown intro shown as an intro box on a fresh Platform Chat thread.\nImages are allowed. Wins over the harness intro. Hidden once the user\ninputs.",
          "example": "Hey, I'm Ava. Ask me anything about your account.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_iterations": {
          "description": "Maximum number of LLM iterations per turn for this agent.",
          "example": 20,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "mcpServers": {
          "$ref": "#/$defs/BTreeMap",
          "description": "Remote MCP servers scoped to this agent."
        },
        "name": {
          "description": "Name, unique per org. Lowercase alphanumeric and hyphens.\nFormat: lowercase alphanumeric and hyphens (e.g. \"customer-support\").",
          "example": "customer-support",
          "type": "string"
        },
        "network_access": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/NetworkAccessList",
              "description": "Network access list controlling which hosts/URLs this agent's sessions can reach.\nIf set, merged with harness and session layers (allowed: intersect, blocked: union).\nExample shape is defined on `NetworkAccessList`."
            }
          ]
        },
        "parallel_tool_calls": {
          "description": "Request-level parallel tool calling preference (EVE-598). `true` signals\nthe provider that parallel tool calls are wanted; `false` requests at\nmost one tool call per turn and forces serial execution. Omit to use the\nprovider default.",
          "example": true,
          "type": [
            "boolean",
            "null"
          ]
        },
        "short_description": {
          "description": "One-line description in simplified Markdown, shown below the chat title\nonce the intro is hidden. Wins over the harness value.",
          "example": "Answers account questions in seconds.",
          "type": [
            "string",
            "null"
          ]
        },
        "starters": {
          "description": "Conversation starters for a fresh Platform Chat thread. Win over the\nharness starters when non-empty. `icon` reuses the harness icon set.",
          "items": {
            "$ref": "#/$defs/ConversationStarter"
          },
          "type": "array"
        },
        "system_prompt": {
          "description": "The system prompt that defines the agent's behavior and capabilities.\nThis is sent as the first message in every conversation.",
          "example": "You are a helpful customer support agent. Be polite and professional.",
          "type": "string"
        },
        "tags": {
          "description": "Tags for organizing and filtering agents.",
          "example": [
            "support",
            "customer-facing"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tools": {
          "description": "Client-side tools for this agent.\nThese tools are sent to the LLM but executed by the client, not the server.",
          "example": [
            {
              "description": "Open URL in the user's browser",
              "name": "open_url",
              "parameters": {
                "properties": {
                  "url": {
                    "type": "string"
                  }
                },
                "required": [
                  "url"
                ],
                "type": "object"
              },
              "type": "client_side"
            }
          ],
          "items": {
            "$ref": "#/$defs/ToolDefinition"
          },
          "type": "array"
        }
      },
      "required": [
        "name",
        "system_prompt"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "import_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/import",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Import ATIF trajectories as eval cases (upserted by name).",
    "input_schema": {
      "description": "Import ATIF trajectories as eval cases (knowledge/evaluation/atif-adoption.md).\n\n`body` is the raw import payload: NDJSON (one trajectory per line), a JSON\narray of trajectories, a single trajectory object, or `{ \"trajectories\":\n[...] }`. Cases are upserted by name, so re-import is idempotent.",
      "properties": {
        "body": {
          "description": "Raw ATIF payload (NDJSON or JSON).",
          "type": "string"
        },
        "eval_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "body"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "import_atif_trajectories",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/atif_import",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Import externally-executed eval results.",
    "input_schema": {
      "$defs": {
        "ImportCaseStatus": {
          "description": "Verdict for an imported case (trusted as-is; not recomputed).",
          "enum": [
            "passed",
            "failed",
            "errored",
            "timeout",
            "skipped"
          ],
          "type": "string"
        },
        "ImportEvalCaseEntry": {
          "description": "One case result. The case is upserted by `name` (identity-only: everruns\nnever re-executes it).",
          "properties": {
            "description": {
              "type": [
                "string",
                "null"
              ]
            },
            "error_message": {
              "type": [
                "string",
                "null"
              ]
            },
            "input": {
              "description": "Display-only input turns shown in the UI.",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "input_tokens": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "latency_ms": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "metrics": {
              "description": "Open-vocab metrics bag (cost_usd, cache/reasoning tokens, ttft, ...)."
            },
            "name": {
              "type": "string"
            },
            "output_tokens": {
              "format": "int64",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "scores": {
              "description": "Named, attributed scores. Stored opaque; everruns does not re-grade.",
              "items": {
                "$ref": "#/$defs/ImportScore"
              },
              "type": "array"
            },
            "status": {
              "$ref": "#/$defs/ImportCaseStatus"
            },
            "target": {
              "$ref": "#/$defs/ImportEvalTarget",
              "description": "Provider/model labels this result was produced against."
            },
            "transcript": {
              "description": "Normalized transcript (messages, tool calls, events, parts, files)."
            },
            "turns": {
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "name",
            "target",
            "status"
          ],
          "type": "object"
        },
        "ImportEvalGroup": {
          "description": "One eval's worth of results within the run. The eval is upserted by `name`.",
          "properties": {
            "cases": {
              "items": {
                "$ref": "#/$defs/ImportEvalCaseEntry"
              },
              "type": "array"
            },
            "description": {
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "type": "string"
            },
            "tags": {
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "name",
            "cases"
          ],
          "type": "object"
        },
        "ImportEvalRunRequest": {
          "description": "A whole external run group: one external run, one entry per eval. Maps to\none everruns EvalRun per eval, all sharing `source.run_id`.",
          "properties": {
            "evals": {
              "items": {
                "$ref": "#/$defs/ImportEvalGroup"
              },
              "type": "array"
            },
            "source": {
              "$ref": "#/$defs/ImportEvalSource"
            }
          },
          "required": [
            "source",
            "evals"
          ],
          "type": "object"
        },
        "ImportEvalSource": {
          "description": "Attribution for the external system that produced the run.",
          "properties": {
            "metadata": {
              "description": "Optional environment/labels (git commit, host, etc.)."
            },
            "run_id": {
              "description": "Stable external run id: cross-eval group key + idempotency key.",
              "type": "string"
            },
            "system": {
              "description": "External system name, e.g. \"mira\".",
              "type": "string"
            },
            "url": {
              "type": [
                "string",
                "null"
              ]
            },
            "version": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "system",
            "run_id"
          ],
          "type": "object"
        },
        "ImportEvalTarget": {
          "description": "Provider/model labels for an externally-executed result.",
          "properties": {
            "model": {
              "type": "string"
            },
            "params": {},
            "provider": {
              "type": "string"
            }
          },
          "required": [
            "provider",
            "model"
          ],
          "type": "object"
        },
        "ImportScore": {
          "description": "A single named score from an external scorer.",
          "properties": {
            "na": {
              "description": "Scorer was not applicable (excluded from aggregate).",
              "type": "boolean"
            },
            "pass": {
              "type": "boolean"
            },
            "reason": {
              "type": "string"
            },
            "scorer": {
              "type": "string"
            },
            "value": {
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "scorer",
            "value",
            "pass"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/ImportEvalRunRequest"
        }
      ],
      "description": "Import a full external run group (everruns as host/viewer for external eval\nsystems). See knowledge/evaluation/evals.md."
    },
    "method": "POST",
    "name": "import_eval_run",
    "output_shape": "unknown",
    "path": "/v1/evals/import",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Import an Open Knowledge Format (OKF) bundle into a knowledge base.",
    "input_schema": {
      "$defs": {
        "OkfFileInput": {
          "description": "A single inline bundle file, for callers that send files as JSON rather\nthan a tarball.",
          "properties": {
            "content": {
              "description": "Raw markdown file content (frontmatter + body).",
              "type": "string"
            },
            "path": {
              "description": "Bundle-relative path, e.g. `tables/orders.md`.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        }
      },
      "description": "Import an OKF bundle into a knowledge base. See knowledge/runtime-resources/okf-adoption.md.",
      "properties": {
        "bundle_base64": {
          "type": [
            "string",
            "null"
          ]
        },
        "files": {
          "items": {
            "$ref": "#/$defs/OkfFileInput"
          },
          "type": "array"
        },
        "kb_id": {
          "description": "Knowledge base's prefixed public identifier.",
          "type": "string"
        },
        "prune": {
          "type": "boolean"
        }
      },
      "required": [
        "kb_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "import_okf_bundle",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}/okf_import",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Install a plugin from a marketplace catalog entry.",
    "input_schema": {
      "description": "Request body for installing a plugin from a marketplace catalog entry.",
      "properties": {
        "marketplace_id": {
          "description": "Public ID of the marketplace to install from.",
          "example": "plgmkt_01933b5a000070008000000000000001",
          "type": "string"
        },
        "plugin_name": {
          "description": "Name of the plugin entry in the marketplace catalog.",
          "example": "microsoft-docs",
          "type": "string"
        }
      },
      "required": [
        "marketplace_id",
        "plugin_name"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "install_plugin",
    "output_shape": "unknown",
    "path": "/v1/plugins",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions participants leave",
    "description": "Mark a session member participant as having left.",
    "input_schema": {
      "properties": {
        "participant_id": {
          "description": "Participant row to mark as left.",
          "type": "string"
        },
        "session_id": {
          "description": "Session that owns the participant.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "participant_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "leave_session_participant",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/participants/{participant_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": null,
    "description": "List the org's agent check rules: built-in rules with their effective enabled/severity, plus custom rules.",
    "input_schema": {
      "description": "List the org's effective check-rule configuration (built-ins + custom).",
      "type": "object"
    },
    "method": "GET",
    "name": "list_agent_check_rules",
    "output_shape": "unknown",
    "path": "/v1/agents/check-rules",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "List an agent's ingress endpoints.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_agent_endpoints",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agents",
    "cli": null,
    "description": "List recent health check runs for an agent.",
    "input_schema": {
      "description": "List recent health check runs for an agent.",
      "properties": {
        "agent_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_agent_health_check_runs",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/health-checks",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agent_identities",
    "cli": null,
    "description": "List all active agent identities. Use search for name search, include_archived=true to include archived.",
    "input_schema": {
      "description": "List agent identities. Supports search and include_archived.",
      "properties": {
        "include_archived": {
          "type": "boolean"
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_agent_identities",
    "output_shape": "unknown",
    "path": "/v1/agent-identities",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agent_triggers",
    "cli": null,
    "description": "List recent execution outcomes for an agent trigger.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "trigger_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "trigger_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_agent_trigger_runs",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/triggers/{trigger_id}/runs",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agent_triggers",
    "cli": null,
    "description": "List an agent's triggers. include_archived=true also returns archived.",
    "input_schema": {
      "description": "List an agent's triggers.",
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "include_archived": {
          "type": "boolean"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_agent_triggers",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/triggers",
    "positional_arg": "agent_id",
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents versions list",
    "description": "List immutable versions for an agent.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "description": "Agent's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_agent_versions",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/versions",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents list",
    "description": "List all active agents. Use search for name search, include_archived=true to include archived. Supports pagination (limit/offset).",
    "input_schema": {
      "description": "List agents. Supports search, include_archived, pagination.",
      "properties": {
        "include_archived": {
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Zero-based offset into the result set.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_agents",
    "output_shape": "paginated",
    "path": "/v1/agents",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "audit_logs",
    "cli": null,
    "description": "List audit logs for the caller's organization. Supports domain, action, actor, and event-type filters.",
    "input_schema": {
      "description": "List audit logs for the caller's organization. Policy-gated read.",
      "properties": {
        "action": {
          "description": "Filter by action string (e.g. \"management.member.invited\").",
          "type": [
            "string",
            "null"
          ]
        },
        "actor_id": {
          "description": "Filter by actor UUID.",
          "format": "uuid",
          "type": [
            "string",
            "null"
          ]
        },
        "before": {
          "description": "Cursor: return entries created before this timestamp.",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "domain": {
          "description": "Filter by audit domain (\"management\" or \"agent\").",
          "type": [
            "string",
            "null"
          ]
        },
        "event_type": {
          "description": "Filter by event type prefix (e.g. \"auth.login\") — legacy.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Max entries to return (default 50, max 200).",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_audit_logs",
    "output_shape": "unknown",
    "path": "/v1/orgs/{org}/audit-logs",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "List ledger entries for a budget.",
    "input_schema": {
      "properties": {
        "budget_id": {
          "type": "string"
        },
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int64",
          "type": "integer"
        },
        "offset": {
          "description": "Zero-based offset into the result set.",
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "budget_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_budget_ledger",
    "output_shape": "unknown",
    "path": "/v1/budgets/{budget_id}/ledger",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "List budgets. Filter by subject_type and subject_id.",
    "input_schema": {
      "properties": {
        "subject_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "subject_type": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_budgets",
    "output_shape": "unknown",
    "path": "/v1/budgets",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "List available capabilities. Use search for name/description filtering. Retired capabilities are excluded unless include_retired is set. Supports pagination (limit/offset).",
    "input_schema": {
      "description": "List available capabilities with optional search and pagination.",
      "properties": {
        "include_retired": {
          "description": "Include capabilities that have been retired (removed but still\nreferenceable). Off by default so catalogs and discovery surfaces do not\nadvertise a capability that no longer does anything; management UIs turn\nit on to explain existing attachments.",
          "type": "boolean"
        },
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Zero-based offset into the result set.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_capabilities",
    "output_shape": "paginated",
    "path": "/v1/capabilities",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "connections",
    "cli": null,
    "description": "List connection providers available in the current organization. This reports provider availability, not whether the current user is connected.",
    "input_schema": {
      "properties": {
        "search": {
          "description": "Optional case-insensitive provider name/ID filter.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_connection_providers",
    "output_shape": "unknown",
    "path": "/v1/user/connections/providers",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "List persisted declarative capabilities.",
    "input_schema": {
      "properties": {
        "include_archived": {
          "type": "boolean"
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_declarative_capabilities",
    "output_shape": "unknown",
    "path": "/v1/capabilities/declarative",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "environments",
    "cli": null,
    "description": "List the environment targets this deployment can offer.",
    "input_schema": {
      "default": null
    },
    "method": "GET",
    "name": "list_environment_targets",
    "output_shape": "unknown",
    "path": "/v1/environment-targets",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "List eval cases.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_eval_cases",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/cases",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "List eval runs.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_eval_runs",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "evals",
    "cli": null,
    "description": "List evals.",
    "input_schema": {
      "description": "Query parameters for listing evals",
      "properties": {
        "include_archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_evals",
    "output_shape": "unknown",
    "path": "/v1/evals",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "events",
    "cli": null,
    "description": "List events for a session.",
    "input_schema": {
      "$defs": {
        "eventId": {
          "description": "Prefixed identifier with 'event' prefix",
          "example": "event_01933b5a000070008000000000000001",
          "pattern": "^event_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "properties": {
        "after_sequence": {
          "description": "Forward cursor: only return events with sequence > after_sequence.\nMutually exclusive with `before_sequence` and `around`.",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "around": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/eventId",
              "description": "Anchor event id: returns up to `window` events on each side\n(default 50, max 500). Mutually exclusive with `since_id`,\n`after_sequence`, and `before_sequence` — combining returns 400."
            }
          ]
        },
        "before_sequence": {
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "exclude": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exec_id": {
          "description": "Filter by `context.exec_id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "from_ts": {
          "description": "`created_at >= from_ts` (RFC 3339).",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "order_desc": {
          "description": "When true, return newest first; default oldest first.",
          "type": "boolean"
        },
        "q": {
          "description": "Full-text search via Postgres tsvector (substring fallback in-memory).",
          "type": [
            "string",
            "null"
          ]
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "since_id": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/eventId"
            }
          ]
        },
        "tags": {
          "description": "Tag any-match against `events.tags`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "to_ts": {
          "description": "`created_at <= to_ts` (RFC 3339).",
          "format": "date-time",
          "type": [
            "string",
            "null"
          ]
        },
        "tool_name": {
          "description": "Match `data.tool_name` (useful for `tool.*` events).",
          "type": [
            "string",
            "null"
          ]
        },
        "trace_id": {
          "description": "Filter by `context.trace_id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "turn_id": {
          "description": "Filter by `context.turn_id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "types": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "window": {
          "description": "Window size for `around` (events on each side).",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_events",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/events",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "List adoptable guardrail presets (the guardrail gallery), each with its config and trust metadata (check-type composition, stages, data egress).",
    "input_schema": {
      "description": "List the read-only guardrail gallery: ready-made `GuardrailsConfig`\npresets an author can adopt into an agent's `guardrails` capability config.\nPure computation over a static catalogue — nothing persisted, no I/O.",
      "type": "object"
    },
    "method": "GET",
    "name": "list_guardrail_examples",
    "output_shape": "unknown",
    "path": "/v1/capabilities/guardrails/examples",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "List all active harnesses. Use search for name search, include_archived=true to include archived.",
    "input_schema": {
      "description": "List harnesses. Supports search and include_archived.",
      "properties": {
        "include_archived": {
          "type": "boolean"
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_harnesses",
    "output_shape": "array",
    "path": "/v1/harnesses",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "images",
    "cli": null,
    "description": "List uploaded images. Supports pagination (limit/offset).",
    "input_schema": {
      "properties": {
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Zero-based offset into the result set.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_images",
    "output_shape": "unknown",
    "path": "/v1/images",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "List knowledge bases in the current organization.",
    "input_schema": {
      "properties": {
        "include_archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_knowledge_bases",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "List entries inside a knowledge base.",
    "input_schema": {
      "properties": {
        "kb_id": {
          "description": "Knowledge base's prefixed public identifier.",
          "type": "string"
        },
        "kind": {
          "description": "Discriminator selecting the variant of this resource.",
          "type": [
            "string",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kb_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_knowledge_entries",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}/entries",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "knowledge_indexes",
    "cli": null,
    "description": "List documents inside a knowledge index.",
    "input_schema": {
      "properties": {
        "index_id": {
          "description": "Knowledge index's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "index_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_knowledge_index_documents",
    "output_shape": "unknown",
    "path": "/v1/knowledge-indexes/{index_id}/documents",
    "positional_arg": "index_id",
    "read_only": true
  },
  {
    "category": "knowledge_indexes",
    "cli": null,
    "description": "List knowledge indexes in the current organization.",
    "input_schema": {
      "properties": {
        "include_archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_knowledge_indexes",
    "output_shape": "unknown",
    "path": "/v1/knowledge-indexes",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "mcp_servers",
    "cli": "mcp-servers list",
    "description": "List all active MCP servers. Use search for name/description search, include_archived=true to include archived.",
    "input_schema": {
      "description": "List MCP servers. Supports search and include_archived.",
      "properties": {
        "include_archived": {
          "type": "boolean"
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_mcp_servers",
    "output_shape": "unknown",
    "path": "/v1/mcp-servers",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "memories",
    "cli": null,
    "description": "List workspace memories in the current organization.",
    "input_schema": {
      "properties": {
        "include_archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_memories",
    "output_shape": "unknown",
    "path": "/v1/memories",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "messages",
    "cli": null,
    "description": "List materialized messages in a session, optionally limited to the most recent N.",
    "input_schema": {
      "properties": {
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_messages",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/messages",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "models",
    "cli": null,
    "description": "List all models across all providers.",
    "input_schema": {
      "$defs": {
        "ModelSource": {
          "description": "How the model was added to the system",
          "enum": [
            "manual",
            "discovered",
            "predefined"
          ],
          "example": "predefined",
          "type": "string"
        }
      },
      "properties": {
        "favorites_only": {
          "type": "boolean"
        },
        "include_stale": {
          "type": "boolean"
        },
        "source": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ModelSource"
            }
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_models",
    "output_shape": "array",
    "path": "/v1/models",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "notifications",
    "cli": null,
    "description": "List notifications for the current user.",
    "input_schema": {
      "properties": {
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_notifications",
    "output_shape": "unknown",
    "path": "/v1/notifications",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "observers",
    "cli": null,
    "description": "List trace scores produced by an observer.",
    "input_schema": {
      "properties": {
        "limit": {
          "format": "int64",
          "type": "integer"
        },
        "observer_id": {
          "type": "string"
        },
        "offset": {
          "format": "int64",
          "type": "integer"
        },
        "session_id": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "observer_id",
        "limit",
        "offset"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_observer_scores",
    "output_shape": "unknown",
    "path": "/v1/observers/{observer_id}/scores",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "observers",
    "cli": null,
    "description": "List observers.",
    "input_schema": {
      "description": "Query parameters for listing observers.",
      "properties": {
        "include_archived": {
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_observers",
    "output_shape": "unknown",
    "path": "/v1/observers",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "List background tasks across every session in the org.",
    "input_schema": {
      "description": "List background tasks across every session in the caller's org.\n\nOrg-scoped observability query (EVE-583): unlike `ListSessionTasks`, this is\nnot bound to a single session. The org is taken from the authenticated\ncaller (`ctx.org_id()`), never from input, so the result is always scoped to\nthe caller's tenant.",
      "properties": {
        "created_after": {
          "description": "Optional age filter: only tasks created at or after this RFC3339 timestamp.",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "description": "Optional kind filter (subagent, external_agent, background_tool, monitor, ...).",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Max tasks to return, newest first. Defaults to 100, capped at 500.",
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "root_session_id": {
          "description": "Optional delegation-tree filter (EVE-680): only tasks whose owning\nsession's root is this session's prefixed public id. Returns a whole\nsubagent tree's work in one query.",
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "description": "Optional state filter (queued, running, awaiting_input, succeeded, failed, canceled).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_org_tasks",
    "output_shape": "unknown",
    "path": "/v1/tasks",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "organizations",
    "cli": null,
    "description": "List organizations for the current user.",
    "input_schema": {
      "type": "object"
    },
    "method": "GET",
    "name": "list_orgs",
    "output_shape": "unknown",
    "path": "/v1/orgs",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "payments",
    "cli": null,
    "description": "List machine-payment wallet accounts.",
    "input_schema": {
      "properties": {
        "owner_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "owner_type": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_payment_accounts",
    "output_shape": "unknown",
    "path": "/v1/payments/accounts",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "payments",
    "cli": null,
    "description": "List machine-payment attempts.",
    "input_schema": {
      "properties": {
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int64",
          "type": "integer"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "limit"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_payment_attempts",
    "output_shape": "unknown",
    "path": "/v1/payments/attempts",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "payments",
    "cli": null,
    "description": "List machine-payment spend policies.",
    "input_schema": {
      "properties": {
        "payment_account_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "subject_id": {
          "type": [
            "string",
            "null"
          ]
        },
        "subject_type": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_payment_policies",
    "output_shape": "unknown",
    "path": "/v1/payments/policies",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "List plugin marketplaces registered for this organization.",
    "input_schema": {
      "description": "List plugin marketplaces registered for this org.",
      "properties": {
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_plugin_marketplaces",
    "output_shape": "unknown",
    "path": "/v1/plugin_marketplaces",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "List installed plugins for this organization.",
    "input_schema": {
      "description": "List installed plugins for this org.",
      "properties": {
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_plugins",
    "output_shape": "unknown",
    "path": "/v1/plugins",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "models",
    "cli": null,
    "description": "List models for a specific provider.",
    "input_schema": {
      "properties": {
        "provider_id": {
          "description": "LLM provider's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "provider_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_provider_models",
    "output_shape": "unknown",
    "path": "/v1/providers/{provider_id}/models",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "providers",
    "cli": null,
    "description": "List all LLM providers.",
    "input_schema": {
      "type": "object"
    },
    "method": "GET",
    "name": "list_providers",
    "output_shape": "unknown",
    "path": "/v1/providers",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "List org-scoped saved report definitions.",
    "input_schema": {
      "default": null
    },
    "method": "GET",
    "name": "list_saved_reports",
    "output_shape": "unknown",
    "path": "/v1/reports/saved",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "List executions for a durable schedule.",
    "input_schema": {
      "description": "Query parameters for listing executions of a schedule — optional status\nfilter plus offset/limit paging.",
      "properties": {
        "limit": {
          "description": "Pagination limit (default: 20, max: 100)",
          "example": 20,
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Pagination offset",
          "example": 0,
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "schedule_id": {
          "description": "Schedule ID",
          "format": "uuid",
          "type": "string"
        },
        "status": {
          "description": "Filter by execution status",
          "example": "completed",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "schedule_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_schedule_executions",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}/executions",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "List durable scheduled tasks.",
    "input_schema": {
      "description": "Query parameters for `GET /v1/schedules` — optional enabled/target-type\nfilters plus standard offset/limit paging.",
      "properties": {
        "enabled": {
          "description": "Filter by enabled status",
          "example": true,
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "description": "Pagination limit (default: 20, max: 100)",
          "example": 20,
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "offset": {
          "description": "Pagination offset",
          "example": 0,
          "format": "int32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "target_type": {
          "description": "Filter by target type (\"workflow\" or \"activity\")",
          "example": "workflow",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_schedules",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "List all budgets for a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_session_budgets",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/budgets",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "session_databases",
    "cli": null,
    "description": "List all SQL databases created inside a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_session_databases",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/databases",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "sessions",
    "cli": "sessions participants list",
    "description": "List the participant history for a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session whose participant history should be returned.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_session_participants",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/participants",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "session_resources",
    "cli": null,
    "description": "List all resources registered in a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_session_resources",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/resources",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "session_storage",
    "cli": null,
    "description": "List all secrets stored for a session without revealing values.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_session_secrets",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/storage/secrets",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "session_storage",
    "cli": null,
    "description": "List all key-value pairs stored for a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_session_storage",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/storage/keys",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "List background tasks owned by a session.",
    "input_schema": {
      "properties": {
        "kind": {
          "description": "Optional kind filter (subagent, external_agent, background_tool, ...).",
          "type": [
            "string",
            "null"
          ]
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "state": {
          "description": "Optional state filter (queued, running, awaiting_input, succeeded, failed, canceled).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_session_tasks",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tasks",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "sessions",
    "cli": "sessions list",
    "description": "List sessions. Filter by agent_id, source, status, owner (mine), and creation window; search by title; order by created_at or last_activity. Supports pagination (limit/offset).",
    "input_schema": {
      "$defs": {
        "SessionFilterArgs": {
          "description": "Filters shared by `ListSessions` and `GetSessionFacets` so the page and the\ncounts that annotate it can never describe different populations (EVE-852).",
          "properties": {
            "agent_id": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/agentId",
                  "description": "Agent's prefixed public identifier."
                }
              ]
            },
            "created_after": {
              "description": "Inclusive lower bound on `created_at` (RFC 3339).",
              "type": [
                "string",
                "null"
              ]
            },
            "created_before": {
              "description": "Exclusive upper bound on `created_at` (RFC 3339).",
              "type": [
                "string",
                "null"
              ]
            },
            "include_archived": {
              "description": "Include archived sessions. Default `false`.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "mine": {
              "description": "Restrict to sessions owned by the calling user.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "order": {
              "description": "`created_at` (default) or `last_activity`. The chat thread list is this\nendpoint with `source=chat`, `mine=true`, `order=last_activity`.",
              "type": [
                "string",
                "null"
              ]
            },
            "search": {
              "description": "Case-insensitive title substring match.",
              "type": [
                "string",
                "null"
              ]
            },
            "source": {
              "description": "Comma-separated sources (`chat`, `api`, `slack`, `ag_ui`, `fcp`,\n`schedule`, `webhook`, `a2a`, `eval`, `subagent`, `unknown`).",
              "type": [
                "string",
                "null"
              ]
            },
            "status": {
              "description": "Comma-separated derived activities (`running`, `paused`, `failed`,\n`completed`, `idle`).",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "agentId": {
          "description": "Prefixed identifier with 'agent' prefix",
          "example": "agent_01933b5a000070008000000000000001",
          "pattern": "^agent_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/SessionFilterArgs"
        },
        {
          "properties": {
            "limit": {
              "description": "Maximum number of items returned in this page.",
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "offset": {
              "description": "Zero-based offset into the result set.",
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "type": "object"
        }
      ]
    },
    "method": "GET",
    "name": "list_sessions",
    "output_shape": "unknown",
    "path": "/v1/sessions",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "skills",
    "cli": "skills list",
    "description": "List all active skills. Use search for name search, include_archived=true to include archived.",
    "input_schema": {
      "description": "List skills. Supports search and include_archived.",
      "properties": {
        "include_archived": {
          "type": "boolean"
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_skills",
    "output_shape": "unknown",
    "path": "/v1/skills",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "skills",
    "cli": "skills usage",
    "description": "Count agents and harnesses referencing each skill capability.",
    "input_schema": {
      "description": "List skill usage counts: how many active agents and harnesses reference each\nskill via its `skill:{uuid}` capability id. Returned map is keyed by the\npublic SkillId string (e.g. `skill_<32hex>`). Skills with zero references\nare omitted; the UI defaults missing entries to zero.",
      "type": "object"
    },
    "method": "GET",
    "name": "list_skills_usage",
    "output_shape": "unknown",
    "path": "/v1/skills/usage",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "List per-task push-notification configs.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "task_id": {
          "description": "Task's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "task_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_task_push_configs",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tasks/{task_id}/push-configs",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "connections",
    "cli": null,
    "description": "List sanitized connection state for the current user. Returns provider identity and connection metadata, never credentials or tokens.",
    "input_schema": {
      "properties": {
        "provider": {
          "description": "Optional provider ID filter.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_user_connections",
    "output_shape": "unknown",
    "path": "/v1/user/connections",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "users",
    "cli": null,
    "description": "List users in the current organization. Supports search filtering.",
    "input_schema": {
      "properties": {
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_users",
    "output_shape": "unknown",
    "path": "/v1/users",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "files",
    "cli": null,
    "description": "Get the root directory listing of session files.",
    "input_schema": {
      "properties": {
        "recursive": {
          "type": "boolean"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "list_workspace_files",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "workspaces",
    "cli": null,
    "description": "List Workspaces in the current organization.",
    "input_schema": {
      "properties": {
        "include_archived": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "search": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "GET",
    "name": "list_workspaces",
    "output_shape": "unknown",
    "path": "/v1/workspaces",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "session_sandbox",
    "cli": null,
    "description": "Pause, resume, or delete the managed sandbox for a session.",
    "input_schema": {
      "$defs": {
        "SessionSandboxAction": {
          "description": "Operator action to take against a session's managed sandbox. `Pause`\nsuspends the instance, `Resume` restarts it, `Delete` releases the\nlease.",
          "enum": [
            "pause",
            "resume",
            "delete"
          ],
          "type": "string"
        }
      },
      "properties": {
        "action": {
          "$ref": "#/$defs/SessionSandboxAction"
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "action"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "manage_session_sandbox",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/sandbox",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "notifications",
    "cli": null,
    "description": "Mark a notification as viewed.",
    "input_schema": {
      "properties": {
        "notification_id": {
          "type": "string"
        }
      },
      "required": [
        "notification_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "mark_notification_viewed",
    "output_shape": "unknown",
    "path": "/v1/notifications/{notification_id}/view",
    "positional_arg": "notification_id",
    "read_only": false
  },
  {
    "category": "files",
    "cli": null,
    "description": "Move or rename a file in the session filesystem.",
    "input_schema": {
      "$defs": {
        "MoveFileRequest": {
          "description": "Request to move/rename a file",
          "properties": {
            "dst_path": {
              "description": "Destination path (relative to the workspace filesystem root).",
              "example": "docs/migration-plan.md",
              "type": "string"
            },
            "src_path": {
              "description": "Source path (relative to the workspace filesystem root).",
              "example": "drafts/migration-plan.md",
              "type": "string"
            }
          },
          "required": [
            "src_path",
            "dst_path"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/MoveFileRequest"
        },
        {
          "properties": {
            "session_id": {
              "description": "Session's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "session_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "move_workspace_file",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/_/move",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Update an installed plugin's status (active/disabled).",
    "input_schema": {
      "$defs": {
        "UpdateInstalledPluginRequest": {
          "description": "Request body for updating an installed plugin (status only; use POST .../update for recompile).",
          "properties": {
            "status": {
              "description": "New lifecycle status: `active` or `disabled`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateInstalledPluginRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Public plugin ID.",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Update an installed plugin's status (enable/disable)."
    },
    "method": "PATCH",
    "name": "patch_installed_plugin",
    "output_shape": "unknown",
    "path": "/v1/plugins/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Pause a durable schedule.",
    "input_schema": {
      "properties": {
        "schedule_id": {
          "description": "Schedule's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "schedule_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "pause_schedule",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}/pause",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions pin",
    "description": "Pin a session for the current user.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "PUT",
    "name": "pin_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/pin",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "session_tasks",
    "cli": null,
    "description": "Send an inbound message to a session task.",
    "input_schema": {
      "$defs": {
        "TaskMessagePart": {
          "description": "One content part of a task message.",
          "oneOf": [
            {
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "text"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "properties": {
                "data": {
                  "type": "object"
                },
                "type": {
                  "enum": [
                    "data"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "data",
                "type"
              ],
              "type": "object"
            }
          ]
        }
      },
      "properties": {
        "content": {
          "description": "Structured message parts (alternative to `text`).",
          "items": {
            "$ref": "#/$defs/TaskMessagePart"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "in_reply_to": {
          "description": "Input request ID this message answers, when applicable.",
          "type": [
            "string",
            "null"
          ]
        },
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "task_id": {
          "description": "Task's prefixed public identifier.",
          "type": "string"
        },
        "text": {
          "description": "Plain-text message (alternative to `content`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "session_id",
        "task_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "post_session_task_message",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tasks/{task_id}/messages",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents preview",
    "description": "Preview the final agent shape with capabilities applied.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "BuiltinTool": {
          "description": "Built-in tool configuration\n\nNote: The `kind` field has been removed. Tools are now identified\nsolely by their `name` field, and execution happens via the ToolRegistry\nwhich looks up tools by name.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering (e.g., \"Get Current Time\" for `get_current_time`)",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for registry lookup)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            },
            "policy": {
              "$ref": "#/$defs/ToolPolicy",
              "description": "Tool policy (auto or requires_approval)"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ClientSideTool": {
          "description": "Client-side tool - executed by the client, not the server\nThe server pauses execution and waits for the client to submit results.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for correlation)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "DeferrablePolicy": {
          "description": "Controls whether a tool's full schema can be deferred (tool_search).\n\nWhen tool_search is active and a model supports it, tools marked as\n`Automatic` or `Always` will have `defer_loading: true` set, meaning\nonly the name+description are sent upfront and full parameter schemas\nare loaded on-demand by the model.",
          "enum": [
            "never",
            "automatic",
            "always"
          ],
          "type": "string"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "SideEffectClass": {
          "description": "How many times a tool call may safely be executed given the same inputs.\n\nUsed by the durable Act activity (EVE-530) to decide what to do when a\nprior execution attempt left a `running` claim in `durable_tool_results`:\n\n* `Pure` / `Idempotent` — the running claim is stale; re-execute freely.\n* `AtMostOnce` — never re-execute from a stale running claim; settle it\n  as `interrupted` and surface an uncertain result to the model instead.\n\nWhen unset (`None`), the conservative default is `AtMostOnce`.",
          "enum": [
            "Pure",
            "Idempotent",
            "AtMostOnce"
          ],
          "type": "string"
        },
        "ToolDefinition": {
          "description": "Tool definition in agent configuration",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/BuiltinTool",
                  "description": "Built-in tool - executed by the worker via ToolRegistry"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "builtin"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Built-in tool - executed by the worker via ToolRegistry"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ClientSideTool",
                  "description": "Client-side tool - executed by the client, not the server"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "client_side"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Client-side tool - executed by the client, not the server"
            }
          ]
        },
        "ToolHints": {
          "description": "Semantic hints describing a tool's behavioral properties.\n\nFollows the MCP tool annotations convention (readOnlyHint, destructiveHint,\nidempotentHint, openWorldHint) plus everruns-specific hints. All fields are\noptional booleans — `None` means \"unknown/unspecified\". Consumers should\ntreat `None` as the conservative default (e.g., assume not readonly, assume\nnot idempotent).\n\nThese hints are informational — they do not enforce policy. Use `ToolPolicy`\nfor execution gating (auto vs requires_approval).",
          "properties": {
            "capability_id": {
              "description": "Capability that contributed this tool definition.\n\nReporting uses this attribution only as metadata. It must never contain\ntool arguments, results, prompts, or any other sensitive payload.",
              "type": [
                "string",
                "null"
              ]
            },
            "capability_name": {
              "description": "Human-readable capability name snapshot for reporting.",
              "type": [
                "string",
                "null"
              ]
            },
            "concurrency_class": {
              "description": "Scheduling conflict key. Tool calls within the same act batch that share\na non-empty `concurrency_class` are executed sequentially in arrival\norder; calls in different classes (or with no class) run concurrently.\n\nSet this on tools that mutate shared session state so that, e.g., two\nfile writes or two SQL mutations in one batch do not race. Read-only\ntools should leave this `None` so they always parallelize. See\n`everruns-engine`'s tool scheduler for how the act phase consumes it.",
              "type": [
                "string",
                "null"
              ]
            },
            "cpu_bound": {
              "description": "Tool performs significant CPU-bound or otherwise non-yielding work in\nprocess (e.g. an in-process interpreter). When true, the act scheduler\nruns the call on its own task (`tokio::spawn`) so a long CPU burst does\nnot starve the cooperative polling of I/O-bound tools in the same batch.\n\nDistinct from `long_running`, which describes wall-clock time for\nI/O-bound work (those tools yield at await points and need no offload).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "destructive": {
              "description": "Tool may irreversibly destroy or delete data.\nSubset of non-readonly — a tool can be non-readonly (writes) without\nbeing destructive (e.g., create/update operations).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "idempotent": {
              "description": "Calling the tool repeatedly with the same arguments produces the same\neffect. Safe to retry on transient failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "long_running": {
              "description": "Tool may take significant time to complete (> ~5s typical).\nUseful for clients to show progress indicators and set timeouts.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "metadata": {
              "description": "Host-owned annotations that core does not interpret.\n\nThe typed hints above are the vocabulary core itself reasons about. This\nis the escape hatch for everything a *host* wants to carry alongside a\ntool — risk tiers for an approval UI, presentation hints, an embedder's\nrouting keys — without adding a field to core for each one. Core reads\nnothing here and no driver sends it to a provider; it travels with the\ndefinition so a consumer sees it at the point of decision (e.g. a\n`PreToolUseHook` gating on what the tool declared).\n\nThe schema belongs to whoever writes it. Never put credentials or other\nsensitive payload here: like the rest of the definition, it is persisted\nand surfaced to clients."
            },
            "narration_noun": {
              "description": "Entity noun for operation-based narration (e.g. \"agent\", \"harness\").\nWhen set, the narration system reads the `operation` argument and\nproduces verb-based narration like \"Created agent: Neon Cartographer\"\ninstead of the generic \"Ran Manage Agents\".",
              "type": [
                "string",
                "null"
              ]
            },
            "open_world": {
              "description": "Tool interacts with external entities beyond the local system\n(network calls, third-party APIs, cloud services).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "persist_output": {
              "description": "Tool output should be persisted to session VFS before truncation.\nWhen set, the `tool_output_persistence` capability (EVE-222, EVE-245) writes\nstdout to `/outputs/{tool_call_id}.stdout` and stderr to\n`/outputs/{tool_call_id}.stderr`, injecting `full_output`, `total_lines`,\nand `output_files` into the result.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "readonly": {
              "description": "Tool does not modify any state (read-only queries, lookups).\nWhen true: safe to call speculatively, result can be cached.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "requires_secrets": {
              "description": "Tool requires API keys, credentials, or other secrets to function.\nUseful for UI to show connection prompts and for LLMs to anticipate\nauthentication failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "side_effect_class": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SideEffectClass",
                  "description": "Replay-safety class used by the durable Act activity (EVE-530).\n\nControls what happens when a worker reclaims a stale `running` claim:\n`Pure`/`Idempotent` tools are re-executed; `AtMostOnce` tools are\nsettled as `interrupted` to prevent double side-effects.\n\n`None` is treated conservatively as `AtMostOnce`."
                }
              ]
            },
            "supports_background": {
              "description": "Tool supports detached background execution via `spawn_background`.\nWhen true, the tool may be executed asynchronously outside the current\nforeground tool call and report status back later.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ToolPolicy": {
          "description": "Tool policy determines how tool calls are handled",
          "enum": [
            "auto",
            "requires_approval",
            "client_side"
          ],
          "type": "string"
        }
      },
      "description": "Preview the final agent shape with capabilities applied.",
      "properties": {
        "capabilities": {
          "items": {
            "$ref": "#/$defs/AgentCapabilityConfig"
          },
          "type": "array"
        },
        "mcp_servers": {
          "$ref": "#/$defs/BTreeMap"
        },
        "system_prompt": {
          "type": [
            "string",
            "null"
          ]
        },
        "tools": {
          "items": {
            "$ref": "#/$defs/ToolDefinition"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "method": "POST",
    "name": "preview_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/preview",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Preview the final harness shape with capabilities applied.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "harnessId": {
          "description": "Prefixed identifier with 'harness' prefix",
          "example": "harness_01933b5a000070008000000000000001",
          "pattern": "^harness_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "description": "Preview the final harness shape with capabilities applied.",
      "properties": {
        "capabilities": {
          "items": {
            "$ref": "#/$defs/AgentCapabilityConfig"
          },
          "type": "array"
        },
        "mcp_servers": {
          "$ref": "#/$defs/BTreeMap"
        },
        "parent_harness_id": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/harnessId"
            }
          ]
        },
        "system_prompt": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "method": "POST",
    "name": "preview_harness",
    "output_shape": "unknown",
    "path": "/v1/harnesses/preview",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "Publish an agent ingress endpoint.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "endpoint_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "endpoint_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "publish_agent_endpoint",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints/{endpoint_id}/publish",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "organizations",
    "cli": "orgs resolve",
    "description": "Resolve the owning organization for a prefixed entity id (agent, session, harness, app, skill, mcp server, identity, eval). Requires an authenticated user; returns NotFound when the caller is not a member of the owning org or the id does not resolve.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "resolve_org",
    "output_shape": "unknown",
    "path": "/v1/resolve-org",
    "positional_arg": "id",
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents exposures resume",
    "description": "Let an agent's live endpoints accept traffic again.",
    "input_schema": {
      "description": "Clear the agent-level exposure suspend, restoring the previously live set.",
      "properties": {
        "agent_id": {
          "description": "Agent's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "resume_agent_exposures",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/exposures/resume",
    "positional_arg": "agent_id",
    "read_only": false
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Resume a durable schedule.",
    "input_schema": {
      "properties": {
        "schedule_id": {
          "description": "Schedule's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "schedule_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "resume_schedule",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}/resume",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Resume all paused session budgets for a session.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "resume_session_budgets",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/resume",
    "positional_arg": "session_id",
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Revoke all share links for an eval run.",
    "input_schema": {
      "properties": {
        "eval_id": {
          "type": "string"
        },
        "run_id": {
          "type": "string"
        }
      },
      "required": [
        "eval_id",
        "run_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "revoke_eval_run_share",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/share",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents versions rollback",
    "description": "Copy an immutable version back into the editable agent draft.",
    "input_schema": {
      "$defs": {
        "RollbackAgentVersionRequest": {
          "description": "Request body for the `rollback_agent_version` operation.",
          "properties": {
            "save_version": {
              "description": "When true, snapshot the current agent state as a new version before rolling back.\nUse this to preserve the in-flight work alongside the recovery point.",
              "example": true,
              "type": "boolean"
            },
            "summary": {
              "description": "Free-text summary attached to the rollback. Shown in the version timeline.",
              "example": "Reverting refund-window change — false positives in production.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "agentverId": {
          "description": "Prefixed identifier with 'agentver' prefix",
          "example": "agentver_01933b5a000070008000000000000001",
          "pattern": "^agentver_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/RollbackAgentVersionRequest"
        },
        {
          "properties": {
            "agent_id": {
              "description": "Agent's prefixed public identifier.",
              "type": "string"
            },
            "version_id": {
              "$ref": "#/$defs/agentverId",
              "description": "Agent version's prefixed public identifier."
            }
          },
          "required": [
            "agent_id",
            "version_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "rollback_agent_version",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/versions/{version_id}/rollback",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Run an org-scoped semantic reporting query.",
    "input_schema": {
      "$defs": {
        "ReportFilter": {
          "description": "One predicate filter applied to the dataset before aggregation.\nCombined with other filters via logical AND.",
          "properties": {
            "field": {
              "description": "Field to filter on. Must be a filter field exposed by the dataset (see `filter_fields` in the catalog).",
              "example": "status",
              "type": "string"
            },
            "op": {
              "$ref": "#/$defs/ReportFilterOp",
              "description": "Comparison operator. Determines the expected shape of `value`\n(scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`, array for `in`)."
            },
            "value": {
              "description": "Comparison value. Type depends on `op`: a scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`,\nan array for `in`. Example for `op = in`: `[\"completed\", \"failed\"]`."
            }
          },
          "required": [
            "field",
            "op",
            "value"
          ],
          "type": "object"
        },
        "ReportFilterOp": {
          "description": "Comparison operator used in a `ReportFilter`. The `In` variant takes a\nJSON array as its value; all others take a scalar.",
          "enum": [
            "eq",
            "neq",
            "in",
            "gt",
            "gte",
            "lt",
            "lte"
          ],
          "type": "string"
        },
        "ReportOrderBy": {
          "description": "One sort clause applied to the aggregated result. Either `dimension`\nOR `measure` is set (mutually exclusive), never both.",
          "properties": {
            "dimension": {
              "description": "Dimension name to sort by. Mutually exclusive with `measure`.",
              "example": "org_id",
              "type": [
                "string",
                "null"
              ]
            },
            "direction": {
              "$ref": "#/$defs/ReportOrderDirection",
              "description": "Sort direction (`asc` or `desc`). Defaults to `asc`."
            },
            "measure": {
              "description": "Measure name to sort by. Mutually exclusive with `dimension`.",
              "example": "session_count",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ReportOrderDirection": {
          "description": "Sort direction for a `ReportOrderBy` clause.",
          "enum": [
            "asc",
            "desc"
          ],
          "type": "string"
        },
        "ReportQuery": {
          "description": "Semantic query a caller submits to the reporting layer. The backend\ncompiles this to its native query language, scopes it to the calling\norg, and returns a `ReportResult`.",
          "properties": {
            "dataset": {
              "description": "Dataset name to query (see `GET /v1/reports/catalog` for the list of available datasets).",
              "example": "sessions",
              "type": "string"
            },
            "dimensions": {
              "description": "Columns to group by. Empty list returns one aggregate row.",
              "example": [
                "status"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "filters": {
              "description": "Predicate filters applied before aggregation.",
              "items": {
                "$ref": "#/$defs/ReportFilter"
              },
              "type": "array"
            },
            "limit": {
              "description": "Maximum number of rows to return (defaults to 100).",
              "example": 100,
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "measures": {
              "description": "Aggregations to compute (count, sum, avg, etc.). Empty list returns row counts only.",
              "example": [
                "session_count",
                "avg_duration_ms"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "order_by": {
              "description": "Sort spec applied after aggregation. Empty list yields unspecified order.",
              "items": {
                "$ref": "#/$defs/ReportOrderBy"
              },
              "type": "array"
            },
            "time_range": {
              "$ref": "#/$defs/ReportTimeRange",
              "description": "Time window for the query. The dataset selects which timestamp column the range applies to."
            }
          },
          "required": [
            "dataset",
            "time_range"
          ],
          "type": "object"
        },
        "ReportTimeRange": {
          "description": "Half-open time window applied to the dataset's primary timestamp column\nduring a report query. `from` is inclusive, `to` is exclusive.",
          "properties": {
            "from": {
              "description": "Start of the window (RFC 3339, inclusive).",
              "example": "2026-04-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            },
            "to": {
              "description": "End of the window (RFC 3339, exclusive).",
              "example": "2026-05-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            }
          },
          "required": [
            "from",
            "to"
          ],
          "type": "object"
        }
      },
      "$ref": "#/$defs/ReportQuery"
    },
    "method": "POST",
    "name": "run_report_query",
    "output_shape": "unknown",
    "path": "/v1/reports/query",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Claim and process pending reporting outbox rows.",
    "input_schema": {
      "properties": {
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "int64",
          "type": "integer"
        }
      },
      "type": "object"
    },
    "method": "POST",
    "name": "run_reporting_projector",
    "output_shape": "unknown",
    "path": "/v1/reports/projector/run",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Run an org-scoped saved report definition.",
    "input_schema": {
      "properties": {
        "report_id": {
          "description": "Saved report's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "report_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "run_saved_report",
    "output_shape": "unknown",
    "path": "/v1/reports/saved/{report_id}/run",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "agents",
    "cli": "agents versions set-default",
    "description": "Set an agent's default immutable version.",
    "input_schema": {
      "$defs": {
        "SetDefaultAgentVersionRequest": {
          "description": "Request body for the `set_default_agent_version` operation.",
          "properties": {
            "version_id": {
              "description": "Agent version's prefixed public identifier.",
              "example": "agentver_01933b5a00007000800000000000001",
              "type": "string"
            }
          },
          "required": [
            "version_id"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/SetDefaultAgentVersionRequest"
        },
        {
          "properties": {
            "agent_id": {
              "description": "Agent's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "agent_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "set_default_agent_version",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/versions/default",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "files",
    "cli": null,
    "description": "Get file metadata in the session filesystem.",
    "input_schema": {
      "$defs": {
        "StatRequest": {
          "description": "Request to get file stat",
          "properties": {
            "path": {
              "description": "Path to the file or directory (relative to the workspace filesystem root).",
              "example": "docs/migration-plan.md",
              "type": "string"
            }
          },
          "required": [
            "path"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/StatRequest"
        },
        {
          "properties": {
            "session_id": {
              "description": "Session's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "session_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "POST",
    "name": "stat_workspace_file",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/_/stat",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "events",
    "cli": null,
    "description": "Stream events via SSE. Not supported in bash mode.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "stream_sse",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/sse",
    "positional_arg": "session_id",
    "read_only": true
  },
  {
    "category": "tool_results",
    "cli": null,
    "description": "Submit client-side tool results back to a waiting session.",
    "input_schema": {
      "$defs": {
        "ClientToolResult": {
          "description": "A single tool result from the client",
          "properties": {
            "error": {
              "description": "Error message if the tool failed",
              "example": "Refund failed: order is outside refund window",
              "type": [
                "string",
                "null"
              ]
            },
            "result": {
              "description": "Result value (any JSON — object, array, string, number, etc.). Null if the tool failed.\nExample: `{\"url\": \"https://example.com/orders/42\"}`."
            },
            "tool_call_id": {
              "description": "Tool call ID (correlates with the tool call from tool.call_requested event)",
              "example": "toolu_01933b5a00007000800000000000001",
              "type": "string"
            }
          },
          "required": [
            "tool_call_id"
          ],
          "type": "object"
        }
      },
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        },
        "tool_results": {
          "items": {
            "$ref": "#/$defs/ClientToolResult"
          },
          "type": "array"
        }
      },
      "required": [
        "session_id",
        "tool_results"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "submit_tool_results",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/tool-results",
    "positional_arg": "session_id",
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents exposures suspend",
    "description": "Stop every endpoint on an agent from accepting traffic.",
    "input_schema": {
      "description": "Take every endpoint on an agent off the internet in one action (EVE-1007).\n\nThis is the incident control, and is deliberately separate from archiving and\nfrom per-endpoint publish: it leaves every endpoint's own `status` untouched,\nso resuming restores exactly the set that was live before — which is what\nmakes it safe to reach for under pressure.",
      "properties": {
        "agent_id": {
          "description": "Agent's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "suspend_agent_exposures",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/exposures/suspend",
    "positional_arg": "agent_id",
    "read_only": false
  },
  {
    "category": "knowledge_indexes",
    "cli": null,
    "description": "Enqueue a manual sync of a knowledge index.",
    "input_schema": {
      "properties": {
        "index_id": {
          "description": "Knowledge index's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "index_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "sync_knowledge_index",
    "output_shape": "unknown",
    "path": "/v1/knowledge-indexes/{index_id}/sync",
    "positional_arg": "index_id",
    "read_only": false
  },
  {
    "category": "memories",
    "cli": null,
    "description": "Queue an immediate sync for a source-backed workspace memory.",
    "input_schema": {
      "properties": {
        "memory_id": {
          "description": "Workspace memory's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "memory_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "sync_memory_now",
    "output_shape": "unknown",
    "path": "/v1/memories/{memory_id}/sync",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Re-sync the plugin marketplace catalog from its source.",
    "input_schema": {
      "description": "Re-sync a plugin marketplace catalog from its source.",
      "properties": {
        "id": {
          "description": "Public marketplace ID.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "sync_plugin_marketplace",
    "output_shape": "unknown",
    "path": "/v1/plugin_marketplaces/{id}/sync",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "providers",
    "cli": null,
    "description": "Discover and sync models from a provider.",
    "input_schema": {
      "properties": {
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "sync_provider_models",
    "output_shape": "unknown",
    "path": "/v1/providers/{id}/sync-models",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "test",
    "cli": null,
    "description": "Exercise command transport error handling.",
    "input_schema": {
      "properties": {
        "kind": {
          "type": "string"
        }
      },
      "required": [
        "kind"
      ],
      "type": "object"
    },
    "method": "GET",
    "name": "test_transport_conflict",
    "output_shape": "unknown",
    "path": "/test/transport-conflict",
    "positional_arg": null,
    "read_only": true
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Add credits to a budget. Reactivates exhausted or paused budgets if balance becomes positive.",
    "input_schema": {
      "properties": {
        "amount": {
          "format": "double",
          "type": "number"
        },
        "budget_id": {
          "type": "string"
        },
        "description": {
          "description": "Human-readable description. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "budget_id",
        "amount"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "top_up_budget",
    "output_shape": "unknown",
    "path": "/v1/budgets/{budget_id}/top-up",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "Run an agent schedule endpoint now.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "endpoint_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "endpoint_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "trigger_agent_endpoint",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints/{endpoint_id}/trigger",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": null,
    "description": "Run a behavioral health check (generated smoke tests) against an agent.",
    "input_schema": {
      "description": "Trigger a behavioral health check for an agent.",
      "properties": {
        "agent_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "trigger_agent_health_check",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/health-checks",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_triggers",
    "cli": null,
    "description": "Manually fire an agent trigger once.",
    "input_schema": {
      "description": "Manually fire a trigger once (does not expose the durable schedule id).",
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "trigger_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "trigger_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "trigger_agent_trigger",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/triggers/{trigger_id}/trigger",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Manually trigger a durable schedule.",
    "input_schema": {
      "properties": {
        "schedule_id": {
          "description": "Schedule's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        }
      },
      "required": [
        "schedule_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "trigger_schedule",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}/trigger",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions unarchive",
    "description": "Restore an archived session to default lists.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "unarchive_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/archive",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Uninstall a plugin. Capability ref becomes dangling for assigned agents.",
    "input_schema": {
      "description": "Uninstall a plugin. Agents/harnesses referencing `plugin:{install_id}` will\nsurface a dangling-ref error (same behaviour as deleted declarative caps).",
      "properties": {
        "id": {
          "description": "Public plugin ID.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "uninstall_plugin",
    "output_shape": "unknown",
    "path": "/v1/plugins/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions unpin",
    "description": "Unpin a session for the current user.",
    "input_schema": {
      "properties": {
        "session_id": {
          "description": "Session's prefixed public identifier.",
          "type": "string"
        }
      },
      "required": [
        "session_id"
      ],
      "type": "object"
    },
    "method": "DELETE",
    "name": "unpin_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/pin",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "Unpublish an agent ingress endpoint.",
    "input_schema": {
      "properties": {
        "agent_id": {
          "type": "string"
        },
        "endpoint_id": {
          "type": "string"
        }
      },
      "required": [
        "agent_id",
        "endpoint_id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "unpublish_agent_endpoint",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints/{endpoint_id}/unpublish",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents update",
    "description": "Update an agent. Only provided fields are changed.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "AgentStatus": {
          "description": "Agent lifecycle status.\n- `active`: Agent is available for use\n- `archived`: Agent is hidden from listings and cannot be modified or assigned\n- `deleted`: Agent is a tombstone kept only for historical references",
          "enum": [
            "active",
            "archived",
            "deleted"
          ],
          "example": "active",
          "type": "string"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "BuiltinTool": {
          "description": "Built-in tool configuration\n\nNote: The `kind` field has been removed. Tools are now identified\nsolely by their `name` field, and execution happens via the ToolRegistry\nwhich looks up tools by name.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering (e.g., \"Get Current Time\" for `get_current_time`)",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for registry lookup)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            },
            "policy": {
              "$ref": "#/$defs/ToolPolicy",
              "description": "Tool policy (auto or requires_approval)"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ClientSideTool": {
          "description": "Client-side tool - executed by the client, not the server\nThe server pauses execution and waits for the client to submit results.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for correlation)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ConversationStarter": {
          "description": "A conversation starter shown on a fresh Platform Chat thread.\nSelecting one inserts its text into the composer. `icon` reuses the\nharness icon name set (`HarnessIcon`); unknown names fall back to the\ndefault glyph.",
          "properties": {
            "icon": {
              "description": "Optional icon name from the harness icon set.",
              "example": "zap",
              "type": [
                "string",
                "null"
              ]
            },
            "text": {
              "description": "Prompt text inserted into the composer when selected.",
              "example": "Triage the newest P1",
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "type": "object"
        },
        "DeferrablePolicy": {
          "description": "Controls whether a tool's full schema can be deferred (tool_search).\n\nWhen tool_search is active and a model supports it, tools marked as\n`Automatic` or `Always` will have `defer_loading: true` set, meaning\nonly the name+description are sent upfront and full parameter schemas\nare loaded on-demand by the model.",
          "enum": [
            "never",
            "automatic",
            "always"
          ],
          "type": "string"
        },
        "InitialFile": {
          "description": "Starter file copied into a new session from an agent or harness.",
          "properties": {
            "content": {
              "description": "File content: plain text or base64-encoded binary.",
              "type": "string"
            },
            "encoding": {
              "description": "Content encoding: `text` or `base64`.",
              "type": "string"
            },
            "is_readonly": {
              "description": "Prevent session-side edits or deletes when true.",
              "type": "boolean"
            },
            "path": {
              "description": "Absolute path within the session workspace. `/workspace` prefix is accepted.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "NetworkAccessList": {
          "description": "Network access list controlling which hosts/URLs an agent session can reach.\n\n- `allowed`: if non-empty, only URLs matching these patterns are permitted.\n- `blocked`: URLs matching these patterns are always denied (takes precedence over allowed).\n\nPattern format:\n- `example.com` — exact domain match (any port, any path)\n- `*.example.com` — domain and all subdomains\n- `https://example.com/api/` — exact URL prefix (scheme + host + path)",
          "example": {
            "allowed": [
              "*.example.com",
              "https://api.acme.com/"
            ],
            "blocked": [
              "169.254.169.254"
            ]
          },
          "properties": {
            "allowed": {
              "description": "Allowed host patterns. If non-empty, only matching URLs are permitted.\nAn empty list means \"no restriction from this layer\" (inherit parent).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "blocked": {
              "description": "Blocked host patterns. Always denied, even if matched by `allowed`.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "SideEffectClass": {
          "description": "How many times a tool call may safely be executed given the same inputs.\n\nUsed by the durable Act activity (EVE-530) to decide what to do when a\nprior execution attempt left a `running` claim in `durable_tool_results`:\n\n* `Pure` / `Idempotent` — the running claim is stale; re-execute freely.\n* `AtMostOnce` — never re-execute from a stale running claim; settle it\n  as `interrupted` and surface an uncertain result to the model instead.\n\nWhen unset (`None`), the conservative default is `AtMostOnce`.",
          "enum": [
            "Pure",
            "Idempotent",
            "AtMostOnce"
          ],
          "type": "string"
        },
        "ToolDefinition": {
          "description": "Tool definition in agent configuration",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/BuiltinTool",
                  "description": "Built-in tool - executed by the worker via ToolRegistry"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "builtin"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Built-in tool - executed by the worker via ToolRegistry"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ClientSideTool",
                  "description": "Client-side tool - executed by the client, not the server"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "client_side"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Client-side tool - executed by the client, not the server"
            }
          ]
        },
        "ToolHints": {
          "description": "Semantic hints describing a tool's behavioral properties.\n\nFollows the MCP tool annotations convention (readOnlyHint, destructiveHint,\nidempotentHint, openWorldHint) plus everruns-specific hints. All fields are\noptional booleans — `None` means \"unknown/unspecified\". Consumers should\ntreat `None` as the conservative default (e.g., assume not readonly, assume\nnot idempotent).\n\nThese hints are informational — they do not enforce policy. Use `ToolPolicy`\nfor execution gating (auto vs requires_approval).",
          "properties": {
            "capability_id": {
              "description": "Capability that contributed this tool definition.\n\nReporting uses this attribution only as metadata. It must never contain\ntool arguments, results, prompts, or any other sensitive payload.",
              "type": [
                "string",
                "null"
              ]
            },
            "capability_name": {
              "description": "Human-readable capability name snapshot for reporting.",
              "type": [
                "string",
                "null"
              ]
            },
            "concurrency_class": {
              "description": "Scheduling conflict key. Tool calls within the same act batch that share\na non-empty `concurrency_class` are executed sequentially in arrival\norder; calls in different classes (or with no class) run concurrently.\n\nSet this on tools that mutate shared session state so that, e.g., two\nfile writes or two SQL mutations in one batch do not race. Read-only\ntools should leave this `None` so they always parallelize. See\n`everruns-engine`'s tool scheduler for how the act phase consumes it.",
              "type": [
                "string",
                "null"
              ]
            },
            "cpu_bound": {
              "description": "Tool performs significant CPU-bound or otherwise non-yielding work in\nprocess (e.g. an in-process interpreter). When true, the act scheduler\nruns the call on its own task (`tokio::spawn`) so a long CPU burst does\nnot starve the cooperative polling of I/O-bound tools in the same batch.\n\nDistinct from `long_running`, which describes wall-clock time for\nI/O-bound work (those tools yield at await points and need no offload).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "destructive": {
              "description": "Tool may irreversibly destroy or delete data.\nSubset of non-readonly — a tool can be non-readonly (writes) without\nbeing destructive (e.g., create/update operations).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "idempotent": {
              "description": "Calling the tool repeatedly with the same arguments produces the same\neffect. Safe to retry on transient failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "long_running": {
              "description": "Tool may take significant time to complete (> ~5s typical).\nUseful for clients to show progress indicators and set timeouts.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "metadata": {
              "description": "Host-owned annotations that core does not interpret.\n\nThe typed hints above are the vocabulary core itself reasons about. This\nis the escape hatch for everything a *host* wants to carry alongside a\ntool — risk tiers for an approval UI, presentation hints, an embedder's\nrouting keys — without adding a field to core for each one. Core reads\nnothing here and no driver sends it to a provider; it travels with the\ndefinition so a consumer sees it at the point of decision (e.g. a\n`PreToolUseHook` gating on what the tool declared).\n\nThe schema belongs to whoever writes it. Never put credentials or other\nsensitive payload here: like the rest of the definition, it is persisted\nand surfaced to clients."
            },
            "narration_noun": {
              "description": "Entity noun for operation-based narration (e.g. \"agent\", \"harness\").\nWhen set, the narration system reads the `operation` argument and\nproduces verb-based narration like \"Created agent: Neon Cartographer\"\ninstead of the generic \"Ran Manage Agents\".",
              "type": [
                "string",
                "null"
              ]
            },
            "open_world": {
              "description": "Tool interacts with external entities beyond the local system\n(network calls, third-party APIs, cloud services).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "persist_output": {
              "description": "Tool output should be persisted to session VFS before truncation.\nWhen set, the `tool_output_persistence` capability (EVE-222, EVE-245) writes\nstdout to `/outputs/{tool_call_id}.stdout` and stderr to\n`/outputs/{tool_call_id}.stderr`, injecting `full_output`, `total_lines`,\nand `output_files` into the result.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "readonly": {
              "description": "Tool does not modify any state (read-only queries, lookups).\nWhen true: safe to call speculatively, result can be cached.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "requires_secrets": {
              "description": "Tool requires API keys, credentials, or other secrets to function.\nUseful for UI to show connection prompts and for LLMs to anticipate\nauthentication failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "side_effect_class": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SideEffectClass",
                  "description": "Replay-safety class used by the durable Act activity (EVE-530).\n\nControls what happens when a worker reclaims a stale `running` claim:\n`Pure`/`Idempotent` tools are re-executed; `AtMostOnce` tools are\nsettled as `interrupted` to prevent double side-effects.\n\n`None` is treated conservatively as `AtMostOnce`."
                }
              ]
            },
            "supports_background": {
              "description": "Tool supports detached background execution via `spawn_background`.\nWhen true, the tool may be executed asynchronously outside the current\nforeground tool call and report status back later.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ToolPolicy": {
          "description": "Tool policy determines how tool calls are handled",
          "enum": [
            "auto",
            "requires_approval",
            "client_side"
          ],
          "type": "string"
        },
        "UpdateAgentRequest": {
          "description": "Request to update an agent. Only provided fields will be updated.",
          "properties": {
            "capabilities": {
              "description": "Capabilities to enable for this agent with per-agent configuration.\nReplaces existing capabilities. Each has a `ref` (capability ID) and optional `config`.",
              "example": [
                {
                  "config": {},
                  "ref": "current_time"
                },
                {
                  "config": {},
                  "ref": "web_fetch"
                }
              ],
              "items": {
                "$ref": "#/$defs/AgentCapabilityConfig"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "default_model_id": {
              "description": "The ID of the default LLM model to use for this agent.",
              "example": "model_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "description": {
              "description": "A human-readable description of what the agent does.",
              "example": "Updated description for the agent",
              "type": [
                "string",
                "null"
              ]
            },
            "display_name": {
              "description": "Human-readable display name shown in UI.",
              "example": "Updated Support Agent",
              "type": [
                "string",
                "null"
              ]
            },
            "harness_id": {
              "description": "Harness ID used as this agent's base execution environment. Omit to leave unchanged.",
              "example": "harness_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "harness_name": {
              "description": "Addressable harness name. Alternative to `harness_id`; omit to leave unchanged.",
              "example": "generic",
              "type": [
                "string",
                "null"
              ]
            },
            "initial_files": {
              "description": "Starter files copied into each new session for this agent.",
              "example": [
                {
                  "content": "Always respond in formal English.\n",
                  "path": "INSTRUCTIONS.md"
                }
              ],
              "items": {
                "$ref": "#/$defs/InitialFile"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "intro_markdown": {
              "description": "Markdown intro for fresh Platform Chat threads. Outer `None` leaves\nunchanged; inner `None` clears.",
              "example": "Hey, I'm Ava. Ask me anything about your account.",
              "type": [
                "string",
                "null"
              ]
            },
            "max_iterations": {
              "description": "Maximum number of LLM iterations per turn for this agent.",
              "example": 20,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "mcpServers": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/BTreeMap",
                  "description": "Remote MCP servers scoped to this agent."
                }
              ]
            },
            "name": {
              "description": "Name, unique per org. Lowercase alphanumeric and hyphens.",
              "example": "updated-support",
              "type": [
                "string",
                "null"
              ]
            },
            "network_access": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/NetworkAccessList",
                  "description": "Network access list controlling which hosts/URLs this agent's sessions can reach.\nSend `{}` (empty object) to clear restrictions. Omit to leave unchanged.\nExample shape is defined on `NetworkAccessList`."
                }
              ]
            },
            "parallel_tool_calls": {
              "description": "Request-level parallel tool calling preference (EVE-598). `true` signals\nthe provider that parallel tool calls are wanted; `false` requests at\nmost one tool call per turn and forces serial execution. Omit to leave\nunchanged.",
              "example": true,
              "type": [
                "boolean",
                "null"
              ]
            },
            "short_description": {
              "description": "One-line description in simplified Markdown. Outer `None` leaves\nunchanged; inner `None` clears.",
              "example": "Answers account questions in seconds.",
              "type": [
                "string",
                "null"
              ]
            },
            "starters": {
              "description": "Conversation starters; omit to leave unchanged, send empty to clear.\n`icon` reuses the harness icon name set.",
              "items": {
                "$ref": "#/$defs/ConversationStarter"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/AgentStatus",
                  "description": "The status of the agent. Set to \"archived\" to soft-delete."
                }
              ]
            },
            "system_prompt": {
              "description": "The system prompt that defines the agent's behavior and capabilities.",
              "example": "You are an updated helpful assistant.",
              "type": [
                "string",
                "null"
              ]
            },
            "tags": {
              "description": "Tags for organizing and filtering agents.",
              "example": [
                "updated-tag"
              ],
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "tools": {
              "description": "Client-side tools for this agent.\nReplaces existing tools if provided.",
              "example": [
                {
                  "description": "Open URL in the user's browser",
                  "name": "open_url",
                  "parameters": {
                    "properties": {
                      "url": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "url"
                    ],
                    "type": "object"
                  },
                  "type": "client_side"
                }
              ],
              "items": {
                "$ref": "#/$defs/ToolDefinition"
              },
              "type": [
                "array",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateAgentRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Update an agent. Only provided fields are changed."
    },
    "method": "PATCH",
    "name": "update_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "agent_endpoints",
    "cli": null,
    "description": "Update an agent ingress endpoint.",
    "input_schema": {
      "$defs": {
        "UpdateAgentEndpointRequest": {
          "description": "Request to update an ingress endpoint owned by an Agent.",
          "properties": {
            "channel_config": {
              "description": "Replacement transport-specific endpoint configuration."
            },
            "enabled": {
              "description": "Whether the endpoint can accept ingress traffic.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateAgentEndpointRequest"
        },
        {
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "endpoint_id": {
              "type": "string"
            }
          },
          "required": [
            "agent_id",
            "endpoint_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_agent_endpoint",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/endpoints/{endpoint_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agent_identities",
    "cli": null,
    "description": "Update an agent identity. Only provided fields are changed.",
    "input_schema": {
      "$defs": {
        "AgentIdentityStatus": {
          "description": "Agent identity lifecycle status.",
          "enum": [
            "active",
            "archived",
            "deleted"
          ],
          "type": "string"
        },
        "UpdateAgentIdentityRequest": {
          "properties": {
            "avatar_url": {
              "type": [
                "string",
                "null"
              ]
            },
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "locale": {
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/AgentIdentityStatus",
                  "description": "Current lifecycle status."
                }
              ]
            },
            "timezone": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateAgentIdentityRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Update an agent identity. Only provided fields are changed."
    },
    "method": "PATCH",
    "name": "update_agent_identity",
    "output_shape": "unknown",
    "path": "/v1/agent-identities/{identity_id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "agent_triggers",
    "cli": null,
    "description": "Update an agent trigger. Only provided fields change.",
    "input_schema": {
      "$defs": {
        "SessionBinding": {
          "description": "What identity keys a session, for every exposure and every transport.\n\nOne enum replaces the former `SessionStrategy` (messaging channels) and\n`InvocationSessionMode` (triggers and request/reply endpoints), which asked\nthe same question with disjoint vocabularies and forced every new surface to\npick a side (EVE-1005).\n\n**The serialized values are deliberately the legacy ones.** Every variant\nrenames in Rust but serializes exactly as it did before, with the new name\naccepted as a read alias. Persisted `channel_config` JSONB therefore needs no\nmigration, and the API and UI keep exchanging the values they already do.\nMoving the wire vocabulary is a separate, migration-bearing change.\n\n`Requester` keys on the **transport's own external actor id** — the Slack\nuser id, the Public Chat visitor id — never on an Everruns principal. Those\nactors are unrelated to Everruns accounts (a Public Chat visitor is anonymous\nor Google-signed-in), so there is one consistent answer rather than a split\nvariant: whatever the transport calls the requester, scoped by the\n`{platform}:` tag prefix that already namespaces it.",
          "enum": [
            "per_thread",
            "per_channel",
            "per_user",
            "shared_session",
            "session_per_invocation"
          ],
          "example": "per_thread",
          "type": "string"
        },
        "UpdateAgentTriggerRequest": {
          "description": "Request to update a trigger. Only provided fields change; the rest are\npreserved from the stored config.",
          "properties": {
            "auth": {
              "description": "Shared endpoint auth is not supported by webhook triggers."
            },
            "cron_expression": {
              "description": "Replacement cron expression.",
              "type": [
                "string",
                "null"
              ]
            },
            "enabled": {
              "description": "Replacement enabled state.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "message": {
              "description": "Replacement message sent when the trigger fires.",
              "type": [
                "string",
                "null"
              ]
            },
            "rate_limit_per_minute": {
              "description": "Replacement per-ingress, per-IP webhook request limit.",
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "session_mode": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SessionBinding",
                  "description": "Replacement session reuse strategy."
                }
              ]
            },
            "timezone": {
              "description": "Replacement IANA timezone identifier.",
              "type": [
                "string",
                "null"
              ]
            },
            "token": {
              "description": "Replacement webhook token.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateAgentTriggerRequest"
        },
        {
          "properties": {
            "agent_id": {
              "type": "string"
            },
            "trigger_id": {
              "type": "string"
            }
          },
          "required": [
            "agent_id",
            "trigger_id"
          ],
          "type": "object"
        }
      ],
      "description": "Update a trigger. Only provided fields change; the rest are preserved."
    },
    "method": "PATCH",
    "name": "update_agent_trigger",
    "output_shape": "unknown",
    "path": "/v1/agents/{agent_id}/triggers/{trigger_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "budgets",
    "cli": null,
    "description": "Update a budget limit, status, or metadata.",
    "input_schema": {
      "properties": {
        "budget_id": {
          "type": "string"
        },
        "limit": {
          "description": "Maximum number of items returned in this page.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "metadata": {
          "description": "Free-form metadata attached to this resource."
        },
        "soft_limit": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "status": {
          "description": "Current lifecycle status.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "budget_id"
      ],
      "type": "object"
    },
    "method": "PATCH",
    "name": "update_budget",
    "output_shape": "unknown",
    "path": "/v1/budgets/{budget_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "capabilities",
    "cli": null,
    "description": "Update a persisted declarative capability.",
    "input_schema": {
      "$defs": {
        "UpdateDeclarativeCapabilityRequest": {
          "description": "Request body for the `update_declarative_capability` operation.",
          "properties": {
            "definition": {
              "description": "Replacement declarative definition. Changing `name` updates the canonical capability ref after uniqueness validation.",
              "type": "object"
            },
            "status": {
              "description": "Optional lifecycle state update. Use `disabled` to hide from runtime selection without archiving.",
              "example": "disabled",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateDeclarativeCapabilityRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_declarative_capability",
    "output_shape": "unknown",
    "path": "/v1/capabilities/declarative/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Update an eval.",
    "input_schema": {
      "$defs": {
        "EvalTarget": {
          "description": "Defines how to instantiate a session for an eval case.\n\nTwo modes:\n- `Session`: mirrors `CreateSessionRequest` — full control over session creation parameters.\n- `App`: references a deployed app by ID.\n\nResolution order: EvalRun.target → EvalCase.target → Eval.target → org default harness.",
          "oneOf": [
            {
              "description": "Session creation parameters (mirrors CreateSessionRequest).",
              "properties": {
                "agent_id": {
                  "description": "Agent to work in this session.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_id": {
                  "description": "Harness for the session. If omitted, org default harness is used.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_name": {
                  "description": "Addressable harness name (alternative to harness_id).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "max_iterations": {
                  "description": "Max LLM iterations per turn.",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "model_id": {
                  "description": "LLM model override.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "system_prompt": {
                  "description": "System prompt override (prepended to agent prompt).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "session"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Reference to a deployed app.",
              "properties": {
                "app_id": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "app"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "app_id",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Label-only target for externally-executed runs (e.g. imported from Mira).\n\nCarries provider/model labels and opaque params instead of session setup:\nexternal runs are ingested already-complete, so everruns never builds a\nsession from this. Mirrors a provider-agnostic `(provider, model)` pair.",
              "properties": {
                "model": {
                  "type": "string"
                },
                "params": {},
                "provider": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "external"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "provider",
                "model",
                "type"
              ],
              "type": "object"
            }
          ]
        },
        "UpdateEvalRequest": {
          "description": "Request to update an eval",
          "properties": {
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "model_override": {
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "tags": {
              "description": "Free-form tags attached to this resource.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "target": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/EvalTarget",
                  "description": "Session setup target (harness+agent, app, or full session params)."
                }
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateEvalRequest"
        },
        {
          "properties": {
            "eval_id": {
              "type": "string"
            }
          },
          "required": [
            "eval_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_eval",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Update an eval case.",
    "input_schema": {
      "$defs": {
        "ArtifactSpec": {
          "description": "Named session file to collect after an eval case completes.",
          "properties": {
            "name": {
              "description": "Export key for this artifact (for example `patch` or `log`).",
              "type": "string"
            },
            "path": {
              "description": "Absolute path in the session filesystem.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "path"
          ],
          "type": "object"
        },
        "EvalInputMessage": {
          "description": "A message to send to the agent during an eval case.",
          "properties": {
            "content": {
              "description": "The text content to send.",
              "type": "string"
            }
          },
          "required": [
            "content"
          ],
          "type": "object"
        },
        "EvalTarget": {
          "description": "Defines how to instantiate a session for an eval case.\n\nTwo modes:\n- `Session`: mirrors `CreateSessionRequest` — full control over session creation parameters.\n- `App`: references a deployed app by ID.\n\nResolution order: EvalRun.target → EvalCase.target → Eval.target → org default harness.",
          "oneOf": [
            {
              "description": "Session creation parameters (mirrors CreateSessionRequest).",
              "properties": {
                "agent_id": {
                  "description": "Agent to work in this session.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_id": {
                  "description": "Harness for the session. If omitted, org default harness is used.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "harness_name": {
                  "description": "Addressable harness name (alternative to harness_id).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "max_iterations": {
                  "description": "Max LLM iterations per turn.",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "model_id": {
                  "description": "LLM model override.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "system_prompt": {
                  "description": "System prompt override (prepended to agent prompt).",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "session"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Reference to a deployed app.",
              "properties": {
                "app_id": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "app"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "app_id",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Label-only target for externally-executed runs (e.g. imported from Mira).\n\nCarries provider/model labels and opaque params instead of session setup:\nexternal runs are ingested already-complete, so everruns never builds a\nsession from this. Mirrors a provider-agnostic `(provider, model)` pair.",
              "properties": {
                "model": {
                  "type": "string"
                },
                "params": {},
                "provider": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "external"
                  ],
                  "type": "string"
                }
              },
              "required": [
                "provider",
                "model",
                "type"
              ],
              "type": "object"
            }
          ]
        },
        "Scorer": {
          "description": "A scoring rule applied to eval case output.",
          "oneOf": [
            {
              "description": "Final assistant message contains substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message does NOT contain substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "not_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message matches regex pattern.",
              "properties": {
                "pattern": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "regex"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "pattern",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent called named tool at least `min` times.",
              "properties": {
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent did NOT call named tool.",
              "properties": {
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_not_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Total tool calls within range.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "tool_call_count"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Completed within N turns.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": {
                  "enum": [
                    "turns_within"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "max",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Session filesystem file contains substring.",
              "properties": {
                "path": {
                  "type": "string"
                },
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "file_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "path",
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message parses as JSON matching schema.",
              "properties": {
                "schema": {},
                "type": {
                  "enum": [
                    "json_schema"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "schema",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness: the answer's citations (see `knowledge/runtime-resources/citations.md`)\nmust cover the claim and be verified as supported. Scored from the\n`TextAnnotation`s on the final message — pair with the\n`citation_verification` capability so verdicts are present.",
              "properties": {
                "min_citations": {
                  "description": "Minimum number of citations the answer must carry.",
                  "example": 1,
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "pass_threshold": {
                  "description": "Minimum fraction of citations verified `entailed` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "type": {
                  "enum": [
                    "citation_faithful"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness judged by an LLM: each cited claim/source pair is\ngraded by a model, so the eval works even without the\n`citation_verification` capability. See `knowledge/runtime-resources/citations.md`.",
              "properties": {
                "model_id": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/$defs/modelId",
                      "description": "Judge model; the org's default is used when absent."
                    }
                  ]
                },
                "pass_threshold": {
                  "description": "Minimum judged score `[0,1]` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "rubric": {
                  "description": "Rubric override; a citation-faithfulness rubric is used when absent.",
                  "example": "Score the fraction of cited claims supported by their source.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "citation_judged"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            }
          ]
        },
        "UpdateEvalCaseRequest": {
          "description": "Request to update an eval case",
          "properties": {
            "artifacts": {
              "description": "Session files to capture after scoring completes.",
              "items": {
                "$ref": "#/$defs/ArtifactSpec"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "conversation": {
              "items": {
                "$ref": "#/$defs/EvalInputMessage"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "max_turns": {
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "position": {
              "format": "int32",
              "type": [
                "integer",
                "null"
              ]
            },
            "post": {
              "description": "Verification messages sent after conversation completes.",
              "items": {
                "$ref": "#/$defs/EvalInputMessage"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "scorers": {
              "items": {
                "$ref": "#/$defs/Scorer"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "tags": {
              "description": "Free-form tags attached to this resource.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "target": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/EvalTarget",
                  "description": "Optional per-case target override."
                }
              ]
            },
            "timeout_seconds": {
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "modelId": {
          "description": "Prefixed identifier with 'model' prefix",
          "example": "model_01933b5a000070008000000000000001",
          "pattern": "^model_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateEvalCaseRequest"
        },
        {
          "properties": {
            "case_id": {
              "type": "string"
            },
            "eval_id": {
              "type": "string"
            }
          },
          "required": [
            "eval_id",
            "case_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_eval_case",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/cases/{case_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "evals",
    "cli": null,
    "description": "Update scores for one eval result.",
    "input_schema": {
      "$defs": {
        "ExternalScoreStatus": {
          "enum": [
            "passed",
            "failed",
            "errored"
          ],
          "type": "string"
        },
        "Score": {
          "description": "Result from a single scorer evaluation.",
          "properties": {
            "pass": {
              "description": "Whether this scorer passed.",
              "type": "boolean"
            },
            "reason": {
              "description": "Human-readable explanation.",
              "type": "string"
            },
            "value": {
              "description": "Score value 0.0–1.0.",
              "format": "double",
              "type": "number"
            }
          },
          "required": [
            "pass",
            "value",
            "reason"
          ],
          "type": "object"
        },
        "UpdateEvalResultScoresRequest": {
          "properties": {
            "metadata": {
              "description": "Free-form metadata attached to this resource."
            },
            "scores": {
              "items": {
                "$ref": "#/$defs/Score"
              },
              "type": "array"
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/ExternalScoreStatus",
                  "description": "Current lifecycle status."
                }
              ]
            }
          },
          "required": [
            "scores"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateEvalResultScoresRequest"
        },
        {
          "properties": {
            "eval_id": {
              "type": "string"
            },
            "result_id": {
              "type": "string"
            },
            "run_id": {
              "type": "string"
            }
          },
          "required": [
            "eval_id",
            "run_id",
            "result_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_eval_result_scores",
    "output_shape": "unknown",
    "path": "/v1/evals/{eval_id}/runs/{run_id}/results/{result_id}/scores",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "harnesses",
    "cli": null,
    "description": "Update a harness. Only provided fields are changed.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "ConversationStarter": {
          "description": "A conversation starter shown on a fresh Platform Chat thread.\nSelecting one inserts its text into the composer. `icon` reuses the\nharness icon name set (`HarnessIcon`); unknown names fall back to the\ndefault glyph.",
          "properties": {
            "icon": {
              "description": "Optional icon name from the harness icon set.",
              "example": "zap",
              "type": [
                "string",
                "null"
              ]
            },
            "text": {
              "description": "Prompt text inserted into the composer when selected.",
              "example": "Triage the newest P1",
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "type": "object"
        },
        "HarnessStatus": {
          "description": "Harness lifecycle status.\n- `active`: Harness is available for use\n- `archived`: Harness is hidden from listings and cannot be modified or assigned\n- `deleted`: Harness is a tombstone kept only for historical references",
          "enum": [
            "active",
            "archived",
            "deleted"
          ],
          "example": "active",
          "type": "string"
        },
        "InitialFile": {
          "description": "Starter file copied into a new session from an agent or harness.",
          "properties": {
            "content": {
              "description": "File content: plain text or base64-encoded binary.",
              "type": "string"
            },
            "encoding": {
              "description": "Content encoding: `text` or `base64`.",
              "type": "string"
            },
            "is_readonly": {
              "description": "Prevent session-side edits or deletes when true.",
              "type": "boolean"
            },
            "path": {
              "description": "Absolute path within the session workspace. `/workspace` prefix is accepted.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "NetworkAccessList": {
          "description": "Network access list controlling which hosts/URLs an agent session can reach.\n\n- `allowed`: if non-empty, only URLs matching these patterns are permitted.\n- `blocked`: URLs matching these patterns are always denied (takes precedence over allowed).\n\nPattern format:\n- `example.com` — exact domain match (any port, any path)\n- `*.example.com` — domain and all subdomains\n- `https://example.com/api/` — exact URL prefix (scheme + host + path)",
          "example": {
            "allowed": [
              "*.example.com",
              "https://api.acme.com/"
            ],
            "blocked": [
              "169.254.169.254"
            ]
          },
          "properties": {
            "allowed": {
              "description": "Allowed host patterns. If non-empty, only matching URLs are permitted.\nAn empty list means \"no restriction from this layer\" (inherit parent).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "blocked": {
              "description": "Blocked host patterns. Always denied, even if matched by `allowed`.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "UpdateHarnessRequest": {
          "description": "Request to update a harness. Only provided fields will be updated.",
          "properties": {
            "capabilities": {
              "description": "Replace the capability list entirely; omit to leave unchanged.",
              "example": [
                {
                  "config": {},
                  "ref": "current_time"
                },
                {
                  "config": {},
                  "ref": "web_fetch"
                }
              ],
              "items": {
                "$ref": "#/$defs/AgentCapabilityConfig"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "default_model_id": {
              "description": "New default model selected when sessions inherit from this harness; omit to leave unchanged.",
              "example": "model_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "example": "Research harness with web tools",
              "type": [
                "string",
                "null"
              ]
            },
            "display_name": {
              "description": "Human-readable display name.",
              "example": "Updated Research Harness",
              "type": [
                "string",
                "null"
              ]
            },
            "embedder_metadata": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Replace the embedder metadata map entirely; omit to leave unchanged.",
              "propertyNames": {
                "type": "string"
              },
              "type": [
                "object",
                "null"
              ]
            },
            "initial_files": {
              "description": "Replace the initial-files list entirely; omit to leave unchanged.",
              "example": [
                {
                  "content": "Cite sources verbatim.\n",
                  "path": "INSTRUCTIONS.md"
                }
              ],
              "items": {
                "$ref": "#/$defs/InitialFile"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "intro_markdown": {
              "description": "Markdown intro shown as an intro box on a fresh Platform Chat thread.\nOuter `None` leaves unchanged; inner `None` clears.",
              "example": "I triage incidents, dig through logs, and draft the update.",
              "type": [
                "string",
                "null"
              ]
            },
            "mcpServers": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/BTreeMap",
                  "description": "Replace the scoped MCP server set entirely; omit to leave unchanged."
                }
              ]
            },
            "name": {
              "description": "Name, unique per org.",
              "example": "updated-research",
              "type": [
                "string",
                "null"
              ]
            },
            "network_access": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/NetworkAccessList",
                  "description": "Network access list. Send `{}` (empty object) to clear. Omit to leave unchanged.\nExample shape is defined on `NetworkAccessList`."
                }
              ]
            },
            "parent_harness_id": {
              "description": "New parent harness for inheritance. Outer `None` leaves unchanged; inner `None` removes inheritance (becomes a root harness).",
              "type": [
                "string",
                "null"
              ]
            },
            "short_description": {
              "description": "One-line description in simplified Markdown. Outer `None` leaves\nunchanged; inner `None` clears.",
              "example": "Triage incidents, dig through logs, draft the update.",
              "type": [
                "string",
                "null"
              ]
            },
            "starters": {
              "description": "Conversation starters; omit to leave unchanged, send empty to clear.\n`icon` reuses the harness icon name set.",
              "items": {
                "$ref": "#/$defs/ConversationStarter"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/HarnessStatus",
                  "description": "Current lifecycle status. Example shape is defined on `HarnessStatus`."
                }
              ]
            },
            "system_prompt": {
              "description": "New system prompt the harness contributes to sessions; omit to leave unchanged.",
              "example": "You are a research assistant. Cite sources verbatim.",
              "type": [
                "string",
                "null"
              ]
            },
            "tags": {
              "description": "Replace the tag list entirely; omit to leave unchanged.",
              "example": [
                "research",
                "web-tools"
              ],
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateHarnessRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Update a harness. Only provided fields are changed."
    },
    "method": "PATCH",
    "name": "update_harness",
    "output_shape": "unknown",
    "path": "/v1/harnesses/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Update a knowledge base.",
    "input_schema": {
      "$defs": {
        "UpdateKnowledgeBaseRequest": {
          "description": "Request body for the `update_knowledge_base` operation.",
          "properties": {
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "embedding_model_id": {
              "description": "Optional embedding model for hybrid retrieval. Set to null to clear.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "example": "support-runbooks-archive",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateKnowledgeBaseRequest"
        },
        {
          "properties": {
            "kb_id": {
              "description": "Knowledge base's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "kb_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_knowledge_base",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_bases",
    "cli": null,
    "description": "Update a knowledge entry.",
    "input_schema": {
      "$defs": {
        "UpdateKnowledgeEntryRequest": {
          "description": "Request body for the `update_knowledge_entry` operation.",
          "properties": {
            "body": {
              "description": "Updated entry body. Markdown is rendered when displayed.",
              "example": "Use the `/v1/payments/{id}/refund` endpoint with `reason: \"past_window\"`. Now requires VP approval.",
              "type": [
                "string",
                "null"
              ]
            },
            "kind": {
              "description": "Discriminator selecting the variant of this resource. One of `note`,\n`table`, `business`, `query`, `runbook`.",
              "example": "runbook",
              "type": [
                "string",
                "null"
              ]
            },
            "resource": {
              "description": "Optional OKF resource URI. Set to null to clear.",
              "type": [
                "string",
                "null"
              ]
            },
            "tags": {
              "description": "Free-form tags attached to this resource.",
              "example": [
                "billing",
                "refunds",
                "vp-approval"
              ],
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "title": {
              "description": "Human-readable title. Safe to render in user-facing messages.",
              "example": "Refund a payment past 60 days",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateKnowledgeEntryRequest"
        },
        {
          "properties": {
            "entry_id": {
              "description": "Knowledge base entry's prefixed public identifier.",
              "type": "string"
            },
            "kb_id": {
              "description": "Knowledge base's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "kb_id",
            "entry_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_knowledge_entry",
    "output_shape": "unknown",
    "path": "/v1/knowledge-bases/{kb_id}/entries/{entry_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "knowledge_indexes",
    "cli": null,
    "description": "Update a knowledge index.",
    "input_schema": {
      "$defs": {
        "UpdateKnowledgeIndexRequest": {
          "description": "Request body for the `update_knowledge_index` operation.",
          "properties": {
            "description": {
              "description": "Human-readable description. Set to null to clear.",
              "type": [
                "string",
                "null"
              ]
            },
            "embedding_model_id": {
              "description": "Embedding model used to embed chunks. Required; cannot be cleared.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "example": "product-docs-v2",
              "type": [
                "string",
                "null"
              ]
            },
            "source_config": {
              "description": "Non-secret source coordinates. Never include credentials."
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateKnowledgeIndexRequest"
        },
        {
          "properties": {
            "index_id": {
              "description": "Knowledge index's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "index_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_knowledge_index",
    "output_shape": "unknown",
    "path": "/v1/knowledge-indexes/{index_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "mcp_servers",
    "cli": "mcp-servers update",
    "description": "Update an MCP server. Only provided fields are changed.",
    "input_schema": {
      "$defs": {
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerStatus": {
          "description": "MCP Server lifecycle status.\n- `active`: Server is available for use\n- `disabled`: Server is disabled and not used\n- `archived`: Server is hidden from listings and cannot be modified or assigned\n- `deleted`: Server is a tombstone kept only for historical references",
          "enum": [
            "active",
            "disabled",
            "archived",
            "deleted"
          ],
          "example": "active",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "UpdateMcpServerRequest": {
          "description": "Request to update an MCP server. Only provided fields will be updated.",
          "properties": {
            "api_key": {
              "description": "API key for authentication. Set to update.",
              "example": "sk-mcp-redacted-1234567890abcdef",
              "type": [
                "string",
                "null"
              ]
            },
            "auth_mode": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/McpServerAuthMode",
                  "description": "Authentication mode."
                }
              ]
            },
            "description": {
              "description": "A human-readable description of what the MCP server provides.",
              "example": "Updated description",
              "type": [
                "string",
                "null"
              ]
            },
            "headers": {
              "additionalProperties": {
                "type": "string"
              },
              "description": "Additional HTTP headers for authentication.",
              "example": {
                "X-Atlassian-Cloud-Id": "00000000-0000-0000-0000-000000000000"
              },
              "propertyNames": {
                "type": "string"
              },
              "type": [
                "object",
                "null"
              ]
            },
            "name": {
              "description": "The name of the MCP server.",
              "example": "updated-mcp-server",
              "type": [
                "string",
                "null"
              ]
            },
            "protocol_mode": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/McpProtocolMode",
                  "description": "Protocol-era policy (`auto`, `legacy`, `stable`, `rc`)."
                }
              ]
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/McpServerStatus",
                  "description": "The status of the MCP server. Set to \"disabled\" to disable."
                }
              ]
            },
            "transport_type": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/McpServerTransportType",
                  "description": "Transport type."
                }
              ]
            },
            "url": {
              "description": "The URL of the MCP server endpoint.",
              "example": "https://mcp.example.com/v1/mcp",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateMcpServerRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Update an MCP server. Only provided fields are changed."
    },
    "method": "PATCH",
    "name": "update_mcp_server",
    "output_shape": "unknown",
    "path": "/v1/mcp-servers/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "memories",
    "cli": null,
    "description": "Update a workspace memory.",
    "input_schema": {
      "$defs": {
        "CreateMemorySourceRequest": {
          "description": "Request body for the `create_memory_source` operation.",
          "example": {
            "branch": "main",
            "repository": "acme/design-docs",
            "root_folder": "docs/",
            "type": "github"
          },
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/GitHubMemorySourceRequest"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "github"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ]
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/GitMemorySourceRequest"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "git"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ]
            }
          ]
        },
        "GitHubMemorySourceRequest": {
          "description": "Request body for GitHub memory source.",
          "properties": {
            "branch": {
              "description": "Branch to sync. Defaults to the repository's default branch when omitted.",
              "example": "main",
              "type": [
                "string",
                "null"
              ]
            },
            "repository": {
              "description": "GitHub repository in `owner/repo` form.",
              "example": "acme/design-docs",
              "type": "string"
            },
            "root_folder": {
              "description": "Sub-directory within the repository to sync. Empty / omitted = whole tree.",
              "example": "docs/",
              "type": [
                "string",
                "null"
              ]
            },
            "sync_interval_secs": {
              "description": "Automatic resync interval in seconds. Omit or set 0 for manual-only; scheduled sync accepts 300 through 604800.",
              "example": 3600,
              "format": "int32",
              "maximum": 604800,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            }
          },
          "required": [
            "repository"
          ],
          "type": "object"
        },
        "GitMemorySourceRequest": {
          "description": "Request body for git memory source.",
          "properties": {
            "branch": {
              "description": "Branch to sync. Defaults to the repository's default branch when omitted.",
              "example": "main",
              "type": [
                "string",
                "null"
              ]
            },
            "root_folder": {
              "description": "Sub-directory within the repository to sync. Empty / omitted = whole tree.",
              "example": "docs/",
              "type": [
                "string",
                "null"
              ]
            },
            "sync_interval_secs": {
              "description": "Automatic resync interval in seconds. Omit or set 0 for manual-only; scheduled sync accepts 300 through 604800.",
              "example": 3600,
              "format": "int32",
              "maximum": 604800,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "url": {
              "description": "Clonable git URL (SSH or HTTPS).",
              "example": "https://github.com/acme/design-docs.git",
              "type": "string"
            }
          },
          "required": [
            "url"
          ],
          "type": "object"
        },
        "UpdateMemoryRequest": {
          "description": "Request body for the `update_memory` operation.",
          "properties": {
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "example": "design-docs",
              "type": [
                "string",
                "null"
              ]
            },
            "source": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/CreateMemorySourceRequest",
                  "description": "Source configuration. Example shape is defined on `CreateMemorySourceRequest`."
                }
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateMemoryRequest"
        },
        {
          "properties": {
            "memory_id": {
              "description": "Workspace memory's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "memory_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_memory",
    "output_shape": "unknown",
    "path": "/v1/memories/{memory_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "models",
    "cli": null,
    "description": "Update a model.",
    "input_schema": {
      "properties": {
        "capabilities": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "display_name": {
          "description": "Human-readable display name. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "description": "Whether this resource is enabled.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        },
        "is_favorite": {
          "type": [
            "boolean",
            "null"
          ]
        },
        "model_id": {
          "description": "LLM model's prefixed public identifier.",
          "type": [
            "string",
            "null"
          ]
        },
        "provider_id": {
          "description": "LLM provider's prefixed public identifier.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "PATCH",
    "name": "update_model",
    "output_shape": "unknown",
    "path": "/v1/models/{id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "observers",
    "cli": null,
    "description": "Update an observer.",
    "input_schema": {
      "$defs": {
        "LlmJudgeConfig": {
          "description": "LLM-as-judge scoring configuration. The judge grades the scoped trace\nslice against `rubric` and returns a 0.0–1.0 value, an optional\ncategorical label, and free-text reasoning.",
          "properties": {
            "model_id": {
              "description": "Org model to judge with. When `None`, the org's default model is used.\nJudge calls go through the org's own providers and are billed to it.",
              "type": [
                "string",
                "null"
              ]
            },
            "pass_threshold": {
              "description": "Score value at/above which `pass` is true.",
              "format": "double",
              "type": "number"
            },
            "rubric": {
              "description": "Grading rubric shown to the judge model. Should describe what a high\nvs. low score means.",
              "type": "string"
            }
          },
          "required": [
            "rubric"
          ],
          "type": "object"
        },
        "ObserverMatch": {
          "description": "Predicates selecting which production sessions an observer scores.\nAll present predicates must match (AND); within a list, any entry matches (OR).\nAn empty match block matches all org traffic. Sessions tagged `eval` are\nalways excluded so synthetic eval-run sessions are never scored.",
          "properties": {
            "agent_ids": {
              "description": "Match sessions running any of these agents.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "harness_ids": {
              "description": "Match sessions on any of these harnesses.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "session_tags": {
              "description": "Match sessions carrying any of these tags.",
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ObserverScope": {
          "description": "What slice of the trace a scorer grades. Phase 1 implements `turn` only;\n`session` and `tool` scopes are specced in knowledge/evaluation/online-evals.md.",
          "enum": [
            "turn"
          ],
          "type": "string"
        },
        "ObserverScorerConfig": {
          "allOf": [
            {
              "$ref": "#/$defs/ScorerMethod",
              "description": "How this scorer grades (rule or llm_judge)."
            },
            {
              "properties": {
                "key": {
                  "description": "Stable name within the observer (score series name).",
                  "type": "string"
                },
                "scope": {
                  "$ref": "#/$defs/ObserverScope",
                  "description": "Trace slice this scorer grades."
                }
              },
              "required": [
                "key"
              ],
              "type": "object"
            }
          ],
          "description": "One scorer inside an observer. `key` names the score series in listings\nand future dashboards; `scope` selects the trace slice; `method` is how\nit grades."
        },
        "ObserverStatus": {
          "description": "Observer lifecycle status. `paused` keeps configuration but stops matching.",
          "enum": [
            "active",
            "paused",
            "archived",
            "deleted"
          ],
          "type": "string"
        },
        "Scorer": {
          "description": "A scoring rule applied to eval case output.",
          "oneOf": [
            {
              "description": "Final assistant message contains substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message does NOT contain substring.",
              "properties": {
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "not_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message matches regex pattern.",
              "properties": {
                "pattern": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "regex"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "pattern",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent called named tool at least `min` times.",
              "properties": {
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Agent did NOT call named tool.",
              "properties": {
                "tool": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "tool_not_called"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "tool",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Total tool calls within range.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "min": {
                  "format": "int32",
                  "minimum": 0,
                  "type": [
                    "integer",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "tool_call_count"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Completed within N turns.",
              "properties": {
                "max": {
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "type": {
                  "enum": [
                    "turns_within"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "max",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Session filesystem file contains substring.",
              "properties": {
                "path": {
                  "type": "string"
                },
                "text": {
                  "type": "string"
                },
                "type": {
                  "enum": [
                    "file_contains"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "path",
                "text",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Final assistant message parses as JSON matching schema.",
              "properties": {
                "schema": {},
                "type": {
                  "enum": [
                    "json_schema"
                  ],
                  "type": "string"
                },
                "weight": {
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "schema",
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness: the answer's citations (see `knowledge/runtime-resources/citations.md`)\nmust cover the claim and be verified as supported. Scored from the\n`TextAnnotation`s on the final message — pair with the\n`citation_verification` capability so verdicts are present.",
              "properties": {
                "min_citations": {
                  "description": "Minimum number of citations the answer must carry.",
                  "example": 1,
                  "format": "int32",
                  "minimum": 0,
                  "type": "integer"
                },
                "pass_threshold": {
                  "description": "Minimum fraction of citations verified `entailed` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "type": {
                  "enum": [
                    "citation_faithful"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            },
            {
              "description": "Citation faithfulness judged by an LLM: each cited claim/source pair is\ngraded by a model, so the eval works even without the\n`citation_verification` capability. See `knowledge/runtime-resources/citations.md`.",
              "properties": {
                "model_id": {
                  "oneOf": [
                    {
                      "type": "null"
                    },
                    {
                      "$ref": "#/$defs/modelId",
                      "description": "Judge model; the org's default is used when absent."
                    }
                  ]
                },
                "pass_threshold": {
                  "description": "Minimum judged score `[0,1]` to pass.",
                  "example": 0.8,
                  "format": "double",
                  "type": "number"
                },
                "rubric": {
                  "description": "Rubric override; a citation-faithfulness rubric is used when absent.",
                  "example": "Score the fraction of cited claims supported by their source.",
                  "type": [
                    "string",
                    "null"
                  ]
                },
                "type": {
                  "enum": [
                    "citation_judged"
                  ],
                  "type": "string"
                },
                "weight": {
                  "description": "Relative weight of this scorer in the case's weighted average.",
                  "example": 1.0,
                  "format": "double",
                  "type": "number"
                }
              },
              "required": [
                "type"
              ],
              "type": "object"
            }
          ]
        },
        "ScorerMethod": {
          "description": "How a scorer grades a trace slice: a deterministic `rule` (reusing the\neval scorer vocabulary) or an `llm_judge`.",
          "oneOf": [
            {
              "description": "Deterministic rule. `file_contains` is rejected for observers (session\nfilesystems are not part of the observable trace contract).",
              "properties": {
                "method": {
                  "enum": [
                    "rule"
                  ],
                  "type": "string"
                },
                "rule": {
                  "$ref": "#/$defs/Scorer"
                }
              },
              "required": [
                "rule",
                "method"
              ],
              "type": "object"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/LlmJudgeConfig",
                  "description": "LLM-as-judge."
                },
                {
                  "properties": {
                    "method": {
                      "enum": [
                        "llm_judge"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "method"
                  ],
                  "type": "object"
                }
              ],
              "description": "LLM-as-judge."
            }
          ]
        },
        "UpdateObserverRequest": {
          "description": "Request to update an observer. Omitted fields are unchanged.",
          "properties": {
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "match": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/ObserverMatch"
                }
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            },
            "sampling_rate": {
              "format": "double",
              "type": [
                "number",
                "null"
              ]
            },
            "scorers": {
              "items": {
                "$ref": "#/$defs/ObserverScorerConfig"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/ObserverStatus",
                  "description": "New lifecycle status (`active`, `paused`, or `archived`)."
                }
              ]
            }
          },
          "type": "object"
        },
        "modelId": {
          "description": "Prefixed identifier with 'model' prefix",
          "example": "model_01933b5a000070008000000000000001",
          "pattern": "^model_[0-9a-f]{32}$",
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateObserverRequest"
        },
        {
          "properties": {
            "observer_id": {
              "type": "string"
            }
          },
          "required": [
            "observer_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_observer",
    "output_shape": "unknown",
    "path": "/v1/observers/{observer_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Update a machine-payment wallet account.",
    "input_schema": {
      "properties": {
        "label": {
          "type": [
            "string",
            "null"
          ]
        },
        "metadata": {
          "description": "Free-form metadata attached to this resource."
        },
        "payment_account_id": {
          "type": "string"
        },
        "private_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "public_address": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Current lifecycle status.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "payment_account_id"
      ],
      "type": "object"
    },
    "method": "PATCH",
    "name": "update_payment_account",
    "output_shape": "unknown",
    "path": "/v1/payments/accounts/{payment_account_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "payments",
    "cli": null,
    "description": "Update a machine-payment spend policy.",
    "input_schema": {
      "properties": {
        "allowed_capabilities": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "allowed_hosts": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "max_amount_usd_per_day": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "max_amount_usd_per_request": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "max_amount_usd_per_turn": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "metadata": {
          "description": "Free-form metadata attached to this resource."
        },
        "payment_policy_id": {
          "type": "string"
        },
        "rail_preference": {
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "require_approval_above_usd": {
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "status": {
          "description": "Current lifecycle status.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "payment_policy_id"
      ],
      "type": "object"
    },
    "method": "PATCH",
    "name": "update_payment_policy",
    "output_shape": "unknown",
    "path": "/v1/payments/policies/{payment_policy_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Re-install an installed plugin at the marketplace's current catalog version.",
    "input_schema": {
      "description": "Re-compile and update an installed plugin from the marketplace's current catalog.",
      "properties": {
        "id": {
          "description": "Public plugin ID.",
          "type": "string"
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "POST",
    "name": "update_plugin",
    "output_shape": "unknown",
    "path": "/v1/plugins/{id}/update",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "plugins",
    "cli": null,
    "description": "Update a plugin marketplace (name or status).",
    "input_schema": {
      "$defs": {
        "UpdatePluginMarketplaceRequest": {
          "description": "Request body for updating a plugin marketplace.",
          "properties": {
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "status": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdatePluginMarketplaceRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Public marketplace ID.",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Update a plugin marketplace's name or status."
    },
    "method": "PATCH",
    "name": "update_plugin_marketplace",
    "output_shape": "unknown",
    "path": "/v1/plugin_marketplaces/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "providers",
    "cli": null,
    "description": "Update an LLM provider.",
    "input_schema": {
      "$defs": {
        "DriverId": {
          "description": "LLM provider type. Built-in: openai, openrouter, azure_openai, openai_completions, anthropic, gemini, llmsim, bedrock, mai, fireworks, meta. Any other string is treated as an embedder-defined external provider.",
          "type": "string"
        },
        "ProviderRequestHeader": {
          "description": "One extra HTTP header sent with every request to a provider connection.",
          "properties": {
            "name": {
              "description": "Header name, e.g. `x-gateway-tenant`.",
              "type": "string"
            },
            "value": {
              "description": "Header value, sent verbatim.",
              "type": "string"
            }
          },
          "required": [
            "name",
            "value"
          ],
          "type": "object"
        },
        "ProviderRequestOptions": {
          "description": "Per-connection request options: what an org wants added to every outbound\nrequest to this provider, beyond endpoint and credentials.\n\nThese are connection-level on purpose. A gateway header or a diagnostics\nopt-in describes the *service* an org talks to, not one agent's behavior, so\nit belongs next to the base URL and credentials rather than on every agent.",
          "properties": {
            "cache_diagnostics": {
              "description": "Ask the provider to explain unexpected prompt-cache misses. Honored by\ndrivers with a diagnostics protocol (today: Anthropic's\n`cache-diagnosis` beta); ignored elsewhere.",
              "type": "boolean"
            },
            "headers": {
              "description": "Extra HTTP headers added to every request to this provider. They\noverride the driver's and the connection's own headers by name, but\nconnection-level headers (`host`, `content-length`, ...) are ignored.",
              "items": {
                "$ref": "#/$defs/ProviderRequestHeader"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "ProviderStatus": {
          "description": "LLM provider status",
          "enum": [
            "active",
            "disabled"
          ],
          "type": "string"
        },
        "ProviderTraceConfig": {
          "description": "Configuration for linking from the chat UI to a provider's observability\ndashboard (\"trace\"/\"logs\").\n\nThis is provider-agnostic: any driver with a dashboard can supply default\ntemplates (see [`DriverId::default_trace_templates`]), and an org enables\nlinks per provider once it has confirmed logging is on for that account.\nURL templates support the `{response_id}`, `{session_id}`, `{turn_id}` and\n`{model}` placeholders, so the same mechanism works for OpenRouter today and\nfor third-party observability backends (Langfuse, Helicone, ...) via an\noverride.",
          "properties": {
            "enabled": {
              "description": "Whether trace links should be shown for this provider. Defaults to\n`false`: vendors typically do not retain trace content unless logging is\nexplicitly enabled, so the org opts in once that is set up.",
              "type": "boolean"
            },
            "generation_url_template": {
              "description": "URL template for a single generation's trace, e.g.\n`\"https://openrouter.ai/logs?id={response_id}\"`.",
              "type": [
                "string",
                "null"
              ]
            },
            "session_url_template": {
              "description": "URL template for a session's grouped trace, e.g.\n`\"https://openrouter.ai/logs\"`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "enabled"
          ],
          "type": "object"
        }
      },
      "properties": {
        "api_key": {
          "type": [
            "string",
            "null"
          ]
        },
        "base_url": {
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
          "type": "string"
        },
        "name": {
          "description": "Human-readable name. Safe to render in user-facing messages.",
          "type": [
            "string",
            "null"
          ]
        },
        "provider_type": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/DriverId"
            }
          ]
        },
        "request_options": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ProviderRequestOptions"
            }
          ]
        },
        "status": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ProviderStatus",
              "description": "Current lifecycle status."
            }
          ]
        },
        "trace": {
          "oneOf": [
            {
              "type": "null"
            },
            {
              "$ref": "#/$defs/ProviderTraceConfig",
              "description": "Trace/observability link configuration override (merged into stored\nsettings, preserving other keys)."
            }
          ]
        }
      },
      "required": [
        "id"
      ],
      "type": "object"
    },
    "method": "PATCH",
    "name": "update_provider",
    "output_shape": "unknown",
    "path": "/v1/providers/{id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "reporting",
    "cli": null,
    "description": "Update an org-scoped saved report definition.",
    "input_schema": {
      "$defs": {
        "ReportFilter": {
          "description": "One predicate filter applied to the dataset before aggregation.\nCombined with other filters via logical AND.",
          "properties": {
            "field": {
              "description": "Field to filter on. Must be a filter field exposed by the dataset (see `filter_fields` in the catalog).",
              "example": "status",
              "type": "string"
            },
            "op": {
              "$ref": "#/$defs/ReportFilterOp",
              "description": "Comparison operator. Determines the expected shape of `value`\n(scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`, array for `in`)."
            },
            "value": {
              "description": "Comparison value. Type depends on `op`: a scalar for `eq`/`neq`/`gt`/`gte`/`lt`/`lte`,\nan array for `in`. Example for `op = in`: `[\"completed\", \"failed\"]`."
            }
          },
          "required": [
            "field",
            "op",
            "value"
          ],
          "type": "object"
        },
        "ReportFilterOp": {
          "description": "Comparison operator used in a `ReportFilter`. The `In` variant takes a\nJSON array as its value; all others take a scalar.",
          "enum": [
            "eq",
            "neq",
            "in",
            "gt",
            "gte",
            "lt",
            "lte"
          ],
          "type": "string"
        },
        "ReportOrderBy": {
          "description": "One sort clause applied to the aggregated result. Either `dimension`\nOR `measure` is set (mutually exclusive), never both.",
          "properties": {
            "dimension": {
              "description": "Dimension name to sort by. Mutually exclusive with `measure`.",
              "example": "org_id",
              "type": [
                "string",
                "null"
              ]
            },
            "direction": {
              "$ref": "#/$defs/ReportOrderDirection",
              "description": "Sort direction (`asc` or `desc`). Defaults to `asc`."
            },
            "measure": {
              "description": "Measure name to sort by. Mutually exclusive with `dimension`.",
              "example": "session_count",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ReportOrderDirection": {
          "description": "Sort direction for a `ReportOrderBy` clause.",
          "enum": [
            "asc",
            "desc"
          ],
          "type": "string"
        },
        "ReportQuery": {
          "description": "Semantic query a caller submits to the reporting layer. The backend\ncompiles this to its native query language, scopes it to the calling\norg, and returns a `ReportResult`.",
          "properties": {
            "dataset": {
              "description": "Dataset name to query (see `GET /v1/reports/catalog` for the list of available datasets).",
              "example": "sessions",
              "type": "string"
            },
            "dimensions": {
              "description": "Columns to group by. Empty list returns one aggregate row.",
              "example": [
                "status"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "filters": {
              "description": "Predicate filters applied before aggregation.",
              "items": {
                "$ref": "#/$defs/ReportFilter"
              },
              "type": "array"
            },
            "limit": {
              "description": "Maximum number of rows to return (defaults to 100).",
              "example": 100,
              "format": "int32",
              "minimum": 0,
              "type": "integer"
            },
            "measures": {
              "description": "Aggregations to compute (count, sum, avg, etc.). Empty list returns row counts only.",
              "example": [
                "session_count",
                "avg_duration_ms"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "order_by": {
              "description": "Sort spec applied after aggregation. Empty list yields unspecified order.",
              "items": {
                "$ref": "#/$defs/ReportOrderBy"
              },
              "type": "array"
            },
            "time_range": {
              "$ref": "#/$defs/ReportTimeRange",
              "description": "Time window for the query. The dataset selects which timestamp column the range applies to."
            }
          },
          "required": [
            "dataset",
            "time_range"
          ],
          "type": "object"
        },
        "ReportTimeRange": {
          "description": "Half-open time window applied to the dataset's primary timestamp column\nduring a report query. `from` is inclusive, `to` is exclusive.",
          "properties": {
            "from": {
              "description": "Start of the window (RFC 3339, inclusive).",
              "example": "2026-04-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            },
            "to": {
              "description": "End of the window (RFC 3339, exclusive).",
              "example": "2026-05-24T00:00:00Z",
              "format": "date-time",
              "type": "string"
            }
          },
          "required": [
            "from",
            "to"
          ],
          "type": "object"
        },
        "SavedReportDashboardMetadata": {
          "description": "Dashboard placement metadata attached to a `SavedReport`. Captures how\nand where to render the report in the operator dashboard UI.",
          "properties": {
            "chart_type": {
              "description": "Rendering hint for the UI (e.g. `\"line\"`, `\"bar\"`, `\"table\"`,\n`\"big_number\"`). Free-form string — the server doesn't enforce a\nclosed set so new chart types can roll out client-side first.",
              "type": [
                "string",
                "null"
              ]
            },
            "position": {
              "description": "Sort key within `section`. Lower values render first. `None` means\n\"place at the end\".",
              "format": "int32",
              "type": [
                "integer",
                "null"
              ]
            },
            "section": {
              "description": "Dashboard section/group this report belongs to (free-form bucket name\nlike `\"Operations\"` or `\"Finance\"`). Used to cluster related reports\nin the UI.",
              "type": [
                "string",
                "null"
              ]
            },
            "title": {
              "description": "Human-readable title. Safe to render in user-facing messages.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "UpdateSavedReportRequest": {
          "description": "Request body for the `update_saved_report` operation.",
          "properties": {
            "dashboard": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SavedReportDashboardMetadata",
                  "description": "Replace dashboard placement metadata. Omit to keep current placement;\nsend `null` to detach the report from its dashboard; send an object\nto overwrite the placement."
                }
              ]
            },
            "description": {
              "description": "Human-readable description. Safe to render in user-facing messages.",
              "example": "Rolling 60-day window; widened from 30d after the Q3 product launch.",
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "description": "Human-readable name. Safe to render in user-facing messages.",
              "example": "Weekly active agents — last 60 days",
              "type": [
                "string",
                "null"
              ]
            },
            "query": {
              "$ref": "#/$defs/ReportQuery",
              "description": "Replace the saved report's query wholesale. Omit to keep the existing\nquery; send a new `ReportQuery` to swap it."
            }
          },
          "type": "object"
        }
      },
      "properties": {
        "report_id": {
          "description": "Saved report's prefixed public identifier.",
          "format": "uuid",
          "type": "string"
        },
        "request": {
          "$ref": "#/$defs/UpdateSavedReportRequest"
        }
      },
      "required": [
        "report_id",
        "request"
      ],
      "type": "object"
    },
    "method": "PATCH",
    "name": "update_saved_report",
    "output_shape": "unknown",
    "path": "/v1/reports/saved/{report_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "schedules",
    "cli": null,
    "description": "Update a durable schedule.",
    "input_schema": {
      "$defs": {
        "ScheduleTarget": {
          "description": "Target for a schedule - either a workflow or activity",
          "example": {
            "input": {
              "session_id": "session_01933b5a00007000800000000000001"
            },
            "name": "session.run",
            "type": "workflow"
          },
          "properties": {
            "input": {
              "description": "Input JSON for the workflow/activity"
            },
            "name": {
              "description": "Workflow type name or activity type name",
              "example": "session.run",
              "type": "string"
            },
            "type": {
              "description": "Target type: \"workflow\" or \"activity\"",
              "example": "workflow",
              "type": "string"
            }
          },
          "required": [
            "type",
            "name"
          ],
          "type": "object"
        },
        "UpdateScheduleRequest": {
          "description": "Update schedule request",
          "properties": {
            "catch_up_missed": {
              "description": "Catch up missed triggers",
              "example": true,
              "type": [
                "boolean",
                "null"
              ]
            },
            "cron_expression": {
              "description": "New cron expression. Standard `min hour day-of-month month day-of-week` form.",
              "example": "0 8 * * 1-5",
              "type": [
                "string",
                "null"
              ]
            },
            "description": {
              "description": "New description",
              "example": "Fires the support-triage agent every weekday at 08:00 UTC",
              "type": [
                "string",
                "null"
              ]
            },
            "enabled": {
              "description": "Enable/disable",
              "example": true,
              "type": [
                "boolean",
                "null"
              ]
            },
            "max_catch_up": {
              "description": "Max catch-up executions",
              "example": 3,
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "max_concurrent": {
              "description": "Max concurrent executions",
              "example": 1,
              "format": "int32",
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "retry_policy": {
              "description": "Retry policy (provider-specific JSON; see the durable engine's `RetryPolicy`).\nExample: `{\"max_attempts\": 3, \"initial_backoff_secs\": 30, \"backoff_multiplier\": 2.0}`."
            },
            "target": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/ScheduleTarget",
                  "description": "New target. Variant shape is defined on `ScheduleTarget`."
                }
              ]
            },
            "timezone": {
              "description": "New timezone (IANA name).",
              "example": "America/New_York",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateScheduleRequest"
        },
        {
          "properties": {
            "schedule_id": {
              "description": "Schedule's prefixed public identifier.",
              "format": "uuid",
              "type": "string"
            }
          },
          "required": [
            "schedule_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_schedule",
    "output_shape": "unknown",
    "path": "/v1/durable/schedules/{schedule_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "sessions",
    "cli": "sessions update",
    "description": "Update session title, tags, or locale.",
    "input_schema": {
      "$defs": {
        "UpdateSessionRequest": {
          "description": "Request to update a session. Only provided fields will be updated.",
          "properties": {
            "agent_identity_id": {
              "description": "Optional resident agent identity used for unattended/background execution.",
              "example": "identity_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "goal": {
              "description": "Updated session objective.",
              "example": "Summarize the incident and list remediations",
              "type": [
                "string",
                "null"
              ]
            },
            "locale": {
              "description": "Session locale (BCP 47, e.g. `uk-UA`).",
              "example": "uk-UA",
              "type": [
                "string",
                "null"
              ]
            },
            "tags": {
              "description": "Tags for organizing and filtering sessions.",
              "example": [
                "resolved"
              ],
              "items": {
                "type": "string"
              },
              "type": [
                "array",
                "null"
              ]
            },
            "title": {
              "description": "Human-readable title for the session.",
              "example": "Updated session title",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateSessionRequest"
        },
        {
          "properties": {
            "session_id": {
              "description": "Session's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "session_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_session",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "skills",
    "cli": "skills update",
    "description": "Update a skill. Only provided fields are changed.",
    "input_schema": {
      "$defs": {
        "SkillStatus": {
          "description": "Skill lifecycle status",
          "enum": [
            "active",
            "disabled",
            "archived",
            "deleted"
          ],
          "type": "string"
        },
        "UpdateSkillRequest": {
          "description": "Request to update a skill",
          "properties": {
            "skill_md": {
              "description": "Updated SKILL.md content (re-parses frontmatter)",
              "type": [
                "string",
                "null"
              ]
            },
            "status": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SkillStatus",
                  "description": "Update status"
                }
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateSkillRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Update a skill. Only provided fields are changed."
    },
    "method": "PATCH",
    "name": "update_skill",
    "output_shape": "unknown",
    "path": "/v1/skills/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "workspaces",
    "cli": null,
    "description": "Update a Workspace's mutable fields.",
    "input_schema": {
      "$defs": {
        "UpdateWorkspaceRequest": {
          "properties": {
            "description": {
              "type": [
                "string",
                "null"
              ]
            },
            "name": {
              "type": [
                "string",
                "null"
              ]
            },
            "status": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateWorkspaceRequest"
        },
        {
          "properties": {
            "workspace_id": {
              "description": "Workspace ID (wsp_<32-hex>).",
              "type": "string"
            }
          },
          "required": [
            "workspace_id"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PATCH",
    "name": "update_workspace",
    "output_shape": "unknown",
    "path": "/v1/workspaces/{workspace_id}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "files",
    "cli": null,
    "description": "Update a file in the session filesystem.",
    "input_schema": {
      "$defs": {
        "UpdateFileRequest": {
          "description": "Request to update a file",
          "properties": {
            "content": {
              "description": "New file content",
              "example": "# Project notes (rev 2)\n\nUpdated migration plan with rollback steps.\n",
              "type": [
                "string",
                "null"
              ]
            },
            "encoding": {
              "description": "Content encoding: \"text\" or \"base64\". Defaults to text.",
              "example": "text",
              "type": [
                "string",
                "null"
              ]
            },
            "is_readonly": {
              "description": "Whether file is read-only",
              "example": false,
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpdateFileRequest"
        },
        {
          "properties": {
            "path": {
              "type": "string"
            },
            "session_id": {
              "description": "Session's prefixed public identifier.",
              "type": "string"
            }
          },
          "required": [
            "session_id",
            "path"
          ],
          "type": "object"
        }
      ]
    },
    "method": "PUT",
    "name": "update_workspace_file",
    "output_shape": "unknown",
    "path": "/v1/sessions/{session_id}/fs/{path}",
    "positional_arg": null,
    "read_only": false
  },
  {
    "category": "agents",
    "cli": "agents upsert",
    "description": "Upsert agent — create (201) or update (200) by ID.",
    "input_schema": {
      "$defs": {
        "AgentCapabilityConfig": {
          "description": "Per-agent capability configuration\n\nAssociates a capability with an agent, including optional per-agent configuration.\nThe config field allows the same capability to behave differently per-agent.",
          "properties": {
            "config": {
              "description": "Per-agent configuration for this capability (capability-specific)"
            },
            "ref": {
              "description": "Reference to the capability ID",
              "type": "string"
            }
          },
          "required": [
            "ref"
          ],
          "type": "object"
        },
        "BTreeMap": {
          "additionalProperties": {
            "description": "Session-, agent-, or harness-scoped remote MCP server configuration.\n\nThis intentionally mirrors the `mcpServers` object shape used by common MCP\nclient config files while staying within Everruns' current remote-HTTP-only\nsupport.",
            "properties": {
              "actsAs": {
                "$ref": "#/$defs/McpServerActsAs",
                "description": "Identity whose grant this attachment requests."
              },
              "args": {
                "description": "Arguments passed to the stdio `command`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "auth_mode": {
                "$ref": "#/$defs/McpServerAuthMode",
                "description": "Authentication mode used when executing tools from this scoped server."
              },
              "command": {
                "description": "Executable to spawn for a stdio transport server.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "env": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Environment variables set for the stdio `command`.",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "headers": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Additional HTTP headers sent on MCP requests (HTTP transport only).",
                "propertyNames": {
                  "type": "string"
                },
                "type": "object"
              },
              "oauth_provider_id": {
                "description": "Provider id used to resolve a user-scoped bearer token.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "protocol_mode": {
                "$ref": "#/$defs/McpProtocolMode",
                "description": "Protocol-era adoption policy for the MCP client (`auto` negotiates)."
              },
              "tool_discovery": {
                "description": "Whether to discover tool definitions live from this server.",
                "type": "boolean"
              },
              "type": {
                "$ref": "#/$defs/McpServerTransportType",
                "description": "MCP transport type. Only remote HTTP is supported today."
              },
              "url": {
                "description": "URL of the remote MCP server endpoint. Required for HTTP transport;\nempty/ignored for stdio.",
                "type": "string"
              },
              "use": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/$defs/McpServerPresetRef",
                    "description": "Organization catalog preset that supplies transport and authentication policy."
                  }
                ]
              }
            },
            "type": "object"
          },
          "propertyNames": {
            "type": "string"
          },
          "type": "object"
        },
        "BuiltinTool": {
          "description": "Built-in tool configuration\n\nNote: The `kind` field has been removed. Tools are now identified\nsolely by their `name` field, and execution happens via the ToolRegistry\nwhich looks up tools by name.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering (e.g., \"Get Current Time\" for `get_current_time`)",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for registry lookup)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            },
            "policy": {
              "$ref": "#/$defs/ToolPolicy",
              "description": "Tool policy (auto or requires_approval)"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ClientSideTool": {
          "description": "Client-side tool - executed by the client, not the server\nThe server pauses execution and waits for the client to submit results.",
          "properties": {
            "category": {
              "description": "Category for tool_search namespace grouping (from parent capability)",
              "type": [
                "string",
                "null"
              ]
            },
            "deferrable": {
              "$ref": "#/$defs/DeferrablePolicy",
              "description": "Whether this tool's schema can be deferred via tool_search"
            },
            "description": {
              "description": "Tool description for LLM",
              "type": "string"
            },
            "display_name": {
              "description": "Human-readable display name for UI rendering",
              "type": [
                "string",
                "null"
              ]
            },
            "full_parameters": {
              "description": "Original full parameter schema saved by `DeferSchemaHook` before stripping.\nSerialized only when present so durable reason-to-act scheduling can\npreserve deferred schemas for `tool_search` in the act phase."
            },
            "hints": {
              "$ref": "#/$defs/ToolHints",
              "description": "Semantic hints describing the tool's behavioral properties"
            },
            "name": {
              "description": "Tool name (used by LLM and for correlation)",
              "type": "string"
            },
            "parameters": {
              "description": "JSON schema for tool parameters"
            }
          },
          "required": [
            "name",
            "description",
            "parameters"
          ],
          "type": "object"
        },
        "ConversationStarter": {
          "description": "A conversation starter shown on a fresh Platform Chat thread.\nSelecting one inserts its text into the composer. `icon` reuses the\nharness icon name set (`HarnessIcon`); unknown names fall back to the\ndefault glyph.",
          "properties": {
            "icon": {
              "description": "Optional icon name from the harness icon set.",
              "example": "zap",
              "type": [
                "string",
                "null"
              ]
            },
            "text": {
              "description": "Prompt text inserted into the composer when selected.",
              "example": "Triage the newest P1",
              "type": "string"
            }
          },
          "required": [
            "text"
          ],
          "type": "object"
        },
        "CreateAgentRequest": {
          "description": "Request to create a new agent",
          "properties": {
            "capabilities": {
              "description": "Capabilities to enable for this agent with per-agent configuration.\nEach capability has a `ref` (capability ID) and optional `config`.",
              "example": [
                {
                  "config": {},
                  "ref": "current_time"
                },
                {
                  "config": {},
                  "ref": "web_fetch"
                }
              ],
              "items": {
                "$ref": "#/$defs/AgentCapabilityConfig"
              },
              "type": "array"
            },
            "default_model_id": {
              "description": "The ID of the default LLM model to use for this agent.\nIf not specified, the system default model will be used.",
              "example": "model_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "description": {
              "description": "A human-readable description of what the agent does.",
              "example": "Handles customer inquiries and support tickets",
              "type": [
                "string",
                "null"
              ]
            },
            "display_name": {
              "description": "Human-readable display name shown in UI.\nFalls back to `name` when absent.",
              "example": "Customer Support Agent",
              "type": [
                "string",
                "null"
              ]
            },
            "harness_id": {
              "description": "Harness ID used as this agent's base execution environment. If omitted,\nthe org's built-in `generic` harness is used.",
              "example": "harness_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "harness_name": {
              "description": "Addressable harness name. Alternative to `harness_id`.",
              "example": "generic",
              "type": [
                "string",
                "null"
              ]
            },
            "id": {
              "description": "Client-supplied agent ID (format: agent_{32-hex}).\nIf not provided, one is auto-generated.",
              "example": "agent_01933b5a00007000800000000000001",
              "type": [
                "string",
                "null"
              ]
            },
            "initial_files": {
              "description": "Starter files copied into each new session for this agent.",
              "example": [
                {
                  "content": "Always respond in formal English.\n",
                  "path": "INSTRUCTIONS.md"
                }
              ],
              "items": {
                "$ref": "#/$defs/InitialFile"
              },
              "type": "array"
            },
            "intro_markdown": {
              "description": "Markdown intro shown as an intro box on a fresh Platform Chat thread.\nImages are allowed. Wins over the harness intro. Hidden once the user\ninputs.",
              "example": "Hey, I'm Ava. Ask me anything about your account.",
              "type": [
                "string",
                "null"
              ]
            },
            "max_iterations": {
              "description": "Maximum number of LLM iterations per turn for this agent.",
              "example": 20,
              "minimum": 0,
              "type": [
                "integer",
                "null"
              ]
            },
            "mcpServers": {
              "$ref": "#/$defs/BTreeMap",
              "description": "Remote MCP servers scoped to this agent."
            },
            "name": {
              "description": "Name, unique per org. Lowercase alphanumeric and hyphens.\nFormat: lowercase alphanumeric and hyphens (e.g. \"customer-support\").",
              "example": "customer-support",
              "type": "string"
            },
            "network_access": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/NetworkAccessList",
                  "description": "Network access list controlling which hosts/URLs this agent's sessions can reach.\nIf set, merged with harness and session layers (allowed: intersect, blocked: union).\nExample shape is defined on `NetworkAccessList`."
                }
              ]
            },
            "parallel_tool_calls": {
              "description": "Request-level parallel tool calling preference (EVE-598). `true` signals\nthe provider that parallel tool calls are wanted; `false` requests at\nmost one tool call per turn and forces serial execution. Omit to use the\nprovider default.",
              "example": true,
              "type": [
                "boolean",
                "null"
              ]
            },
            "short_description": {
              "description": "One-line description in simplified Markdown, shown below the chat title\nonce the intro is hidden. Wins over the harness value.",
              "example": "Answers account questions in seconds.",
              "type": [
                "string",
                "null"
              ]
            },
            "starters": {
              "description": "Conversation starters for a fresh Platform Chat thread. Win over the\nharness starters when non-empty. `icon` reuses the harness icon set.",
              "items": {
                "$ref": "#/$defs/ConversationStarter"
              },
              "type": "array"
            },
            "system_prompt": {
              "description": "The system prompt that defines the agent's behavior and capabilities.\nThis is sent as the first message in every conversation.",
              "example": "You are a helpful customer support agent. Be polite and professional.",
              "type": "string"
            },
            "tags": {
              "description": "Tags for organizing and filtering agents.",
              "example": [
                "support",
                "customer-facing"
              ],
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "tools": {
              "description": "Client-side tools for this agent.\nThese tools are sent to the LLM but executed by the client, not the server.",
              "example": [
                {
                  "description": "Open URL in the user's browser",
                  "name": "open_url",
                  "parameters": {
                    "properties": {
                      "url": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "url"
                    ],
                    "type": "object"
                  },
                  "type": "client_side"
                }
              ],
              "items": {
                "$ref": "#/$defs/ToolDefinition"
              },
              "type": "array"
            }
          },
          "required": [
            "name",
            "system_prompt"
          ],
          "type": "object"
        },
        "DeferrablePolicy": {
          "description": "Controls whether a tool's full schema can be deferred (tool_search).\n\nWhen tool_search is active and a model supports it, tools marked as\n`Automatic` or `Always` will have `defer_loading: true` set, meaning\nonly the name+description are sent upfront and full parameter schemas\nare loaded on-demand by the model.",
          "enum": [
            "never",
            "automatic",
            "always"
          ],
          "type": "string"
        },
        "InitialFile": {
          "description": "Starter file copied into a new session from an agent or harness.",
          "properties": {
            "content": {
              "description": "File content: plain text or base64-encoded binary.",
              "type": "string"
            },
            "encoding": {
              "description": "Content encoding: `text` or `base64`.",
              "type": "string"
            },
            "is_readonly": {
              "description": "Prevent session-side edits or deletes when true.",
              "type": "boolean"
            },
            "path": {
              "description": "Absolute path within the session workspace. `/workspace` prefix is accepted.",
              "type": "string"
            }
          },
          "required": [
            "path",
            "content"
          ],
          "type": "object"
        },
        "McpProtocolMode": {
          "description": "Per-server policy for which MCP protocol era the client uses.\n\n`Auto` (the default) probes the server and adapts — it tries the stateless\n`2026-07-28` path first and transparently falls back to the stateful\nhandshake when a server demands it, so a single configuration speaks to\nevery era without operator action. The pinned variants skip negotiation when\nan operator knows a server's era (or to work around a server that\nmis-signals it).\n\nWire values are the version dates. The pre-release names (`legacy`,\n`stable`, `rc`) stay accepted as deserialization aliases so stored config\nkeeps loading, but they are no longer emitted.",
          "enum": [
            "auto",
            "2025-03-26",
            "2025-06-18",
            "2026-07-28"
          ],
          "example": "auto",
          "type": "string"
        },
        "McpServerActsAs": {
          "description": "Identity whose OAuth grant a scoped MCP attachment requests.",
          "enum": [
            "none",
            "service",
            "user"
          ],
          "example": "service",
          "type": "string"
        },
        "McpServerAuthMode": {
          "description": "MCP server authentication mode.",
          "enum": [
            "none",
            "api_key",
            "oauth"
          ],
          "example": "api_key",
          "type": "string"
        },
        "McpServerPresetRef": {
          "description": "Reference to an organization MCP server catalog entry.",
          "example": "catalog:linear",
          "type": "string"
        },
        "McpServerTransportType": {
          "description": "MCP Server transport type.",
          "enum": [
            "http",
            "stdio"
          ],
          "example": "http",
          "type": "string"
        },
        "NetworkAccessList": {
          "description": "Network access list controlling which hosts/URLs an agent session can reach.\n\n- `allowed`: if non-empty, only URLs matching these patterns are permitted.\n- `blocked`: URLs matching these patterns are always denied (takes precedence over allowed).\n\nPattern format:\n- `example.com` — exact domain match (any port, any path)\n- `*.example.com` — domain and all subdomains\n- `https://example.com/api/` — exact URL prefix (scheme + host + path)",
          "example": {
            "allowed": [
              "*.example.com",
              "https://api.acme.com/"
            ],
            "blocked": [
              "169.254.169.254"
            ]
          },
          "properties": {
            "allowed": {
              "description": "Allowed host patterns. If non-empty, only matching URLs are permitted.\nAn empty list means \"no restriction from this layer\" (inherit parent).",
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            "blocked": {
              "description": "Blocked host patterns. Always denied, even if matched by `allowed`.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "type": "object"
        },
        "SideEffectClass": {
          "description": "How many times a tool call may safely be executed given the same inputs.\n\nUsed by the durable Act activity (EVE-530) to decide what to do when a\nprior execution attempt left a `running` claim in `durable_tool_results`:\n\n* `Pure` / `Idempotent` — the running claim is stale; re-execute freely.\n* `AtMostOnce` — never re-execute from a stale running claim; settle it\n  as `interrupted` and surface an uncertain result to the model instead.\n\nWhen unset (`None`), the conservative default is `AtMostOnce`.",
          "enum": [
            "Pure",
            "Idempotent",
            "AtMostOnce"
          ],
          "type": "string"
        },
        "ToolDefinition": {
          "description": "Tool definition in agent configuration",
          "oneOf": [
            {
              "allOf": [
                {
                  "$ref": "#/$defs/BuiltinTool",
                  "description": "Built-in tool - executed by the worker via ToolRegistry"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "builtin"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Built-in tool - executed by the worker via ToolRegistry"
            },
            {
              "allOf": [
                {
                  "$ref": "#/$defs/ClientSideTool",
                  "description": "Client-side tool - executed by the client, not the server"
                },
                {
                  "properties": {
                    "type": {
                      "enum": [
                        "client_side"
                      ],
                      "type": "string"
                    }
                  },
                  "required": [
                    "type"
                  ],
                  "type": "object"
                }
              ],
              "description": "Client-side tool - executed by the client, not the server"
            }
          ]
        },
        "ToolHints": {
          "description": "Semantic hints describing a tool's behavioral properties.\n\nFollows the MCP tool annotations convention (readOnlyHint, destructiveHint,\nidempotentHint, openWorldHint) plus everruns-specific hints. All fields are\noptional booleans — `None` means \"unknown/unspecified\". Consumers should\ntreat `None` as the conservative default (e.g., assume not readonly, assume\nnot idempotent).\n\nThese hints are informational — they do not enforce policy. Use `ToolPolicy`\nfor execution gating (auto vs requires_approval).",
          "properties": {
            "capability_id": {
              "description": "Capability that contributed this tool definition.\n\nReporting uses this attribution only as metadata. It must never contain\ntool arguments, results, prompts, or any other sensitive payload.",
              "type": [
                "string",
                "null"
              ]
            },
            "capability_name": {
              "description": "Human-readable capability name snapshot for reporting.",
              "type": [
                "string",
                "null"
              ]
            },
            "concurrency_class": {
              "description": "Scheduling conflict key. Tool calls within the same act batch that share\na non-empty `concurrency_class` are executed sequentially in arrival\norder; calls in different classes (or with no class) run concurrently.\n\nSet this on tools that mutate shared session state so that, e.g., two\nfile writes or two SQL mutations in one batch do not race. Read-only\ntools should leave this `None` so they always parallelize. See\n`everruns-engine`'s tool scheduler for how the act phase consumes it.",
              "type": [
                "string",
                "null"
              ]
            },
            "cpu_bound": {
              "description": "Tool performs significant CPU-bound or otherwise non-yielding work in\nprocess (e.g. an in-process interpreter). When true, the act scheduler\nruns the call on its own task (`tokio::spawn`) so a long CPU burst does\nnot starve the cooperative polling of I/O-bound tools in the same batch.\n\nDistinct from `long_running`, which describes wall-clock time for\nI/O-bound work (those tools yield at await points and need no offload).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "destructive": {
              "description": "Tool may irreversibly destroy or delete data.\nSubset of non-readonly — a tool can be non-readonly (writes) without\nbeing destructive (e.g., create/update operations).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "idempotent": {
              "description": "Calling the tool repeatedly with the same arguments produces the same\neffect. Safe to retry on transient failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "long_running": {
              "description": "Tool may take significant time to complete (> ~5s typical).\nUseful for clients to show progress indicators and set timeouts.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "metadata": {
              "description": "Host-owned annotations that core does not interpret.\n\nThe typed hints above are the vocabulary core itself reasons about. This\nis the escape hatch for everything a *host* wants to carry alongside a\ntool — risk tiers for an approval UI, presentation hints, an embedder's\nrouting keys — without adding a field to core for each one. Core reads\nnothing here and no driver sends it to a provider; it travels with the\ndefinition so a consumer sees it at the point of decision (e.g. a\n`PreToolUseHook` gating on what the tool declared).\n\nThe schema belongs to whoever writes it. Never put credentials or other\nsensitive payload here: like the rest of the definition, it is persisted\nand surfaced to clients."
            },
            "narration_noun": {
              "description": "Entity noun for operation-based narration (e.g. \"agent\", \"harness\").\nWhen set, the narration system reads the `operation` argument and\nproduces verb-based narration like \"Created agent: Neon Cartographer\"\ninstead of the generic \"Ran Manage Agents\".",
              "type": [
                "string",
                "null"
              ]
            },
            "open_world": {
              "description": "Tool interacts with external entities beyond the local system\n(network calls, third-party APIs, cloud services).",
              "type": [
                "boolean",
                "null"
              ]
            },
            "persist_output": {
              "description": "Tool output should be persisted to session VFS before truncation.\nWhen set, the `tool_output_persistence` capability (EVE-222, EVE-245) writes\nstdout to `/outputs/{tool_call_id}.stdout` and stderr to\n`/outputs/{tool_call_id}.stderr`, injecting `full_output`, `total_lines`,\nand `output_files` into the result.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "readonly": {
              "description": "Tool does not modify any state (read-only queries, lookups).\nWhen true: safe to call speculatively, result can be cached.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "requires_secrets": {
              "description": "Tool requires API keys, credentials, or other secrets to function.\nUseful for UI to show connection prompts and for LLMs to anticipate\nauthentication failures.",
              "type": [
                "boolean",
                "null"
              ]
            },
            "side_effect_class": {
              "oneOf": [
                {
                  "type": "null"
                },
                {
                  "$ref": "#/$defs/SideEffectClass",
                  "description": "Replay-safety class used by the durable Act activity (EVE-530).\n\nControls what happens when a worker reclaims a stale `running` claim:\n`Pure`/`Idempotent` tools are re-executed; `AtMostOnce` tools are\nsettled as `interrupted` to prevent double side-effects.\n\n`None` is treated conservatively as `AtMostOnce`."
                }
              ]
            },
            "supports_background": {
              "description": "Tool supports detached background execution via `spawn_background`.\nWhen true, the tool may be executed asynchronously outside the current\nforeground tool call and report status back later.",
              "type": [
                "boolean",
                "null"
              ]
            }
          },
          "type": "object"
        },
        "ToolPolicy": {
          "description": "Tool policy determines how tool calls are handled",
          "enum": [
            "auto",
            "requires_approval",
            "client_side"
          ],
          "type": "string"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/CreateAgentRequest"
        },
        {
          "properties": {
            "id": {
              "description": "Prefixed public identifier. See [ID Schema](https://docs.everruns.com/advanced/id-schema/).",
              "type": "string"
            }
          },
          "required": [
            "id"
          ],
          "type": "object"
        }
      ],
      "description": "Upsert agent — create or update by ID."
    },
    "method": "PUT",
    "name": "upsert_agent",
    "output_shape": "unknown",
    "path": "/v1/agents/{id}",
    "positional_arg": "id",
    "read_only": false
  },
  {
    "category": "agents",
    "cli": null,
    "description": "Create or update an agent check rule (built-in override or custom rule).",
    "input_schema": {
      "$defs": {
        "UpsertCheckRuleRequest": {
          "description": "Upsert request for a single rule (keyed by rule_id in the path).",
          "properties": {
            "category": {
              "type": [
                "string",
                "null"
              ]
            },
            "enabled": {
              "type": "boolean"
            },
            "kind": {
              "description": "`builtin_override`, `declarative`, or `nl_rubric`.",
              "type": "string"
            },
            "match_mode": {
              "type": [
                "string",
                "null"
              ]
            },
            "message": {
              "type": [
                "string",
                "null"
              ]
            },
            "pattern": {
              "type": [
                "string",
                "null"
              ]
            },
            "rubric": {
              "type": [
                "string",
                "null"
              ]
            },
            "severity": {
              "description": "Optional everywhere. For `builtin_override` it overrides the rule's\ndefault severity; for custom rules it sets the finding severity,\ndefaulting to `warning` for `declarative` and `info` for `nl_rubric`\nwhen omitted.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "enabled"
          ],
          "type": "object"
        }
      },
      "allOf": [
        {
          "$ref": "#/$defs/UpsertCheckRuleRequest"
        },
        {
          "properties": {
            "rule_id": {
              "description": "Built-in rule id (for `builtin_override`) or `custom.<slug>` (custom).",
              "type": "string"
            }
          },
          "required": [
            "rule_id"
          ],
          "type": "object"
        }
      ],
      "description": "Create or update a single check rule."
    },
    "method": "PUT",
    "name": "upsert_agent_check_rule",
    "output_shape": "unknown",
    "path": "/v1/agents/check-rules/{rule_id}",
    "positional_arg": null,
    "read_only": false
  }
]
