---
description: List available LLM models on Everruns
argument-hint: ""
---

List models via the MCP `execute` tool calling `list_models`.

- Render: id, provider, display name, context window, pricing (if exposed).
- Group by provider.
- Note which model is the default for new agents if the API surfaces it.
