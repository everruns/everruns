---
description: Show the current Everruns user and active organization
---

Call the `me` MCP tool. If it fails with an auth error, tell the user to complete the OAuth flow in the browser Claude Code opens.
