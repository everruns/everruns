---
description: Show the current Everruns user and active organization
argument-hint: ""
---

Call the Everruns `me` MCP tool and report the authenticated user's profile and the active organization. If the user belongs to multiple organizations, also call `list_organizations` and surface them so the user knows which org is default and what else they could switch to via `switch_organization`.
