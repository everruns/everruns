---
description: Search the Everruns API catalog for operations
argument-hint: "<query> | --all"
---

Call the `discover` MCP tool. With `--all`, pass `all: true`; otherwise pass the text as `query`. See the `everruns` skill for examples.

Arguments: `$ARGUMENTS`
