---
description: Run a bash script against the Everruns API (every operation is a builtin)
argument-hint: "<bash script>"
---

Call the `execute` MCP tool with the provided script as `commands`. Every Everruns API operation is a bash builtin; `jq` is available. See the `everruns` skill for the command inventory, patterns, and examples.

Arguments: `$ARGUMENTS`
